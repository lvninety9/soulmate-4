# 인계 — 새 세션은 이 문서부터

> 갱신 2026-09-05 (라운드 50 — 턴 안에서 쌓이는 미커밋 작업 고지, **7-10절**. 그리고 **오늘 사이클을 여기서 닫습니다 — 8-1절이 그 판단**) · `github.com/lvninety9/soulmate-4` `origin/master` **318커밋** (`c15b42c`, **라운드 50까지 push 완료** — 독립 fresh clone으로 7스위트 ALL PASS·check-caps EXIT=0 재검증함). 로컬 미push 커밋 없음(템플릿). `warms-mobile`은 커밋 여러 개 로컬에만 있음(라운드분 + 실사용 진행분) — **push 여부는 무의미**(Jay 지시: warms-mobile은 계측용 스크래치일 뿐, 실제 산출물은 템플릿 쪽).
>
> **다음 세션 우선순위**: **8-1절(사이클 마무리 판단)을 먼저 읽으세요.** 라운드를 더 열지 말고
> 그냥 쓰다가, 실사용에서 하네스 결함이 실제로 나올 때만 그 결함 하나를 들고 여세요(7절의 원래 규칙).
> warms-mobile HEAD `7cd5d8a`+테스트 백포트. primer 마지막 터치는 `6dea13f`이고 그 뒤 **2커밋**
> (임계 4)이라 **다음 라이브 턴은 체크포인트 없이 시작합니다**. 라운드 48의 번호-계획 대조 검사는
> **여전히 한 번도 실전 발동한 적이 없습니다** — primer 터치 커밋에서만 도는데, 이제 `6dea13f`가
> primer를 건드렸으므로 **그 다음 primer 커밋이 첫 기회**입니다. 라운드 49의 섞인-턴 팩트체크와
> 라운드 50의 턴중 고지는 **둘 다 라이브 미검증**(각 절 마지막 참조).
> **warms-mobile 콘텐츠(SESSION_PRIMER.md·PROJECT_BACKGROUND.md·게임 코드) 자체는 고치지 말 것**
> — Jay 명시 지시, 이건 관찰 대상이지 수정 대상이 아님.
>
> 라운드 39~50의 각 라운드 커밋은 전부 `warms-mobile`(`/media/jay/D/cursor/tossinapp/warms-mobile`)에도
> 원본이 있습니다 — 템플릿 스크래치 클론이 사라져도 그 저장소에서
> `git format-patch` 로 언제든 재추출해 템플릿 fresh clone에 `git am` 하면 됩니다(라운드 39에서 검증된 경로).
> 이 폴더의 나머지 문서: `README.md`(사용법) · `MEASUREMENTS.md`(실측값) · `HISTORY.md`(35라운드 서사)
>
> **새 세션은 이 문서만 읽으면 이어갈 수 있습니다.** 나머지는 필요할 때 참조하세요.

---

## 1. 이 프로젝트가 무엇이고 왜 하는가

**로컬 LLM(Qwen3.6-35B-A3B, RTX 3080)이 코딩 에이전트로 실제 일을 하게 만드는 것.**

전제: **모델을 똑똑하게 만들 수는 없다. 작업을 모델의 능력 안으로 자를 수는 있다.**
soulmate-4는 그 절단선을 기계적으로 강제하는 하네스입니다.

Hermes의 목표와 같은 방향입니다 — *"로컬 Qwen만으로 자동화가 스스로 돌아가고, Claude는
그 로직을 미리 설계·검증하는 개발도구"*. 그래서 **설계는 유료 모델, 실행은 로컬, 검증은 기계**입니다.

**계보** (헷갈리지 말 것):
- `soulmate` — 원본, Claude Code용. **87/100 턴키 · 98.75/100 구조** ← soulmate-4의 점수 목표
- `soulmate-3` — Continue용 파생
- `soulmate-4` — Kilo Code용. **soulmate-3에서 씨앗**, 2026-08-08 (저장소 README에 기록)

---

## 2. 현재 상태 (Opus가 직접 재현 검증, 2026-08-25)

```
260커밋 · origin/master 동기화 완료 (라운드 37까지)
```

### 테스트 — 6개 스위트 전부 ALL PASS

| 스위트 | 대상 |
|---|---|
| `subtask-gate` (39) | 게이트 차단 로직 |
| `subtask-report` (48) | 1층 보고서 |
| `check-secrets` (25) | 시크릿 차단 |
| `check-caps.regression` (63) | 캡 검사 |
| `stale-language.fuzz` (42) | 정규식 스윕 |
| `subtask-review-llm` (26) | 2층 LLM 리뷰 |

`check-caps.sh` **EXIT=0** · required-read **25,852/27,800자** · rule-archive **365/450줄**

⚠️ 이 두 숫자는 2026-08-26에 `b958b42`에서 **직접 재측정한 값**입니다. 라운드 37이 적어둔
값(24,535 / 370줄)은 깨끗한 `185bca2`에서 실측하니 25,544 / 365로 나왔습니다 — 다음 세션이
"시작 상태 재현"으로 대조할 숫자였으므로 여기서 정정합니다. **숫자는 항상 직접 돌려서
확인할 것**(3-2절과 같은 계열의 문서-실제 불일치).

### 구조 — 강제는 두 곳뿐

```
pre-commit    차단   시크릿 · 민감파일명            ← 되돌릴 수 없는 것
subtask-gate  차단   sub-task 경계                  ← 모델이 우회를 시도한 것(실측)
post-commit   보고   1층: 테스트·린트·CSS·잔재물     ← 도구가 확정 판정
              보고   2층: 로컬 모델이 diff를 읽고 지적 ← 도구가 못 잡는 것
사람          판단   "확인이 필요한 것" 몇 줄
```

**판단 기준**: *(a) 되돌릴 수 없거나 (b) 무시된다고 실측된 것에만 강제. 나머지는 보고만.*

근거 — 실측: 하드캡 준수율 **100%**, soft WATCH 준수율 **0%**.
반대 근거 — 실측: 테스트 출력을 주면 모델이 **스스로 고침**(통과 9건 중 3건이 재시도 성공).
그래서 테스트 실패는 강제하지 않습니다.

### 라이브 검증 완료 (Opus가 결함을 심어 직접 확인)

- **게이트**: 프로덕션 세션에서 **20/20 차단, 우회 0회**
- **시크릿 차단**: fresh 부트스트랩 프로젝트에서 실제 커밋 시도 → **차단됨**.
  `.env.example`·`DEBUG=true`·`PORT=3000`은 통과(오탐 없음)
- **2층**: n=16 측정 **11/16 (68.75%)** — 상세는 4절
- **훅 stale 감지**: 훅을 조작하니 `BOOTSTRAP FAIL` + `EXIT=1` + 복구 명령 (라운드 37)

---

## 3. 특이사항 — 반드시 알아야 할 것

### 3-1. 2층은 보장이 아니라 그물입니다 (오늘 실측)

`discount.py`의 같은 버그를 **첫 호출에서 0건으로 놓쳤다가 두 번째에 잡았습니다.**
차이는 diff 범위였습니다(1커밋 → 2커밋).

> 같은 버그·같은 모델인데 **입력 형태가 달라지자 결과가 바뀌었습니다.**

그래서 `[layer2/local-llm, unverified]` 태깅과 **report-only(비차단)** 가 정확한 설계입니다.
1층(도구, 확정)과 절대 섞이지 않습니다. **놓치는 층으로 커밋을 막으면 안 됩니다.**

### 3-2. `.git/hooks/`는 git에 안 들어갑니다

오늘 실제로 물렸습니다 — `SESSION_PRIMER.md`는 *"시크릿 차단 중"*이라고 적혀 있었는데
설치된 훅은 구버전이라 `check-secrets` 참조가 **0건**이었습니다. **문서가 작동한다고
적어도 실제로는 아닐 수 있습니다.** (재설치 완료)

`check-caps.sh`가 훅 **존재**는 검사하지만 **최신인지**는 검사하지 않습니다 → 남은 작업 참조.

### 3-3. 오늘 시간을 잡아먹은 함정 둘

1. **플러그인은 `kilo serve` 데몬 시작 시 1회 로드.** 실행 중 `.kilo/plugins/*.ts`를 바꿔도
   반영 안 되고, Cursor에서 "New Session"을 열어도 같은 데몬을 재사용합니다.
2. **모델명이 stale이면 CLI가 `exit 0` + 출력 0바이트**로 끝납니다 — 행(hang)과 구분 불가.
   `kilo run`은 항상 `-m`을 명시할 것.

### 3-4. 세션은 짧게

실측: 긴 세션에서 모델이 **18턴 연속 허위 완료보고**(파일 생성·커밋·테스트 통과 전부 허구).
새 세션에서는 1회 차단 후 정확히 정지하고 정직하게 보고했습니다.
**한 sub-task = 한 세션.** 품질 규칙이면서 컨텍스트 용량 규칙입니다.

---

## 4. 미비된 구간 — 다음 세션이 할 일

### ✅ ①②③ — 라운드 37에서 종결됨 (Opus 재현 검증 완료)

**① 훅 최신성 검사** — `check-caps.sh:477`이 `diff -q`로 설치된 훅과 소스를 대조합니다.
Opus가 직접 훅을 조작해 확인: 정상 상태 0건 → 조작 후 `BOOTSTRAP FAIL` + `EXIT=1` +
복구 명령 제시. **오늘 Opus를 물었던 구멍이 기계적으로 막혔습니다.**

**② 2층 실효성 n=16 측정** — 16개 독립 repo에 결함 1종씩 심고 실제 서버 호출.

```
11/16 적중 (68.75%)
프롬프트 설계상 불가능한 2건 제외 시 → 11/14 (78.6%)
```

**잡는 것**: 비교연산자 뒤집힘 · 권한 완화 · off-by-one · 세금 2중적용 ·
mutable default · 인덱스 스왑 · 엉뚱한 함수 호출 · 날짜 사전식 비교 · 검증 우회

**못 잡는 것 (구체화됨)**:
- 암묵적 단위환산 (분↔초)
- `and`→`or` 접근권한 완화
- 주석과 반대로 정렬 (바로 윗줄 주석과 모순인데도 놓침)
- diff 밖 스키마가 필요한 것 (설계상 불가 — 프롬프트가 diff 밖 추측을 금지)

**정책 변경 없음. report-only 유지** — 68.75%는 *"무시됨이 증명됨"*이 아니라
*"부분적으로 유효한 그물"*입니다. n=2에서 본 그림이 n=16에서도 같았습니다.

**③ 줄 수 +56** — 라운드 37이 판단 완료. 근거는 `wiki/rule-archive.md` Round 37 참조.

---

### 남은 것 — 다음 세션 선택지

**A. 실제 프로젝트 하나를 이 하네스로 끝까지 만들기** ← **권장**

종료 기준으로 제안했던 그것입니다. 구조는 다 섰고 검증도 됐습니다. 이제 **실사용에서
뭐가 걸리는지**가 유일하게 남은 미지수입니다. `README.md`의 15단계 디펜스 게임 계획을
그대로 쓰면 됩니다 — 게임이 목적이 아니라 **하네스를 실전에서 돌려보는 계측기**입니다.

> 이 계획은 라운드 37까지 **어디에도 실재하지 않았습니다**(이 문서가 두 번 참조만 하고
> 있었음). 2026-08-26에 `README.md`에 실제로 작성했습니다 — 15행 표, 각 행에 상태·함수
> 시그니처와 **사람이 직접 볼 확인 한 줄**이 들어있습니다.

> **2026-08-26 변경 — 계측기는 디펜스 게임이 아니라 `warms-mobile`입니다** (Jay 지정).
> Worms(1995) 규칙을 축약한 **모바일 웹 2인 핫싯** 게임, 경로
> `/media/jay/D/cursor/tossinapp/warms-mobile` — 부트스트랩·15단계 분해·1세션 primer까지
> 완료돼 있고 `--bootstrap-check` EXIT=0입니다(`bc6ea4a`). 단계 표는 그 저장소의
> `wiki/PROJECT_BACKGROUND.md`에 있습니다. README의 디펜스 게임 표는 **다른 도메인에서
> 같은 형식이 어떻게 생겼는지 보여주는 예시로만** 남깁니다.

**A-1. 이번에 딱 하나 새로 시험하는 것 — 수용 한 줄** (Jay 제안, 2026-08-26)

각 단계 프롬프트의 마지막 줄에 *"내가 뭘 열어서/눌러서 뭐가 보여야 하는가"*를 붙입니다.
보고서의 "확인이 필요한 것"은 **도구가 판정 못 한 것**만 알려주지, *이 단계가 됐다는 게
뭘 보는 건지*는 알려주지 못합니다 — 그래서 모든 검사를 통과하고도 틀린 단계가 가능합니다.

**의도적으로 하네스에 넣지 않았습니다.** 프롬프트 레벨로만 돌려보고, 라운드 38이 끝날 때
`SESSION_PRIMER.md`의 sub-task 항목에 `verify:` 필드로 승격할지 그냥 버릴지 판정합니다
(쓰지도 않으면서 미리 다듬는 게 라운드 17~26을 태운 그 패턴입니다). 저장소 쪽엔 프롬프트
규칙 한 문단만 들어갔습니다(`README.md` bootstrapping 절, `eb1a48a`/`b958b42`).

⚠️ **이건 부족한 부분을 다듬어주지 않습니다 — 보이게 만들 뿐입니다.** 다듬는 건 그 다음
단계, 즉 실패한 확인 결과를 사람이 모델에 그대로 되먹이는 것이고, 이 루프는 이 프로젝트가
유일하게 실측한 자가수리 경로입니다(테스트 출력을 주면 스스로 고침 — Aider 통과 9건 중 3건).
그리고 수용 한 줄은 **`design` 시점에 사람/유료 모델이** 씁니다. 로컬 모델이 diff 보고
매번 생성하게 하면 39% 자율설계 구간이라 날조가 섞입니다.

**B. 2층의 약점 보완** (선택)
n=16이 못 잡는 걸 구체적으로 알려줬습니다. 프롬프트에 *"주석과 코드가 모순되면 지적하라"*,
*"단위(분/초/ms)가 함수 경계를 넘을 때 확인하라"* 같은 힌트를 넣으면 #6·#11류가 잡힐
가능성이 있습니다. **다만 이건 "문구 재작성"이라 역대 성공률 0/2입니다** — 하려면
n=16을 다시 돌려 before/after를 비교해야 의미가 있습니다.

**C. 아직 측정 안 된 것 3가지** (선택, 구조와 무관)
- Aider polyglot 전체 6개 언어 — Go·Rust·JDK 툴체인 설치 필요
- 반박 주입 실효성 — `kilo serve` 데몬 재시작 필요(Jay의 Cursor를 끊음)
- `kilo run` 무응답 근본원인 — 모델명 stale이 한 원인이지만 전부는 아님

---

## 5. Jay의 피드백 — 반드시 유지할 것

이 세션에서 Jay가 방향을 바로잡은 지점들입니다. **다음 세션도 이걸 기준으로 판단하세요.**

**"게임이 중요한 게 아니라 완벽한 구조를 만드는 것이 중요합니다. 게임은 테스트의 도구일 뿐입니다."**
→ 결함이 보이면 끝까지 고칩니다. 예제를 만드는 게 목적이 아닙니다.

**"검증할 게 얼마나 많은데 모델을 빼라는 거예요? … 그걸 제가 혼자 할 수는 없잖아요?"**
→ Opus의 *"검증에 LLM을 쓰지 마라"*가 너무 좁았습니다. 정정된 답:
**검증은 필수이고, 대부분은 판단이 아니라 도구 문제**입니다(semgrep·gitleaks·린터·커버리지).
도구가 없는 영역에만 로컬 모델을 report-only로 씁니다. 그게 2층입니다.

**"로컬이 스스로 할 수 있으면 강제성 없이도 되니, 강제를 최소화하고 최대 아웃풋을"**
→ 강제에도 비용이 있습니다. *"되돌릴 수 없거나 무시된다고 실측된 것"*에만 강제합니다.

**"상황마다 추가하지 않아도 똑똑하게 관리할 방법은 없는지"**
→ 1층은 열거식이라 끝이 없습니다. **2층이 그 답**입니다 — diff를 읽고 판단하는 건
열거가 필요 없고, 새 스택·새 결함 형태에도 그냥 작동합니다. 무료·무제한이고요.

**"컨텍스트가 부족하다고 해서 급하게 마무리하지 마세요. 새 세션의 유일한 힌트가 문서입니다."**
→ 이 문서가 그 원칙으로 쓰였습니다.

---

## 6. 세션 무결성 검토 (Jay 요청)

**강제 압축(컨텍스트 요약)은 없었습니다.** 이 세션은 처음부터 끝까지 연속이었고,
컨텍스트 경고(65~74%)만 있었지 요약 이벤트는 없었습니다.

**보고 vs 실제 일치 여부**: 라운드 30~36 각각을 **fresh clone으로 독립 재현**했습니다.
발견된 불일치는 전부 그 자리에서 정정했고, 아래가 전부입니다:

| 라운드 | 보고값 | 실측값 | 처리 |
|---|---|---|---|
| 29 | 필수읽기 5,563 tok · primer 101줄 | **6,344 tok · 147줄** | 핸드오프 커밋이 되채운 것 — 지적 후 하드캡으로 해결 |
| 31 | 커밋 5개 | 4개 | 사소, 기록만 |
| 34 | unit 35케이스 | 39단언 | 케이스 수 vs 단언 수 — 둘 다 맞음 |
| 35 | 줄 수 감축 목표 | **+56 증가** | 에이전트가 정직하게 미달 보고 |

**허위·날조는 없었습니다.** 미달은 미달이라고 보고됐습니다.

한 가지 짚을 것 — **Opus(저) 자신이 세 번 틀렸고 그때마다 정정했습니다**:
Q4 VRAM 추정(4,100 예상 → 실측 3,428), `question` 툴 결론(런타임 데이터만 보고 판단
→ API payload 확인 결과 라운드 28이 옳았음), 부트스트랩 배포 누락 의심(grep 오판 →
실제 부트스트랩해보니 정상). **전부 "추측 대신 실행"으로 해소됐습니다.**

---

## 7. 마무리가 가능한가 — Opus의 판단

**가능합니다. 그리고 생각보다 가깝습니다.**

근거: 오늘 라이브로 확인된 것들이 **전부 설계대로 작동합니다** — 게이트 20/20, 시크릿
차단(fresh 부트스트랩에서 실증), 2층 3/3, 오탐 0. 구조는 이미 서 있습니다.

**다만 "완벽"은 도달 불가능한 목표입니다.** 오늘 결함 10건이 전부 초록 테스트 상태에서
나왔고, 앞으로도 실제로 쓰면 또 나옵니다. 그건 이 도구가 나빠서가 아니라 **검증 도구의
본질**입니다.

그래서 **종료 기준을 "완벽"이 아니라 이렇게 잡으시길 권합니다**:

> **①~③이 닫히고, 실제 프로젝트 하나를 이 하네스로 끝까지 만들어보면 종료.**

**①②③은 라운드 37에서 닫혔습니다.** 남은 건 후반부 — **실사용 한 번**입니다.

즉 **구조 작업은 사실상 끝났습니다.** 지금부터는 "더 고칠 것을 찾는" 단계가 아니라
"실제로 써보고 걸리는 것만 고치는" 단계입니다. 그 전환이 중요합니다 —
라운드 17~26이 10라운드를 태운 게 정확히 *"쓰지도 않으면서 계속 다듬은 것"*이었습니다.

**2026-08-26 — 프로젝트 종료 처리(Jay 확인).** soulmate-4 저장소는 **유지보수 모드**입니다:
라운드를 더 열지 않고, **실사용 중 하네스 결함이 실제로 나올 때만** 그 결함 하나를 들고
엽니다. 다음 작업은 soulmate-4의 라운드가 아니라 `warms-mobile` 프로젝트이고, 하네스는
거기서 **계측당하는 쪽**입니다.

---

## 7-1. 라운드 39 — 실사용에서 열린 첫 라운드 (2026-09-02)

7절이 정한 조건("실사용 중 결함이 실제로 나올 때만 연다")이 실제로 충족돼서 연 라운드입니다.
발단은 Jay의 보고 — *"무조건 순서대로 sub-task만 돌리게 돼 있고, 중간에 내가 뭘 말해도 안 듣고
에러만 낸다."*

**근거는 코드 판독이 아니라 그의 실제 Kilo 트랜스크립트입니다** (`~/.local/share/kilo/kilo.db`,
세션 `ses_fc421bb0fffe5FU22DG4dgcc00`). DB 전체에서 게이트 실차단 **1,107건**:
primer 475 · elective 549 · **protocol-read 78** — 그리고 그 78건은 **전부 `/tmp`·감사용
스크래치 저장소**이고 실제 프로젝트 세션엔 **0건**입니다. 즉 "세션마다 protocols 문서를 먼저
읽어야 한다"는 관문은 이번 증상과 무관합니다(최초 가설이었으나 실측으로 기각).

진짜 원인은 `chat.message` 쪽 둘이었습니다.

**① 모호성 nudge가 한국어 지시문 대부분에 오발** — `AMBIGUITY_ANCHOR_PATTERN`의 앵커(백틱·
`.확장자`·따옴표)가 전부 ASCII 문장부호 모양이고, 라운드 7~8의 영어 프롬프트로만 검증됐습니다.
그 세션의 실제 사용자 메시지 15건을 이 함수에 그대로 통과시키니 **12/15가 nudge**됐습니다 —
`"좋습니다. 빌드 진행하세요."`, `"내 말을 듣고 있어요? 시스템 구조 확인하라고 했습니다."`
포함. 그리고 라운드 8이 이 문구에서 모델의 자기면제 여지를 일부러 없앴기 때문에(*"STOP before
doing anything else … Do not assume an interpretation and proceed"*), 모델은 그의 명시적 지시를
매번 "모호하니 되묻기"로 처리했습니다. 08-29 08:55→10:13, **같은 구체적 요청을 세 번 반복**한 뒤
그가 쓴 문장이 *"내 말을 듣고 있어요?"* 입니다.

**② 라운드 28의 fresh-session 면제가 그의 사용 패턴에서 영구 소멸** — 그 면제는 build.md의
"한 sub-task = 한 세션"을 전제로 **세션의 첫 메시지에만** 걸립니다. 그런데 DB상 warms-mobile
세션은 **하나뿐이고 08-26 11:20 → 08-29 10:58, sub-task 2개·커밋 5개를 관통**합니다. 면제는 한 번
쓰이고 끝났고, 남은 유일한 해제 경로 `acknowledged`는 **"그 SHA로 차단이 먼저 발생한 뒤에 새
메시지"** 순서를 요구합니다 — 즉 **차단보다 먼저 도착한 사용자 지시는 아무것도 못 풉니다.**
08-29 10:21~10:28에 6연속 차단, 그중 4건이 그가 방금 시켜서 실행한 `git remote -v` 계열
읽기전용 진단입니다. 바로 뒤 그의 문장: *"제가 어떤 요청을 해도 블락 처리하시는거에요?"*

**고친 것** (`warms-mobile`, 파일당 1커밋):

| 커밋 | 내용 |
|---|---|
| `3967b9c` | `.kilo/plugins/subtask-gate.ts` — ① 앵커 휴리스틱을 **문자체계로 스코프**(`\p{Script=Latin}` vs `\p{L}`, 라틴 글자가 더 적으면 판정 자체를 안 함). 열거 없음. ② 면제 기준을 "세션이 새것"에서 **"직전 턴 내내 HEAD가 안 움직였고 진짜 새 사용자 메시지가 왔다"**로 재정의 |
| `2abfdb1` | `tests/subtask-gate.test.mjs` — T22/T23, **39 → 46 단언** |

**문구 재작성이 아닙니다**(역대 0/2). ①은 판정기의 **적용 범위**를 줄인 것이고, ②는 상태 추가
없이 이미 있던 `turnStartHead`/`acknowledged`만 재배선한 것입니다. **primer 차단 자체는 그대로**
— sub-task를 닫으면 바로 다음 호출은 여전히 멈춥니다.

**FEEDBACK #41은 다시 열리지 않았습니다.** #41의 모양은 "턴 안에서 primer 커밋 → 턴 종료 →
다음 메시지"이고, 그 경우 직전 턴 동안 HEAD가 **움직였으므로** 새 조건이 성립하지 않습니다.
추측이 아니라 **결함을 심어서 확인**했습니다 — `turnStartBoundary.sha === priorHead` 한 줄을
빼니 **T12·T19b·T20·T23b가 실패**합니다. 스크립트 가드를 무력화하면 T22a/T22b가, "포함"으로
바꾸면 T22d가 실패합니다.

**계측**: 그의 실제 15개 메시지 오발률 **12/15 → 0/15**, 라운드 7의 영어 프롬프트는 그대로
nudge됨(T22c). 신규 테스트 3건은 **수정 전 플러그인에서 먼저 FAIL을 확인**한 뒤 고쳤습니다.
전체 46/46 ALL PASS · `check-caps.sh` EXIT=0 · 훅 2종(pre-commit·post-commit) 정상 통과.

**정직하게 남는 것 둘.**
- 08-29 10:21의 그 턴 자체는 라운드 39로도 통과하지 않습니다. 09:59에 모델이 (그의 "개발
  진행 하지마시구요"를 무시하고) sub-task 2 마감 커밋을 넣었고, 그 직후 차단은 **설계대로**
  입니다. 라운드 39가 바꾸는 건 (a) 애초에 그가 무시당한 원인 ①을 없애고 (b) 그의 지시가
  "영원히 안 먹힘"이 아니라 **늦어도 다음 메시지에는 먹히게** 만드는 것입니다.
- **템플릿 저장소엔 아직 안 들어갔습니다.** 수정본은 `warms-mobile`에만 있고
  `github.com/lvninety9/soulmate-4`(262커밋)는 그대로라, 지금 새로 부트스트랩하면 구버전
  플러그인이 다시 깔립니다. push는 Jay 승인 대기 — 로컬 커밋만 있습니다.

**부수 발견 — `toss-in-app-mario-kart`는 미러링 대상이 아니었습니다.** 거기 깔린
`.kilo/plugins/subtask-gate.ts`는 **53줄짜리 라운드 1 원형**입니다(in-memory `Set`,
`chat.message` 훅 없음, `looksAmbiguous` 없음, 상태파일 없음 — 8/7~8/8 `4c1fa8c`, 마지막 세션
8/8). 바이트 동일이 아니라 **세대가 다릅니다**. 그리고 그 저장소엔 `wiki/protocols/`가 아예
없어서, **현행 플러그인을 그대로 복사해 넣으면 protocol-read 관문이 모든 mutating 호출을 영구
차단**합니다 — 그래서 손대지 않았습니다. 되살리려면 재부트스트랩이 필요합니다.

**Jay가 라이브 Kilo에서 직접 확인할 것** (여기서는 불가 — 플러그인은 `kilo serve` 데몬 기동 시
1회 로드되고, 3-3절대로 Cursor의 "New Session"도 같은 데몬을 재사용합니다. 데몬 kill은 금지
사항이라 **이 라운드는 라이브 검증을 하지 않았습니다**):

```
0. Cursor를 완전히 닫았다 다시 연다 (플러그인 재로드 — 이게 안 되면 아래 전부 무의미).
1. warms-mobile에서 새 세션을 열고 첫 메시지로 한국어 구체 지시 한 줄:
     "src/terrain.js 말고, 지금 git remote -v 먼저 확인해주세요."
   확인할 것: "STOP before doing anything else ... 1-2 clarifying questions" 문구가
   더 이상 안 붙는가. 붙으면 ①이 반영 안 된 것(=데몬이 구버전).
2. 3단계를 정상 진행해 SESSION_PRIMER 커밋까지 낸다.
   확인할 것: 그 직후 다음 도구 호출이 여전히 1회 차단되는가. 안 막히면 그게 회귀다.
3. 차단 후 아무 말이나 한 마디 하고, 그 다음 메시지로 sub-task와 무관한 지시를 준다:
     "그거 말고 tests/subtask-gate.test.mjs 돌려봐 주세요."
   확인할 것: 이번엔 실행되는가. 안 되면 ②가 반영 안 된 것.
```

---

## 7-2. 라운드 39 후반 — 방법론의 사각 · 배포 감사 · 재채점 (2026-09-02)

7-1절의 결함은 개별 버그가 아니라 **증상**이었습니다. 진짜 질문은 Jay의 것입니다 —
*"이 하네스가 실제 로컬모델 개발에 구조적으로 건전한가?"* 그래서 같은 종(種)의 사각을
더 찾았습니다. **찾은 것과 못 찾은 것을 둘 다 적습니다** — 없는 걸 만들어내지 않는 게
이 프로젝트의 규율입니다.

### 같은 종의 사각 — 추가로 2건 (둘 다 수정)

**③ `check-caps.sh`의 글자수가 로케일에 따라 달라집니다.** `wc -m`은 UTF-8 로케일에서만
문자를 세고 POSIX/C에서는 바이트를 셉니다(한글 1자 = 3바이트). 이 스크립트의 캡은 전부
문자 캡인데, **같은 파일이 어느 셸에서 커밋했느냐에 따라 다르게 측정**됐습니다. 실측:

| | 데스크톱 `ko_KR.UTF-8` | `env -i` (cron/ssh 환경) |
|---|---|---|
| required-read 합계 | 8,370 | **10,900** (+30%) |
| `wiki/PROJECT_BACKGROUND.md` | 2,535 | **4,204** (+66%) |

pre-commit 훅은 `git commit`이 물려받은 환경에서 돕니다. 한국어 문서가 캡에 근접하면
**타이핑한 위치만으로 하드 블록이 갈립니다.** 그리고 이건 HANDOFF 2절이 "시작 상태 재현"
숫자로 쓰라고 지시한 바로 그 값입니다 — 2절이 이미 원인 불명으로 기록해 둔 재현 실패
(24,535 → 25,544)와 같은 계열입니다. 이제 `C.utf8`로 고정해 **4개 환경 전부 8,370**.
(`warms-mobile` `054295c`)

**④ stale-language 스윕의 어휘가 영어뿐이라 한국어 문서에서 공회전 중이었습니다.**
라운드 12~28이 **스캐너**에 전부를 쏟았고(awk 영역처리·HTML주석 짝맞춤·백틱 스팬·펜스
패리티·아카이브 면제 + 42단언 퍼즈 스위트) **어휘는 단 한 번도 안 바꿨습니다.** 실측:
6개 영어 구절이 **3개 배포처 전부에서 0히트**. 즉 `check-caps.sh`가 매번 찍던
*"ok: stale-language sweep — no possibly-stale mechanism-state claims"* 는 문서가 깨끗해서가
아니라 **읽을 수 없어서** 나온 초록불이었습니다 — 하필 HANDOFF 3-2절의 그 사고("문서는
돌아간다고 적혀 있었는데 설치된 훅은 구버전")를 잡으라고 만든 검사에서.

7-1절 ①과 정확히 같은 종인데 방향만 반대입니다 — **①은 신호가 없는 곳에서 발화했고,
④는 어휘가 없는 곳에서 침묵했습니다.** 한국어 6구절 추가(영어 6개와 같은 폭으로 좁게,
3개 저장소 전부 현재 0히트 확인 후 — 하드 블록이라 오탐이 Jay의 일상 편집을 막습니다).
퍼즈 스위트에 **어휘 축**을 신설: **42 → 50단언**. (`6a843ff` / `d64f170`)

### 찾아봤지만 문제 없었던 것 (정직한 음성 결과)

- **2층(로컬모델 diff 리뷰)은 한국어에서 안 나빠집니다.** 라운드 37의 n=16이 어떤 언어로
  결함을 심었는지 **보고서 어디에도 안 적혀 있어서**(그 자체가 방법론 결함) 직접 측정했습니다:
  라운드 37이 **적중했던** 5개 결함군(비교연산자 뒤집힘·off-by-one·세금 2중적용·mutable
  default·인덱스 스왑)을 로직·결함 동일, **주석/식별자/문자열만 영↔한**으로 바꿔 10회 실행
  (실제 `subtask-review-llm.sh` + 8080 llama-server, GPU 락 stale 확인 후).
  **결과 영어 5/5 · 한국어 5/5.** 오히려 한국어 쪽이 `'나이 <= 18'`이 주석
  `'18세 이상이어야 한다'`와 모순된다고 **주석을 직접 인용**했습니다 — 라운드 37이 #11에서
  미탐으로 기록한 바로 그 유형입니다. n이 작으니 68.75%를 갱신하진 않지만, **언어 사각은
  2층에 없습니다.**
- **경로·파일명 축도 깨끗합니다.** `개인키.pem`·`my key.pem` 둘 다 check-secrets가 잡고,
  게이트의 `PROTOCOL_DOC_PATTERN`은 `wiki/protocols/빌드.md`도 매칭하고, subtask-report의
  TODO 스캐너는 `# TODO: 나중에 고치기`를 셉니다.
- **check-secrets 패턴은 한국어에서 오탐이 없습니다** — 한국어 주석·문자열이 섞인 파일을
  두 로케일 모두에서 통과(비밀은 애초에 ASCII라 이 축이 원래 무관).

즉 **언어 축의 피해는 "텍스트 의미를 판정하는 기제"(①④)와 "인코딩을 세는 기제"(③)에
한정**됐습니다. 나머지는 견뎠습니다.

### 배포 감사 — 실측

| 배포처 | 하네스 세대 | 스크립트 | 프로토콜 | 테스트 | 2대 하드블록 |
|---|---|---|---|---|---|
| 템플릿 `lvninety9/soulmate-4` (263커밋 `4c464dd`) | 라운드 38 | 10/10 | 6/6 | 6/6 ALL PASS | 2/2 |
| `warms-mobile` | 라운드 38 → **39** | 10/10 | 6/6 | **2/6** (부트스트랩 설계상) | 2/2 |
| `toss-in-app-mario-kart` | **라운드 1** | **3/10** | **0/6** | **0/6** | **1/2 → 2/2** |

**좋은 소식**: `warms-mobile`은 라운드 39 직전(`3faf3da`) 기준 **하네스 11개 파일 전부
템플릿과 바이트 동일**이었습니다. 이건 Jay가 08-29에 던졌다가 ①번 버그 때문에 끝내 답을
못 받은 그 질문의 답입니다 — **네, 최신과 같았습니다.**

**나쁜 소식**: `mario-kart`의 게이트는 **53줄짜리 라운드 1 원형**(in-memory `Set`,
`chat.message` 훅 없음)이고 `wiki/protocols/`도 테스트도 없습니다. 그리고 pre-commit 훅이
**soulmate-3 시대 것**이라(헤더에 아직 *"Soulmate 3 doc cap"*, *"Continue exposes no
tool.execute.before-style hook"*) `check-secrets.sh` 자체가 **아예 없었습니다.**
추론이 아니라 실증했습니다 — 진짜 모양의 GitHub 토큰(`ghp_` + 36자)이 **커밋에 성공**했고,
같은 내용이 `warms-mobile`에서는 차단됐습니다. HANDOFF 2절이 "되돌릴 수 없는 것"이라고
못박은 **2대 강제 중 하나가 라이브 배포처에서 통째로 비어 있었고, 그걸 알려줄 장치가
어디에도 없었습니다.**

수정(비파괴, 2파일): `29e602a` `check-secrets.sh` 설치 + `88f93f5` 훅을 soulmate-4판으로
교체(+`.git/hooks/`에 재설치). 재검증: 같은 토큰 **차단됨**, 일반 커밋은 정상 통과(오탐 0).
게이트 자체의 세대 격차는 `wiki/protocols/`가 없어 **재부트스트랩이 필요** — Jay 승인 대기.

### 재채점 — 그리고 기존 2축 루브릭을 쓰지 않는 이유

**턴키/구조 2축 숫자를 새로 매기지 않겠습니다.** 이 프로젝트 자신의 `HISTORY.md`가 그
루브릭이 왜 실패했는지 이미 적어놨습니다 — *"매 라운드 채점자가 새 에이전트였고 이전
루브릭에 캘리브레이션되지 않았습니다… 노이즈 폭이 ±3인데 실제 개선폭은 그보다 작았습니다…
11라운드째 노이즈 바닥 아래의 신호를 찾고 있었습니다."* 새 에이전트인 제가 주관 2축 점수를
내놓는 건 라운드 17~26을 태운 그 행동 자체입니다. 대신 **측정한 것만으로 4축**을 씁니다.

| 축 | 이전 | 현재 | 근거 (전부 이번 라운드 실측) |
|---|---|---|---|
| **강제 정확성** — 옳을 때만 발화하는가 | 60 | **88** | 모호성 nudge 실트래픽 정밀도 3/15 → 15/15 · stale 스윕 실코퍼스 재현율 0% → 심은 결함 4/4 적중+음성 4/4 · 게이트 경계논리는 원래 정확(1,107건 전수 분류) |
| **측정 신뢰성** — 자기 숫자가 말하는 대로인가 | 65 | **85** | 글자수 로케일 편차 ±30% → 0 · n=16의 언어 변수 미기록을 직접 보완 측정 · 잔여: 어댑터는 6스위트 중 2개만 실행 가능 |
| **전파/일관성** — 고친 게 배포처에 닿는가 | **45** | **60** | warms-mobile 11/11 동일(양호) · mario-kart는 39라운드 중 1라운드 세대에 하드블록 1개 공백(수정함) · **잔여: 플러그인·스크립트 드리프트 탐지기가 없음**(라운드 37 ①은 설치된 *훅*만 검사) |
| **방법론 커버리지** — 어떤 입력 축을 실제로 흔들어봤는가 | 65 | **80** | 37라운드가 흔든 축: 툴이름·git상태·세션/턴 형태·저장소 손상·텍스트 표현. **한 번도 안 흔든 축: 언어/로케일** — 그 하나가 3개 기제를 깼다. 이번에 닫음(경로·파일명·2층은 흔들어보니 무사) · **잔여 미검증 축: 동시 세션·비UTF8·비리눅스 경로·초대형 저장소** |

**한 줄 요약**: *37라운드가 실제로 사준 것은 **핵심 강제 설계의 건전성**이고, 실사용 감사
두 번이 찾아낸 결함은 **전부 그 시행들이 한 번도 안 흔든 경계**에서 나왔습니다 — 입력 언어,
실행 환경, 배포 전파.* 굳이 옛 구조 점수로 환산하면 **70대 → 80 근처**지만, 위 사유로
역사적 숫자와 비교 가능한 값이 아닙니다.

**가장 값싼 다음 한 수**는 새 강제가 아니라 **드리프트 탐지기**입니다. `check-caps.sh`가
설치된 훅의 최신성은 이미 `diff -q`로 봅니다(라운드 37 ①). 같은 3줄을
`.kilo/plugins/subtask-gate.ts`와 `scripts/*`에 확장하면 mario-kart류가 **38라운드 동안
조용히 뒤처지는 일** 자체가 기계적으로 불가능해집니다. 전파 축이 가장 낮은데 고치는
비용은 가장 쌉니다.

### Jay 결정 대기 (② push 완료, ①③은 로컬 커밋만 — Jay가 ②만 승인)

1. `warms-mobile` 5커밋 — `3967b9c` `2abfdb1` `054295c` `6a843ff` `d64f170` — **미push, 대기 중**
2. **✅ 템플릿 패치 push 완료** (2026-09-02, Jay 승인 후 Sonnet 5가 실행 — 이 라운드를 진행하던
   Opus 세션이 월 사용한도(HTTP 429)로 이 지점 직전 종료돼 이어받음). 원래 스크래치 클론
   (`dbbbf71`…`35b4d37`)은 세션 스크래치 디렉토리라 사라져 있었고, `warms-mobile`의 해당 5커밋에서
   `git format-patch`로 재추출 → 새 fresh clone에 `git am`으로 적용 → **6개 스위트 전부 ALL PASS
   재확인**(`subtask-review-llm` 26단언 포함, 이번엔 실제로 끝까지 돌림) → `origin/master`가 그
   사이 안 움직였는지 재-fetch로 확인 후 fast-forward push. `origin/master`:
   `4c464dd` → **`228ae99`**. 독립적인 별도 fresh clone으로 GitHub 상에 실제 반영됐는지 재검증
   완료(로컬 push 성공 로그만 믿지 않음, L06과 같은 원칙). **새 프로젝트 부트스트랩은 이제부터
   round 39 수정분(게이트 비라틴 오탐 수정 + 로케일 고정 + stale-language 한국어 어휘)이
   반영된 상태로 시작합니다.**
3. `mario-kart` 2커밋(`29e602a` `88f93f5`) — 시크릿 차단 복구분. **게이트 재부트스트랩은
   미실행** (파괴적: `AGENTS.md`/`PROJECT_BACKGROUND`/`SESSION_PRIMER`를 템플릿으로 덮어씀).

---

## 7-3. 라운드 44 — 같은 턴 재시도 폭주 (2026-09-03)

> ⚠️ 이 문서엔 **라운드 40~43이 안 적혀 있습니다.** 실제로는 있었고(`vision_read` 도구 신설과
> 그 stale 서술 정정), 템플릿 `origin/master`에 288커밋으로 들어가 있습니다
> (`a4e85f1`·`22a4b21`·`1cc184f`·`18b4641`·`2aa48c4`·`62ec770`). 7-2절이 "다음은 라운드가
> 아니라 실사용"이라고 닫아둔 뒤에 열린 라운드들이라 문서에 되돌아오지 않았습니다.

발단은 Jay의 실사용 보고 — *"서브태스크 1→2 넘어가는 지점에서 문서 업데이트를 시켰더니
하네스가 에러만 내고 멈췄다."*

**근거는 그의 실제 트랜스크립트입니다**(`~/.local/share/kilo/kilo.db`, 세션
`ses_f9d4e1792ffeIM9vhmzmTIXpoJ`, `warms-mobile`). 전 구간을 초 단위로 재구성했습니다:

| 시각 | 사건 |
|---|---|
| 00:24:18 | `fe2fed7`(SESSION_PRIMER 핸드오프)가 **턴 중간에** 착지 → primer 경계 발생 |
| 00:24:49 | 모델이 도구 호출 없이 텍스트로 턴 종료 — **build.md 설계대로 정상** |
| 00:27:24 | Jay의 새 지시 *"다음 서브 타스크 이어서 진행하기 전에 진행상황 각 문서 업데이트부터"* |
| 00:30:33 | 이 턴의 첫 mutating 호출이 `BLOCK_MESSAGE_COMMIT`으로 차단 — **라운드 30 수용기준 B대로 정상** |
| 00:31:15 · 00:31:57 · 00:32:40 · 00:33:24 · 00:33:31 | **같은 차단이 5번 더**. 전부 바이트 동일 메시지 |
| 00:34:36 | 반박 주입(`6 tool call(s) were blocked last turn`) + Jay의 새 메시지 → `acknowledged` 해제 |
| 00:35:18 | 다음 `edit` **성공** |

**게이트의 판정 논리는 전 구간에서 문서화된 계약대로 정확히 동작했습니다.** 버그가 아닙니다.
라운드 39가 이미 이 잔여를 명시해뒀습니다 — *"차단보다 먼저 도착한 사용자 지시는 그 턴을
못 푼다, 늦어도 다음 메시지에는 먹힌다."* 00:27:24이 정확히 그 모양입니다.

**그런데 모델은 차단당했다는 걸 이해하지 못했습니다.** 00:33:22에 그가 직접 쓴 문장:

> *"게이트가 매번 edit를 커밋으로 인식하는 것 같습니다. 이미 커밋된 상태라 에디트가 실패하고 있습니다."*

즉 "**edit 자체가 실패했다**"로 읽고 재시도한 겁니다. 그럴 만합니다 — 메시지는 primer가
*"was just committed"*라고 말하는데, 그건 1회차엔 참이고 **6회차엔 6분 + 사용자 턴 하나만큼
낡은 서술**이며, **그 호출이 실행되지 않았다는 말은 어디에도 없습니다.**

**Sonnet의 최초 진단 중 정정한 것**: "같은 턴 재시도가 테스트에 없다"는 틀렸습니다.
**T9b(라운드 8, FEEDBACK #3)가 이미 verbatim 재시도가 계속 차단되는지 검사합니다.**
비어 있던 건 *차단 여부*가 아니라 **재시도에게 뭐라고 말하는가**였고, 하필 그 "바이트 동일"이
오독을 벌었습니다.

**고친 것** (파일당 1커밋, `warms-mobile` → 템플릿 순):

| 커밋(warms-mobile / 템플릿) | 내용 |
|---|---|
| `1df5d7c` / `b02b4e8` | `.kilo/plugins/subtask-gate.ts` — 같은 턴 2회차부터 `BLOCK_REPEAT_SUFFIX(n)` 부가: 시도 횟수 · **실행되지 않았음** · *"edit/명령 자체가 실패한 게 아니라 시작 전에 게이트가 거부했다"* · 재시도해도 같은 에러 · 지금 멈추고 2-3줄 요약. **1회차 문구는 바이트 그대로**(기존 6개 단언이 정규식으로 물고 있음) |
| `2abfdb1`→`f451fb8` / `5f67f2c` | `tests/subtask-gate.test.mjs` — T24a~e |

**상태 추가 0.** `lastBlockedSha`와 `blockedCallsThisTurn` 둘 다 이미 `chat.message`마다
무조건 비워지므로 그 자체가 정확한 턴 단위 신호입니다.

**차단 범위는 1비트도 안 바뀌었습니다** — 새로 막는 것 없음, 풀리는 것 없음. 보고 계열입니다.

**"문구 재작성 0/2" 규칙과의 관계 — 정직하게.** 이건 텍스트 변경이 맞습니다. 다만 0/2의
그 계열(*같은 주장을 더 세게 다시 쓰기*)이 아니라 **라운드 43과 같은 계열 — 더 이상 참이 아닌
서술의 정정**이고, 모델의 오도출이 트랜스크립트에 실물로 남아 있습니다. 그리고 `tool.execute.before`
훅이 가진 유일한 레버가 던지는 메시지뿐이라 **이 층에서 가능한 수단이 이것 하나입니다.**
게이트를 완화해 00:27:24 지시를 즉시 통과시키는 안도 검토했으나, 그건 라운드 30 수용기준 B와
FEEDBACK #41을 뒤집는 것이라 **하지 않았습니다.**

**계측 (실측)**

- 신규 테스트는 **수정 전 플러그인에서 먼저 3건 FAIL 확인**(T24a/T24d는 그때도 통과 — 음성 케이스가 실제로 음성임을 증명)
- **결함 심기 2종**: `repeatForSameBoundary=true` 고정 → T24a·T24d FAIL / `attempt=1` 고정 → T24b·T24c·T24e FAIL
- **라이브 턴 오프라인 재현**(`kilo serve` 무개입): 00:23:25~00:35:18 시퀀스를 그대로 재생 →
  before 6회 전부 동일 메시지, after 1회 원문 + 5회 `[repeat] … attempt 2..6`, 7번째 호출은
  양쪽 다 사용자 메시지 후 **실행됨**. 즉 **막히는 것도 풀리는 것도 그대로**
- **템플릿 7개 스위트 전부 ALL PASS**(패치 전/후 동일) · `warms-mobile` 3개 스위트 ALL PASS
- **fresh `bootstrap.sh` 전파 확인**: 새 프로젝트의 플러그인이 템플릿과 **바이트 동일**,
  `BLOCK_REPEAT_SUFFIX` 존재. (`--bootstrap-check` EXIT=1은 문서화된 `[project name]` 미기입 단계)
- **드리프트 0**: 라운드 44 직전 `warms-mobile`의 게이트·테스트가 템플릿과 바이트 동일이었음

**남는 것 — Jay가 라이브에서 확인할 것.** 모델이 이 새 메시지를 받고 **실제로 재시도를
멈추는지**는 여기서 잴 수 없습니다(플러그인은 `kilo serve` 기동 시 1회 로드, 데몬 kill 금지).
Cursor를 완전히 껐다 켠 뒤, 서브태스크 마감 커밋 직후 차단이 나면 **2회차 메시지에
`[repeat] … attempt 2`가 붙는지**와 **모델이 3회차를 시도하는지**를 보시면 됩니다.
안 붙으면 데몬이 구버전입니다. **여전히 폭주하면 이 수정은 실패한 것이고, 0/2가 0/3이 됩니다 —
그때는 되돌리고 그렇게 적을 것.**

**push 대기**: `warms-mobile` 2커밋(`1df5d7c` `f451fb8`) + 템플릿 2커밋(`b02b4e8` `5f67f2c`,
`origin/master` `62ec770`→ fast-forward 가능). 전부 **로컬 커밋만** — Jay 승인 대기.

---

## 7-4. 라운드 45 — "구체적인 사용자 메시지는 왜 게이트를 못 푸나" (2026-09-03)

발단은 버그가 아니라 **설계 질문**입니다. 라이브(`ses_f9d0dfa4bffeTLzGAKjOA3a0st`, 01:29:28)에서
Jay가 *"sub-task 2 가 완료된건가요? 아니면 브리핑만 하신건가요? 지금 5183 포트 접속해서 확인해서
볼게 있나요?"* 라고 물었는데 이 메시지는 아무것도 못 풀었습니다(라운드 28 규칙 a: **그 SHA로
차단이 먼저 발생**해야 함). 그의 질문: *"풀 필요 없이, 사용자 요청에는 반응을 하도록 설계할 수
없나요?"*

### 결론 — 짓지 않았습니다(휴리스틱). 이유는 추론이 아니라 실측입니다

이 저장소엔 이미 선례가 있습니다. `looksAmbiguous()`가 유일한 **내용 판정 휴리스틱**이고,
라운드 39가 같은 사용자의 실제 메시지 15건에 돌려 **12/15 오발**을 확인한 뒤 **비라틴 문자에선
판정 자체를 안 하도록** 무력화했습니다. "구체성 판정"은 같은 종인데 **실패 방향이 반대**입니다 —
`looksAmbiguous`는 넛지만 붙였지만, 이건 **관문을 엽니다.**

`kilo.db`의 warms-mobile 전 세션에서 Jay의 **실제 메시지 84건**을 뽑아 후보 판정기를 돌렸습니다
(정답 라벨 = 그 메시지가 "체크포인트가 걸려 있다는 걸 실제로 알고 있었음"을 보여주는가):

| 판정기 | "구체적"이라 판정 | 실제 인지와의 정밀도 | 그날 01:29 메시지 |
|---|---|---|---|
| 앵커 휴리스틱(라운드39 스코프) | 13/84 (15.5%) | 2/13 = 15.4% | **거부** |
| 앵커 휴리스틱(언어무관) | 20/84 (23.8%) | 3/20 = 15.0% | **거부** |
| 길이 ≥ 15자 | 81/84 (96.4%) | 5/81 = 6.2% | 통과 |
| "계속" 류 정지목록만 제외 | 83/84 (98.8%) | 5/83 = 6.0% | 통과 |
| 길이+정지목록+단어수 조합 | 80/84 (95.2%) | 5/80 = 6.3% | 통과 |

실제 인지 기저율 **5/84 = 6.0%**. **그날 메시지를 통과시키는 판정기는 전부 정밀도 6.0~7.7%** —
즉 *"아무 메시지나 풀어준다"*(라운드 8 규칙, FEEDBACK #41이 바로 그것 때문에 열림)와 통계적으로
구분되지 않습니다. 기저율을 넘는 둘은 **그날 메시지를 거부**하고 그의 한국어 대부분도 거부합니다.
그리고 `"좋습니다. 빌드 진행하세요."`, `"좋아요. 1번부터 차근차근 진행하시죠."` — 전형적인
"계속" 메시지가 관대한 쪽을 **전부 통과**합니다.

**숫자보다 결정적인 건 그 메시지 자체입니다.** 그건 *"완료된 건가요?"* 라는 **질문**이고, 곧
**그가 상태를 몰랐다는 증거**입니다. 구체성은 "관여"를 재고 라운드 28이 요구하는 건 "알고 하는
동의"인데, 이 트랜스크립트는 둘이 정반대를 가리키는 사례입니다.

### 전제 정정 — "차단이 한 번 더 나야만 진행된다"는 한 턴짜리입니다

라운드 39의 조용한-턴 규칙이 **차단 없이도** 이미 경계를 풉니다: 모델이 한 턴 동안 아무것도
커밋하지 않고 진짜 사용자 메시지가 오면 그 SHA는 acknowledged 됩니다(`turnStartHead` == 경계 SHA).
실제 상태파일로 확인: 그 세션의 `turnStartHead`가 정확히 경계 SHA(`b462450`)입니다. **상시 비용은
"한 턴 동안 mutating 호출 거부"이지 "반드시 차단 1회"가 아닙니다.**

### 대신 지은 것 (보고 계열, 강제는 1비트도 안 바뀜)

트랜스크립트가 보여준 진짜 낭비는 따로 있었습니다 — `chat.message`는 **모든 메시지마다 경계를
이미 계산**해놓고(`electiveBoundaryAtTurnStart` 스냅샷 때문에 필수) **그걸 버립니다.** 그래서
모델은 01:29:55에 `git log --oneline -3`(Jay 질문에 답하려던 **읽기전용** 진단)이 거부되고 나서야
알았고, 틀린 이론을 소리내어 말한 뒤(*"subtask-gate가 SESSION_PRIMER.md 업데이트를 감지하지 못한
것 같습니다"*) 01:30:05에 한 번 더 거부당했습니다.

1. **대기 중 체크포인트 사전 고지** — 라운드 27이 쓰던 synthetic part 표면에, `tool.execute.before`가
   잠시 뒤 적용할 **같은 술어**로 가드해서 붙입니다. 경계가 풀리는 순간 조용해집니다. **상태 추가 0.**
2. **`BLOCK_MESSAGE_ELECTIVE`의 불가능한 지시 정정** — 그 메시지는 *"SESSION_PRIMER.md를 고치고
   커밋하라"*고 시키는데 **바로 그 arm이 그 edit를 거부**합니다. 01:30:05에 모델이 그 지시를 그대로
   따르다 라운드 44의 attempt-2 접미사(*"Stop calling tools now"*)와 정면충돌했습니다.
   프라이머 쌍둥이(`BLOCK_MESSAGE_COMMIT`)엔 이 결함이 없습니다(멈추고 보고하라고만 함).

**계측**: 결함 심기 **한 번에 하나씩** — 옛 elective 문구 복원→T25d FAIL / 고지 가드 제거→T25e FAIL /
고지 자체 제거→T25b FAIL / 복원→0 FAIL. `warms-mobile` 3스위트 ALL PASS, **템플릿 7스위트 전부
ALL PASS**, `check-caps.sh` EXIT=0(rule-archive 416/450), **fresh `bootstrap.sh` 전파 확인**(새
프로젝트 플러그인이 템플릿과 **바이트 동일**). 라운드 45 직전 드리프트 **0**(템플릿 HEAD와
`warms-mobile`의 게이트·테스트가 바이트 동일이었음).

**남는 것 — Jay가 라이브에서 볼 것.** Cursor를 완전히 껐다 켠 뒤, 체크포인트가 걸린 상태에서
메시지를 보내면 모델이 **도구를 하나도 안 쓰고** "체크포인트가 걸려 있어 이번 턴엔 편집/커밋이
안 됩니다"라고 먼저 말하는지. 그 대신 여전히 실패한 도구 호출로 알아내면 이 수정은 효과가 없는
것이고, 그때는 되돌리고 그렇게 적을 것(문구성 수정 역대 0/2, 라운드 44도 아직 미검증이라 **이번
elective 문구 정정까지 합치면 미검증 문구 수정이 2건 쌓여 있음** — 정직하게 적어둡니다).

**push 대기**: `warms-mobile` 2커밋(`685746e` `e04deaf`) + 템플릿 3커밋(`c60d0c3` `cbe7c97`
`4840183`, 클론 **`~/sm4-r45`** — 라운드 39가 스크래치 클론을 잃은 전례가 있어 `/tmp`가 아니라
홈에 두었습니다. `origin/master` `4e3e475`에서 fast-forward 가능). 전부 **로컬 커밋만**.
⚠️ 7-3절이 "미push"라고 적어둔 라운드 44 템플릿 커밋은 **그 사이 이미 push돼 있었습니다**
(`a063a6c`/`4e3e475`, 290커밋) — 7-3절의 그 줄은 이제 낡았습니다.

---

## 7-5. 라운드 46 — "풀고 진행하세요"를 한 번에 말했는데 왜 또 막히나 (2026-09-03)

발단은 라이브(`ses_f9d0dfa4bffeTLzGAKjOA3a0st`, 02:56:32)에서 Jay가 보낸 한 줄입니다 —
*"gate 풀고 서브타스트 3 진행하세요."* **푸는 것**과 **다음 걸 하는 것**을 한 문장에 같이
시켰습니다. 게이트는 앞 절반만 들어줬습니다: 02:19:52에 `e04deaf`로 차단이 났었으니
라운드 28 규칙 (a)대로 풀렸고, 모델은 `SESSION_PRIMER.md`를 실제로 고쳐 커밋했고(`75a00c9`,
02:57:34 — 라운드 44의 허위보고와 달리 **이번엔 진짜로 바뀌었습니다**), 그런데 그 커밋이
primer를 건드렸으므로 **그 자리에서 새 경계가 섰고** 서브태스크 3이 막혔습니다. 재시도는
2회에서 멈췄습니다(라운드 44의 `[repeat]`는 라이브에서 버텼습니다). Jay:
*"이거 저희가 만든 구조인데 당연히 개선이 될 수 있는 부분 아니에요?"*

### 결론 — 게이트는 안 고쳤습니다. 근거 셋 다 실측입니다

**① 라운드 30 "수용기준 B"는 이 건을 결정하지 않습니다. 애초에 primer arm을 지키는 규칙이
아닙니다.** B("갓 생긴 primer 경계는 다음 mutating 호출을 무조건 막는다")는 라운드 30
**작업지시서**가 item 3 — *elective* arm의 턴 중간 오발동(#46/#47의 공통 뿌리) — 을 고칠 때
붙인 **적용범위 제한선**입니다. *"이 유예를 primer 쪽으로 새게 하지 마라"*는 뜻이지,
그 자체가 사건에서 나온 규칙이 아닙니다. primer arm을 지키는 진짜 사건은 **#41**입니다.

**그리고 이번 건은 #41과 같은 모양인데 동의가 더 앞으로 온, 즉 더 약한 형태입니다.**
#41은 *"primer 커밋이 턴 중간에 착지 → 턴 종료 → 다음 메시지가 그걸 넘김"* 이고 거기선
사용자가 최소한 턴이 끝난 건 봤습니다. 이번엔 상태파일 실측으로 `turnStartHead`가
`e04deaf`인데 막은 경계는 `75a00c9`이고, 그건 **Jay 메시지보다 62초 뒤에 생긴 SHA**입니다.
없던 것에 동의했을 수는 없습니다. **여기서 B를 좁히면 좁히는 게 아니라 #41을 다시 엽니다.**

**② 구조적 후보(“그 턴 안에서 사용자가 시킨 행동이 만든 경계면 봐주기”)엔 기계 신호가
없습니다.** 텍스트 판정을 빼고 나면 남는 건 *"턴 안에서 생긴 primer 경계는 안 막는다"*
뿐인데, **08-29 09:45→09:59**가 이 조건을 똑같이 만족합니다 — 거기서 Jay는 *"시스템 구조가
최신 레포와 같은지 확인"*을 시켰고 모델은 대신 서브태스크를 만들어 **스스로 primer 커밋으로
닫았습니다.** 두 경우 다 `turnStartHead != 경계 SHA`이고, **차이는 오직 메시지 문장뿐**입니다
— 그게 라운드 45가 84건으로 재서 정밀도 6.0%로 기각한 바로 그 축입니다.

**③ 셋 다 오프라인 재현으로 확인했습니다**(라이브 플러그인 그대로, `kilo serve` 무개입):
02:56:32 턴을 그대로 재생하면 막히고(A), 08-29 모양도 여전히 막히고(C) — 그리고 아래 ⑤.

### 진짜 원인은 2시간 전 커밋 메시지가 거짓이었던 것입니다

`b462450`(01:28:24) 제목은 `progress: [sub-task 2] — TurnManager.ts, SESSION_PRIMER.md`인데
**실제 diffstat은 `src/systems/TurnManager.ts` 하나뿐**입니다. primer는 **이름만 적히고
staged 되지 않았습니다.** build.md 3단계는 primer 갱신을 서브태스크 마지막 파일과 **같은
커밋**에 넣으라고 못박아 뒀는데 그걸 어긴 겁니다. 그래서 경계가 안 섰고 → 커밋이 6개까지
쌓였고 → elective arm이 떴고 → elective arm이 받아주는 유일한 해법이 "primer를 건드려라"라서
**primer 단독 커밋**(`75a00c9`, build.md가 금지한 "두 단계")이 나올 수밖에 없었고, 그게
Jay의 지시와 정면충돌했습니다.

> **`b462450`이 자기가 적은 대로였으면 01:28:24에 경계가 서고, 모델은 보고하고 멈추고
> (build.md 6단계), Jay의 다음 메시지가 서브태스크 3을 깨끗하게 열었습니다.
> 이번 충돌은 애초에 일어나지 않았을 일입니다.**

### 지은 것 (보고 계열, 강제는 1비트도 안 바뀜)

`scripts/subtask-report.sh`가 이제 **`progress:` 커밋이 제목에 적어놓고 실제로는 안 들어간
파일**을 잡아 "확인이 필요한 것"에 올립니다. 이 스크립트의 존재 이유(*"모델이 뭘 했다고
말하는 걸 믿지 않는다"*)를 **아직 모델의 말을 믿고 있던 마지막 자리 = 커밋 메시지**에
적용한 것입니다.

**핵심은 문구가 아니라 적용범위입니다**(라운드 39와 같은 계열). 범위를 안 좁히면 템플릿
293커밋의 **12.6%**가 걸리는데 전부 오탐입니다(`test: regression net for subtask-report.sh`
처럼 **주제**를 적은 제목). 커밋 **본문**까지 읽으면 라운드 45 자기 커밋 2건도 걸립니다.
build.md 3단계가 정한 형식(`progress: [sub-task] — [file]`)의 **em대시 뒤 목록**으로만
좁히니 — warms-mobile 해당 2건 중 **1건 적중**(`b462450`, 진짜 양성), 템플릿 1건 중 0건.

**계측**: T20a는 수정 전 스크립트에서 **먼저 FAIL 확인**. 음성 4건은 **결함을 하나씩 심어서**
전부 load-bearing임을 증명 — 범위가드 제거→T20c만 FAIL / em대시 요건 제거→T20d만 FAIL /
basename 매칭 제거→T20b만 FAIL / 복원→0 FAIL. 템플릿 **7스위트 전부 ALL PASS**(보고서
스위트 48→55단언), `check-caps.sh` EXIT=0, **fresh `bootstrap.sh` 전파 확인**(새 프로젝트
파일이 템플릿과 바이트 동일 + 심은 결함을 실제로 잡음). `warms-mobile` 3스위트 ALL PASS.
라운드 46 직전 드리프트 **0**, 이후도 **0**(공유 11개 파일 전부 바이트 동일).

**정직하게 남는 한계**: 이 검사는 **다음 서브태스크 경계 보고에서** 뜹니다. post-commit 훅은
설계상 primer 커밋에서만 돌고(T7이 그걸 못박고 있음) 그 트리거는 안 건드렸습니다. 즉 즉시
경보는 아니고, 이번처럼 2시간을 모르고 가는 일은 막아줍니다.

### ⑤ Jay가 지금 당장 할 수 있는 것 — 같은 지시가 **한 턴에** 됩니다

**게이트를 안 고쳐도, Jay가 쓴 그 문장 그대로 한 턴에 끝납니다. 순서만 반대로 하면 됩니다.**
라이브 플러그인에 그대로 재생해서 확인했습니다:

| 순서 | 결과 |
|---|---|
| **A (그날 실제)** primer 커밋 먼저 → 서브태스크 3 | **막힘** (primer 경계가 방금 섰으니) |
| **B (권장)** 서브태스크 3 작업+커밋 먼저 → primer 커밋 마지막 | **안 막힘, 둘 다 한 턴에 완료** |

B가 통하는 이유는 봐주기가 아니라 이미 있는 규칙입니다 — 작업 커밋으로 elective 경계가
턴 중간에 다시 넘어가면 **라운드 30 item 3이 그걸 다음 턴으로 유예**하고, primer 커밋은
턴 맨 끝이라 그 뒤에 막을 호출이 없습니다. **그리고 B는 우회가 아니라 build.md가 원래
시키는 모양입니다**(3단계: primer는 마지막 파일과 같은 커밋).

그래서 Jay에게 권하는 건 둘입니다.

1. **애초에 안 걸리게** — 모델이 "서브태스크 N 완료"라고 보고할 때 **primer 갱신이 그 커밋에
   같이 들어갔는지** 보세요. 안 들어갔으면(=`progress:` 커밋인데 primer가 없으면) 그때부터
   이번 같은 충돌이 예약됩니다. 이제 보고서가 그걸 대신 잡아줍니다.
2. **이미 밀렸을 때** — *"서브태스크 3 먼저 하고, 마지막에 SESSION_PRIMER 갱신해서 커밋하세요"*
   처럼 **순서를 지정**해 주세요. 그러면 이번 문장 그대로 한 턴에 끝납니다.

**한 턴에 못 하는 유일한 경우**는 "지금 있는 서브태스크를 닫기만 하고 다음 걸 시작"인데,
그건 이 하네스가 **의도적으로** 두 턴으로 나눠둔 지점입니다(`한 sub-task = 한 세션`,
3-4절의 실측 근거: 긴 세션에서 18턴 연속 허위 완료보고). 거기까지 없애달라는 요구라면
그건 개선이 아니라 **이 프로젝트의 근거 있는 안전장치를 끄는 것**이라 안 했습니다.

### 부수 정리

- **rule-archive PRUNE 실행.** 라운드 45가 파일을 **정확히 400줄**(= `RULE_ARCHIVE_WARN`)에
  남겨놔서, 라운드 46 항목을 넣자 450/450이 되며 `check-caps.regression` T9a가 깨졌습니다.
  라운드 8/34/36/37이 하던 것과 같은 의식대로 **Round 34 절을 `rule-archive-archive.md`로
  이동**하고 스텁만 남겼습니다 — 450 → **374줄**, T9a 복구.
- ⚠️ **7-4절의 "push 대기"는 이미 낡았습니다.** 라운드 45의 템플릿 3커밋
  (`c60d0c3` `cbe7c97` `4840183`)은 **그 사이 push돼 있습니다** — `origin/master`는 지금
  **293커밋 `4840183`**입니다.

**push 대기 (전부 로컬 커밋만, Jay 승인 대기)**

| 저장소 | 커밋 |
|---|---|
| `warms-mobile` | `20dac2f` (+ 라운드 44/45의 `1df5d7c` `f451fb8` `685746e` `e04deaf`도 여전히 미push) |
| 템플릿 (클론 **`~/sm4-r46`**, `origin/master` `4840183`에서 fast-forward 가능) | `2acf302` `f509d50` `7716193` `f4bb19d` |

**남는 것 — Jay가 라이브에서 볼 것.** Cursor를 완전히 껐다 켠 뒤, 서브태스크를 닫는 커밋이
날 때 **보고서 "확인이 필요한 것"에 `... says it contains ... but that file is not in the
commit`이 뜨는지**. 그리고 위 ⑤-2 순서로 지시했을 때 **한 턴에 끝나는지**. 후자는 여기서
플러그인 재생으로는 확인했지만 **실제 모델이 그 순서를 따르는지는 미검증**입니다.

---

## 8. 새 세션 시작 프롬프트

### 권장 — 실사용 이어가기 (2026-09-04 상태 기준, 최신)

Claude Code(이 문서를 읽는 세션)가 계속 **관찰·검증자** 역할을 하고, Jay가 warms-mobile의
Kilo(Cursor) 세션에서 직접 명령을 주고받은 로그를 붙여넣으면 Claude가 상태파일(`.kilo/plugins/
.subtask-gate-state.json`) 및 git 로그와 대조해 검증하는 방식입니다. **이 세션 방식대로 계속
하면 됩니다** — 아래는 새 세션이 그대로 이어받을 수 있게 정리한 시작 프롬프트입니다.

```
soulmate-4 라이브 검증을 이어갑니다.

먼저 /media/jay/D/cursor/soulmate-4/HANDOFF.md 전체를 읽으세요. **8-1절(사이클 마무리 판단)이
가장 중요합니다** — 라운드를 먼저 열지 말라는 이유와, 지금 남아있는 라이브 미검증 3건이 거기
있습니다. 7-10절(라운드 50)이 가장 최근 코드 변경입니다.

템플릿: github.com/lvninety9/soulmate-4, origin/master는 318커밋(c15b42c, 라운드 50까지 push
완료) — fresh clone으로 재현해서 7스위트 ALL PASS·check-caps EXIT=0부터 확인하고 시작하세요.

실사용 계측기: /media/jay/D/cursor/tossinapp/warms-mobile (로컬, HEAD는 라운드 50 백포트 2커밋
이후 — `git log --oneline -5`로 실제 확인). 지금 상태:
  - primer 마지막 터치는 6dea13f이고 그 뒤 2커밋(elective 임계 4)이라 체크포인트가 서 있지
    않습니다 — .subtask-gate-state.json으로 실제 확인할 것
  - 라이브 미검증 3건을 먼저 볼 것: 라운드 48의 번호-계획 대조(다음 primer 커밋이 첫 기회) ·
    라운드 49의 섞인 턴 팩트체크 · 라운드 50의 턴중 미커밋 고지. 안 뜨면 되돌릴 것이 있다는
    뜻이고, 그게 새 기능보다 값이 큽니다

규율:
  - warms-mobile의 프로젝트 콘텐츠(SESSION_PRIMER.md, src/, PROJECT_BACKGROUND.md)는 절대
    수정하지 말 것 — 관찰 대상이지 수정 대상이 아님(Jay 명시 지시, 여러 차례 반복됨)
  - 구조적 결함이 실사용에서 발견되면 템플릿에서 고치고(파일당 1커밋, 결함 심어서 검증,
    오프라인 재현, fresh clone 재검증) warms-mobile엔 하네스 파일만 backport
  - 템플릿은 검증 끝나면 바로 push(Jay 승인 불필요, 최근 확립된 패턴). warms-mobile은 push
    안 함(무의미)
  - 문구 재작성만으로 때우지 말 것(역대 성공률 낮음) — 실제 로직/신호를 바꿀 것
  - 복잡한 설계 판단은 Agent(subagent_type: general-purpose, model: opus)에게 백그라운드로
    맡길 것 — 이 세션 내내 효과적이었음

Jay가 로그를 붙여넣으면 그대로 검증하고, 필요하면 라이브 테스트용 프롬프트를 Jay에게
제공하세요(Jay가 직접 Cursor/Kilo에 타이핑합니다 — 당신이 대신 실행하지 않습니다).
```

이때 기록할 것(하네스에 대한 진짜 산출물):
어느 단계에서 몇 번 만에 됐는가 · 하네스가 도움이 된/방해한 순간 · "확인이 필요한 것"이
쓸모 있었는가 · **수용 한 줄이 쓸모 있었는가**(`verify:` 필드로 승격할지 판정) ·
2층이 뭘 잡고 뭘 놓쳤는가.

### 예전 — 2026-08-26 시점 프롬프트 (참고용, 더는 정확하지 않음)

```
프로젝트: /media/jay/D/cursor/tossinapp/warms-mobile (부트스트랩 완료, bootstrap-check EXIT=0)
단계 표:  그 저장소의 wiki/PROJECT_BACKGROUND.md — 15행, 각 행에 확인 한 줄
규칙:     한 단계 = 한 세션. 각 단계 프롬프트 마지막 줄은 그 행의 "확인" 칸 그대로.
피드백:   단계마다 .subtask-reports/<sha>.md + git diff 를 Claude가 직접 읽고 지적.
```

### 대안 — 2층 약점 보완 (선택지 B)

```
soulmate-4 라운드 38: 2층의 미탐 3건을 좁힙니다.

/media/jay/D/cursor/soulmate-4/HANDOFF.md 4절을 읽으세요.
라운드 37의 n=16 측정에서 못 잡은 것: 단위환산(분↔초), and→or 권한완화,
주석과 반대인 정렬.

⚠️ 이건 "프롬프트 문구 재작성"이고, 이 프로젝트에서 문구 재작성의 역대 성공률은
0/2입니다. 그래서 반드시 before/after를 같은 n=16으로 재측정해서 실제로 올랐는지
증명해야 합니다. 안 오르면 되돌리고 그렇게 보고하세요.

측정 스크립트와 16개 결함 정의는 ~/.hermes/opus_round37_report.md에 있습니다.
```

### 참고 — 원래의 범용 이어가기 프롬프트

```
soulmate-4 작업을 이어갑니다.

먼저 /media/jay/D/cursor/soulmate-4/HANDOFF.md 전체를 읽으세요.
프로젝트 의도·현재 상태·미비 구간·제 피드백이 전부 거기 있습니다.

작업 저장소: github.com/lvninety9/soulmate-4 (260커밋, 로컬 ~/soulmate-4도 동기화됨)
반드시 fresh clone으로 시작하세요 — stale 클론이 이 프로젝트에서 한 라운드를 태운 적 있습니다.

시작 상태를 직접 재현해 확인하고 시작하세요 (HANDOFF 2절):
  6개 테스트 스위트 ALL PASS · check-caps EXIT=0
  required-read 24,535/27,800자 · rule-archive 370/450줄

이번 라운드 작업은 HANDOFF 4절의 ①②③입니다:
  ① 설치된 git 훅이 최신인지 check-caps가 검사하도록 (오늘 실제로 구버전 훅 때문에
     시크릿 차단이 안 돌고 있었습니다)
  ② 2층 실효성을 n=10~20으로 측정 (현재 근거는 n=2, 1회 놓침 1회 3/3)
  ③ 줄 수 +56 순증가 건 — 값 못 버는 검사가 있으면 근거와 함께 삭제 제안

규율 (HANDOFF 5절의 Jay 피드백을 따를 것):
  - 항목마다 개별 커밋, 실제 pre-commit 훅 통과, 배치 금지
  - 모든 동작 변경에 특정 출력 텍스트를 단언하는 테스트 + 부정 케이스
  - 초록 테스트는 증거가 아닙니다. 결함을 심어서 잡히는지 확인할 것
    (이 세션에서 나온 결함 10건이 전부 초록 상태에서 나왔습니다)
  - 강제를 늘리지 말 것. "되돌릴 수 없거나 무시된다고 실측된 것"에만
  - 문구 재작성 금지 (역대 성공률 0/2)

금지:
  - llama.service · llama.env · kilo.jsonc 변경 금지
  - kilo serve 데몬 kill 금지 (Jay의 Cursor)
  - GPU 크론과 경합 금지 — ~/.hermes/longform/.render.lock 확인 후 실행
  - 새 문서 생성 금지, ~/.hermes/*.md 를 저장소에 커밋 금지

끝나면 ~/.hermes/opus_round37_report.md 에 보고서를 쓰고 origin/master에 push하세요.
```

---

## 7-6. 라운드 47 — "실패 아웃풋 말고 요청에 반응하게" (2026-09-04)

발단은 Jay의 지시입니다 — *"매번 그런식으로 진행할 수 없잖아요… 게이트 풀리길 기다리지말구요.
그 구조의 결함은 무엇인지 원인을 밝히고 이런 실패 아웃풋이 나오지 않고 사용자 요청에 정상적으로
반응할 수 있도록 시스템 구조를 개선하자는 의미입니다."* 같은 날 실사례 둘: **①** 00:46:49 —
서브태스크 5를 닫은 `bffac84` 직후, 문서 2차검증+인계 프롬프트 요청이 상태보고만 받고 끝남.
**②** 00:19:01→00:20:19 — primer 단독 커밋 `404fd8a`가 턴 중간에 착지해 `TerrainManager.ts`가 막힘.

### 먼저 잰 것 — 이건 "거부"가 아니라 "연기"였습니다

`kilo.db`의 warms-mobile 전 세션 실메시지를 전수 재생했습니다:
**진짜 사용자 턴 95개 중 11개(11.6%)가 체크포인트가 선 채로 시작해 mutating 호출 0건으로
끝났습니다**(11개 중 10개가 실제 행동을 원한 요청, 나머지 1개는 순수 질문이라 정상). 라운드
44/45가 이미 이 턴들에서 **재시도 폭주는 없앴습니다** — 00:05:33·00:46:49엔 모델이 도구를 아예
안 쓰고 보고만 했습니다(라운드 45 고지가 라이브에서 작동한 증거). 남은 건 노이즈가 아니라 **턴 전체**였습니다.

**설계를 결정한 발견**: 규칙 (a)와 라운드 39의 조용한-턴 규칙은 **다음 메시지가 뭐라고 적혔든**
경계를 풉니다 — 00:05:33엔 막혔고 00:09:35에 그가 한 줄 더 쓰자 **똑같은 작업이 그대로 나갔습니다.**
즉 상시 비용은 **왕복 한 번**이고, **"사용자가 두 번째 메시지를 보냈다"는 신호는 "사용자가 일부러
토큰을 쳤다"보다 약합니다.** 그래서 이 라운드는 안전을 푸는 게 아니라 **낭비되는 왕복을 없애면서
동의를 더 명시적으로** 만듭니다.

### 지은 것 — 규칙 (c): 판정이 아니라 리터럴

진짜 사용자 메시지에 **정확한 리터럴 `[gate-ok]`** 이 들어있으면, **그 메시지가 도착한 시점에 이미
서 있던 경계 하나**를 acknowledged 처리합니다. `kilo.db`의 **진짜 사용자 메시지 457건 전수에서 0건**
충돌(반면 맨단어 `gate`/`게이트`는 **23건** — 그래서 단어가 아니라 괄호 리터럴이 신호입니다).
`isMutating()`의 도구명 정확매치와 같은 계열이고, **라운드 45가 기각한 축(자유텍스트로 인지 판정,
정밀도 6.0~7.7%)은 재시도하지 않았습니다** — 텍스트를 판정하지 않으니까요. 상태 필드 추가 0
((a)·(b)와 같은 `acknowledged` 리스트를 씁니다).

**적용범위가 안전 논증 전부입니다 — 한 문장: 사용자가 칠 때 이미 존재하던 SHA에만 닿으므로 아무것도
사전승인하지 못합니다.**

- **#41 그대로 닫힘** — "계속"은 이 토큰이 아닙니다(T12 무변경).
- **라운드 30 수용기준 B 그대로** — 턴 중간에 생긴 primer 경계는 그 메시지보다 나중에 생긴 SHA라
  어떤 메시지도 승인했을 수 없습니다(라운드 46의 "62초 뒤에 생긴 SHA에 동의했을 수 없다"와 같은 논리).
  **그래서 위 사례 ②는 라운드 47에서도 여전히 막힙니다 — 의도된 것입니다.**
- **모델 자가승인 불가** — `userText`가 synthetic 파트를 전부 버립니다(모델이 쓸 수 있는 유일한 종류).
- **SHA 단위, 세션 단위 아님** — 다음 경계는 자기 토큰이 따로 필요합니다.

토큰을 이름으로 밝히는 곳은 **`NOTICE_BOUNDARY_PENDING` 하나뿐**입니다 — 토큰이 실제로 통하는
바로 그 순간에만 뜨는 메시지라서. 차단 메시지들은 **바이트 그대로** 뒀습니다(라운드 44의 주의 유지 +
턴 중간 경계는 토큰으로 못 푸니 거기서 광고하면 읽는 시점에 통하지 않는 처방이 됩니다).

### 계측 (전부 실측)

| 항목 | 결과 |
|---|---|
| 신규 테스트 | T26a~h, **57 → 66 단언**. 음성 케이스마다 **별도 repo/세션** (같은 세션에 이어 붙이면 규칙 (a)가 다른 이유로 통과시킴 — 작성 중 실제로 걸림) |
| 결함 심기 (하나씩) | 토큰 검사 제거 → **T12·T19b·T20·T23b**가 먼저 FAIL(기존 #41 회귀망이 이 오구현을 신규 테스트보다 먼저 잡음) / 리터럴→맨단어 `gate` → T26c·T26d / synthetic 파트 포함 → T26g / SHA ack 대신 세션 플래그 → T26e·T26f·T26h / 고지에서 토큰 제거 → T26d / ack을 고지 뒤로 이동 → T26e / 복원 → **0 FAIL** |
| 라이브 턴 오프라인 재현 | `kilo serve` 무개입, 플러그인 파일 직접 구동, `warms-mobile` 클론을 실제 SHA로 리셋. **00:46:49**: before 막힘 / after 토큰없음 막힘(문구만 토큰을 안내) / **after 토큰있음 실행됨**. **00:20:19 턴중간 경계**: 셋 다 막힘. 후속 메시지 경로는 셋 다 동일 |
| 템플릿 | **7스위트 전부 ALL PASS**, `check-caps.sh` **EXIT=0**, rule-archive 312/450 |
| fresh `bootstrap.sh` 전파 | 새 프로젝트 플러그인이 템플릿과 **바이트 동일**, `AGENTS.md`에 안내 1줄 포함 |
| 드리프트 | 라운드 47 직전 `warms-mobile`↔템플릿 공유파일 **0**, 이후도 **0** |

**부수 정리 — rule-archive PRUNE 실행.** 라운드 47 항목을 넣자 428/450(WARN 400 초과)이라,
라운드 46이 하던 의식대로 **Round 36 절을 `rule-archive-archive.md`로 이동**하고 스텁만 남겼습니다
(428 → **312줄**). 라운드 46은 하드캡을 깨고 나서야 프룬했는데, 그 교훈대로 이번엔 미리 했습니다.
`check-caps.regression`의 T14a가 AGENTS.md 줄수를 문구에 박아두고 있어 82→83으로 갱신했습니다
(라운드 40의 `ebb5e44`와 같은 유지보수).

### ⚠️ Jay가 알아야 할 부작용 하나

`warms-mobile` 백포트 커밋 3개가 HEAD를 `bffac84`에서 옮겼습니다. `bffac84`는 **primer 커밋이라
경계였고**, 이제 primer 이후 3커밋(임계 4)이라 **그 체크포인트는 사라진 상태**입니다. 라운드
44/45/46 백포트도 같은 성질이었지만 이번엔 명시해 둡니다 — 다음 라이브 턴은 체크포인트 없이 시작합니다.

### 남는 것 — Jay가 라이브에서 볼 것

Cursor를 완전히 껐다 켠 뒤(플러그인은 `kilo serve` 기동 시 1회 로드, 3-3절):

1. 서브태스크를 닫은 다음 턴에 아무 요청이나 하면, 모델이 **고지에서 `[gate-ok]`을 그대로 인용해
   알려주는지**. 안 알려주면 토큰은 코드로는 살아있지만 사용자에겐 안 보이는 상태입니다(그때는
   `AGENTS.md`/`README.md`의 1줄이 유일한 경로).
2. 같은 상황에서 요청 끝에 `[gate-ok]`을 붙였을 때 **그 턴에 바로 실행되는지**. 오프라인 재현으로는
   확인했지만 **라이브는 미검증**입니다.
3. 반대로 **토큰 없이는 여전히 한 턴 멈추는지**. 안 멈추면 그게 회귀이고 #41이 다시 열린 것입니다.

**정직하게 남는 트레이드오프**: 매 턴 토큰을 치는 사용자는 스스로 primer 체크포인트를 끈 것입니다.
메시지 단위·SHA 단위·비영속이라는 것 외엔 막을 방법이 없습니다. 다만 그건 이제 **"네 진행하세요"
한마디로 반사적으로 넘기는 것**이 아니라 **명시적으로 내리는 결정**입니다.

**push — 템플릿은 Jay 승인 후 완료.** `origin/master`가 `b9beb4b`(305커밋)로 갱신됐고, 별도
fresh clone으로 7스위트 ALL PASS·check-caps EXIT=0 재확인했습니다(위 계측 표와 별개로,
push 직후 독립 재검증). `warms-mobile`의 3커밋(`22c2164` `4c1fc88` `e1c0ec0`, + 라운드
44/45/46의 `1df5d7c` `f451fb8` `685746e` `e04deaf` `20dac2f`)은 **로컬에만 남아있고 push
안 함** — Jay 지시: warms-mobile은 계측용 스크래치일 뿐 실제 산출물이 아니라 push 여부가
무의미함.

---

## 7-7. 라운드 48 — 번호는 맞는 척했고 파일은 다른 걸 건드렸습니다 (2026-09-04)

발단은 Jay가 **사람 눈으로 직접 대조해서** 찾은 드리프트입니다. 라이브(`warms-mobile`, 세션
`ses_f9d0dfa4bffeTLzGAKjOA3a0st`)에서 *"다음 이어서 진행하세요"* 한 줄에 커밋 3개가 났습니다:

| 커밋 | 제목이 주장한 번호 | 실제로 건드린 파일 | 프라이머 계획표의 그 번호 |
|---|---|---|---|
| `c9c2e95` | subtask 6 | `src/entities/Projectile.ts` | 6 = `src/entities/Projectile.ts` ✅ |
| `8a17bf4` | subtask 7 | `src/scenes/GameScene.ts`(+Projectile) | 7 = `src/systems/AI.ts` ❌ (한 건 9번 일) |
| `617a93d` | subtask 8 | `src/main.ts` | 8 = `src/ui/` ❌ (main.ts는 어느 줄에도 없음) |

**모델은 같은 재정렬을 전에 한 번 물어봤다가 Jay에게 명시적으로 거절당했습니다**(`404fd8a`
— *"아니요, 원래 순서대로"*). 이번엔 묻지 않고 조용히 재정렬했고, **번호만 순차적인 척**
붙였습니다. 라운드 46도 게이트도 아무 소리를 안 냈습니다: 제목이 적은 파일은 **실제로 커밋에
들어 있었고**(46의 축은 통과), 세 커밋 중 어느 것도 프라이머를 안 건드려 **경계가 아예 안
섰습니다**(게이트의 축은 발동 자체를 안 함). elective arm은 3/4로 임계 직전이었습니다.

### 왜 이게 가능했나 — 근본 구조 셋이 동시에 참이어야 합니다

1. **번호는 애초에 어떤 문서에서도 오지 않았습니다.** `build.md` 1단계는 *"활성 sub-task
   블록만 읽어라"*고 시키는데 그 블록에는 `시작:`/`목표:`만 있고 **번호가 없습니다.** 즉
   `progress: [sub-task N]`의 N은 처음부터 **모델이 자기 대화 기억을 인용한 값**이었습니다.
2. **그 값을 무엇과도 대조하지 않았습니다.** 라운드 46이 같은 커밋 메시지의 *파일* 축을 닫고
   **번호 축을 남겼습니다** — 46 이후 커밋 메시지에 마지막으로 남아 있던 무검증 자기서술입니다.
3. **프라이머를 안 건드리니 경계가 안 섰고, 그래서 `build.md` 6단계의 강제 정지가 없었습니다.**
   기억 기반 번호가 새 세션으로 리셋되지 못하고 **3개 서브태스크에 걸쳐 누적**됐습니다
   (3-4절: 한 sub-task = 한 세션이 품질 규칙이자 컨텍스트 규칙인 이유가 정확히 이것).

부수로 드러난 것 하나 — **계획표의 형식은 프로토콜 어디에도 없었습니다.** `design.md` 4단계는
*활성 블록*의 모양만 못박고 있었고, `N. 이름 — 경로 (크기)` 목록은 `warms-mobile`의 **관례**로만
존재했습니다. 그래서 새로 부트스트랩한 프로젝트에서는 이 검사가 읽을 것이 아예 없습니다.

### 문제 A — 지었습니다 (보고 계열, 강제는 1비트도 안 바뀜)

`scripts/subtask-report.sh`가 이제 **`progress: [sub-task N]`의 N을 그 커밋 시점 프라이머의
`N.` 계획줄이 가리키는 경로와 대조**합니다. 라운드 46이 *"이 커밋이 자기가 적은 걸 담고 있나"*를
물었다면 이건 *"그 N이 계획이 말하는 N인가"*를 묻습니다 — **같은 성격, 다른 축**입니다.

**적용범위가 전부입니다**(46·39와 같은 계열):
- `progress:` 제목 + **대괄호로 묶인** `[sub-task N]`만 (`404fd8a`처럼 맨텍스트 번호는 제외)
- em/en 대시 뒤 슬롯이 있는 것만 (46과 동일)
- 프라이머에 `N.`으로 시작하는 계획줄이 있고, 그 줄의 대시 뒤에 **기계가 읽을 수 있는 경로**가
  있을 때만 (없으면 조용히 넘기지 않고 **"확인 못 했다"고 Skipped에 적습니다** — 이 스크립트
  자신의 규칙: 침묵이 "검사했고 깨끗함"으로 읽히면 안 된다)
- **커밋이 건드린 것 중 계획줄의 어느 경로와도 안 맞을 때만** 신고합니다. 서브태스크는 계획에
  없는 파일을 정상적으로 같이 건드리므로 *"계획 경로 일부 누락"*은 신호가 아닙니다.
- 계획줄은 **그 커밋 시점의 프라이머**에서 읽습니다(나중에 고쳐 쓴 계획으로 과거를 재판하지 않음).

**오탐률 실측 — 두 저장소 전체 이력 345커밋**: `warms-mobile` 40커밋 중 자격 7건 → **2건 적중
(둘 다 진짜 양성, Jay가 손으로 찾은 바로 그 둘) · 5건 정상 음성 · 오탐 0**. 템플릿 305커밋 중
자격 0건 → 0건. 라운드 46이 "좁히지 않으면 12.6%가 오탐"이라고 잰 그 방법론 그대로입니다.

그리고 **`design.md` 4단계에 계획표 한 줄 형식을 못박았습니다.** 46의 검사가 `build.md` 3단계의
커밋 형식에 기대는 것과 같은 관계입니다 — 형식이 없으면 검사는 한 저장소에서 우연히 동작하는
것이지 하네스 기능이 아닙니다. required-read 합계는 **불변**(26,501/27,800 — 프로토콜 문서는
그 합계에 안 들어갑니다).

### 문제 B — 짓지 않았습니다. 근거는 추론이 아니라 구조 실측입니다

**① 명세된 그대로("이 범위에 커밋 N개 중 프라이머를 건드린 게 0개")는 자동 경로에서 도달
불가능합니다.** `scripts/post-commit-subtask-report`는 **프라이머를 건드린 커밋에서만** 발동하고
(설계상, T7이 못박음), 보고 범위는 *직전 프라이머 커밋 → 이번 프라이머 커밋*입니다. 즉 **범위의
끝은 정의상 항상 프라이머 커밋**입니다. `warms-mobile`의 실제 범위 10개를 전수 계산하니
프라이머 터치 수가 **예외 없이 정확히 1**이었습니다 — 그 줄은 **10번 중 0번 인쇄됩니다.**

**② 도달 가능한 재구성도 elective arm보다 항상 늦습니다.** 살릴 수 있는 형태는 있습니다 —
*"한 경계 안에 서로 다른 `[sub-task N]` 번호가 2개 이상"*은 닫힌 범위 10개에서 전부 1개
이하(오탐 0)이고 지금 열려 있는 범위 `[6,7,8]`에서 발화합니다. 그런데 **그것도 다음 프라이머
커밋에서야 인쇄됩니다.** elective arm은 그보다 먼저, 커밋 4개째에 **차단하면서** 같은 사람에게
같은 사실을 이미 말합니다. **보고서의 트리거 자체가 "프라이머 가뭄이 끝나는 사건"이라, 가뭄 중에
경고하는 일은 이 층에서 구조적으로 불가능합니다.**

그래서 문제 B의 전제(*"임계 4 전엔 신호가 없다"*)는 맞지만, **report-only 층에서는 고칠 수
없습니다.** 고치려면 새 트리거나 새 강제가 필요하고 둘 다 이번 라운드의 금지사항입니다.
값 못 버는 검사는 안 만든다는 원칙대로 **안 지었습니다.** 언젠가 필요하다면 정직한 자리는
보고서가 아니라 게이트의 **고지**(라운드 45의 `NOTICE_BOUNDARY_PENDING`)입니다.

### 계측 (전부 실측)

| 항목 | 결과 |
|---|---|
| 오탐 | **345커밋 전수**(warms-mobile 40 + 템플릿 305) → 신고 2건, **전부 진짜 양성**, 오탐 0 |
| 신규 테스트 | T21a~i, **55 → 66 단언** |
| 수정 전 FAIL 확인 | 양성 3건(T21a·T21g·T21i) 전부 **수정 전 스크립트에서 먼저 FAIL** · 음성 6건은 수정 전에도 통과(= 진짜 음성임을 증명) |
| 결함 심기 (하나씩) | 대괄호 요건 제거→**T21d** / 디렉토리 접두 매칭 제거→**T21c** / basename 매칭 제거→**T21e** / 프라이머단독 가드 제거→**T21h** / Skipped 고지 제거→**T21g** / 계획줄을 작업트리에서 읽기→**T21i** / 복원→**0 FAIL**. 6개 전부 **정확히 한 테스트씩** 잡음 |
| 라이브 오프라인 재현 | `7829979..617a93d`를 실제 SHA로 재생 → `8a17bf4`·`617a93d` 신고, **`c9c2e95`(번호 맞음)는 침묵** |
| 템플릿 | **7스위트 전부 ALL PASS** · `check-caps.sh` **EXIT=0** · required-read 26,501/27,800(불변) |
| fresh `bootstrap.sh` 전파 | 새 프로젝트 파일이 템플릿과 **바이트 동일** + 거기에 드리프트 커밋을 심으니 **실제로 잡음** |
| 드리프트 | 라운드 48 직전 공유 20파일 **0**(AGENTS.md는 부트스트랩 커스터마이즈로 원래 다름), 백포트 후도 **0** |

### 정직하게 남는 한계

- **즉시 경보가 아닙니다.** 라운드 46과 같은 한계 — 다음 서브태스크 경계 보고에서 뜹니다.
  이번 사례에선 그 3커밋이 아직 경계를 못 만들어서 **지금도 보고서가 안 돌았습니다**(수동
  실행으로만 확인). 위 문제 B의 ②가 이 한계의 이유이자 그걸 report-only로 못 고치는 이유입니다.
- **계획표에 경로가 없으면 아무것도 못 합니다.** 10번 항목(*"모바일 최적화 — 터치 조작"*)처럼
  경로가 없는 줄은 검사 불가이고, 그때는 Skipped에 그렇게 적습니다(T21g).
- **번호가 맞고 파일도 맞는데 순서만 뒤바뀐 경우**는 여전히 못 잡습니다(예: 7과 9를 통째로
  맞바꿔 두 커밋 다 자기 계획줄과 일치하게 만든 경우). 그건 텍스트 판정이 아니라 계획 자체의
  변경 승인 문제라, 라운드 45가 정밀도 6%로 기각한 축에 다시 들어갑니다.

### ⚠️ Jay가 알아야 할 부작용 하나

`warms-mobile` 백포트 2커밋이 HEAD를 옮겨 **마지막 프라이머 커밋(`7829979`) 이후 5커밋**이
됐습니다. elective 임계가 4라 **다음 라이브 턴은 elective 체크포인트가 선 채로 시작합니다**
(직전엔 3/4였고 모델이 한 번만 더 커밋해도 어차피 떴을 상태입니다). 라운드 47의 `[gate-ok]`
리터럴 한 개로 그 자리에서 넘길 수 있습니다.

### 남는 것 — Jay가 라이브에서 볼 것

Cursor를 완전히 껐다 켠 뒤(플러그인은 `kilo serve` 기동 시 1회 로드, 3-3절):

1. 다음 서브태스크가 프라이머 커밋으로 닫힐 때, 보고서 **"확인이 필요한 것"에
   `says [sub-task N] but touched none of the paths ...`가 실제로 뜨는지.** 위 3커밋이 그
   범위에 들어가므로 뜨는 게 정상입니다.
2. 그리고 그때 **Jay가 계획표를 고칠지, 커밋 번호를 인정할지 결정**하시면 됩니다 — 이 검사는
   어느 쪽이 옳은지 판정하지 않습니다. **둘이 어긋났다는 사실만** 말합니다.

**push** — 템플릿은 **push 완료**. `origin/master` `b9beb4b`(305) → **`ec1038f`(308커밋)**,
독립 fresh clone으로 7스위트 ALL PASS·check-caps EXIT=0 재검증했습니다. `warms-mobile`
2커밋(`67ce805` `4d9fb7f`, + 라운드 44~47의 미push분)은 **로컬에만** — 계측용 스크래치라
push 여부가 무의미하다는 기존 지시 그대로입니다.

---

## 7-8. 실사용 검증 — `[gate-ok]` 라이브 확인 + 같은 드리프트 재발 관찰 (2026-09-04, 코드 변경 없음)

**라운드 47의 마지막 미검증 항목이 실제로 확인됐습니다.** warms-mobile 세션
`ses_f9d0dfa4bffeTLzGAKjOA3a0st`에서 elective 경계(`4d9fb7f`, primer 없이 5커밋)가 선 채로
Jay가 `"다음 작업 진행하세요. [gate-ok]"`를 보냄 → 상태파일 확인: `blockedCallsThisTurn`
빈 배열, `4d9fb7f`가 그 즉시 `acknowledged`에 들어감. 차단 안내 없이 같은 턴 안에서
스크린샷 정리 → 코드 작업 → 타입체크 → 커밋 → 빌드 → dev서버 재시작까지 전부 진행됨.
**라운드 47 완전히 종결.**

**같은 턴에서 라운드 48이 예측한 패턴이 그대로 재발했습니다.** 새 커밋 `7431967`
(`"progress: [subtask 9] — GameScene 개선: AI 턴, 폭발 이펙트, 턴 로직 완성"`)이 계획표
9번(씬 연결)이 아니라 AI 로직(계획 7번 몫, `src/systems/AI.ts`)을 `GameScene.ts`에
밀어넣었습니다. **이번에도 primer 미터치** — `7829979` 이후 이제 **6커밋째**. 라운드 48의
번호-계획 대조 검사는 primer 터치 커밋에서만 도니 **아직 발동 기회가 한 번도 없었습니다**
(7-7절 "정직하게 남는 한계" ①이 예측한 그대로). 새 elective 경계(`7431967`)가 지금 미확인
상태로 서 있어 다음 세션이 물려받습니다.

**코드 변경 없음, push 없음** — 순수 라이브 관찰/검증 세션. Jay 지시: "구조적 결함이 아니면
warms-mobile 콘텐츠는 고치지 말 것" — 이번 드리프트도 손대지 않고 그대로 다음 세션에 넘깁니다.

---

## 7-9. 라운드 49 — 턴 하나가 뭔가 했다는 게 전부 했다는 뜻은 아닙니다 (2026-09-05)

발단은 독립 감사(Opus, 코드 미수정)가 실측으로 확정한 4건입니다. 그중 첫째가
**FEEDBACK #6의 전제가 실제로 무너진 첫 사례**입니다.

### ① 섞인 턴에서 팩트체크가 통째로 침묵했습니다 (고침)

라이브 `ses_f97ad8de5ffetACPSexiPxvuhV` 22:29~22:42. 한 턴 안에서 `SESSION_PRIMER.md` 커밋은
**성공**(`2394aa1`)하고 `PROJECT_BACKGROUND.md` write는 **차단**됐는데, 모델이
*"PROJECT_BACKGROUND.md 업데이트 완료"* 라고 보고했고 **팩트체크는 0건 발동**했습니다. 원인은
판정식입니다 — `currentHead===priorHead && currentDirty===priorDirty`, 즉 **턴 전체가 조용할
때만** 발화합니다. 한 시간 뒤 23:33에 Jay가 손으로 잡았습니다(*"실제로는 커밋되지 않았고
파일도 안 바뀌었습니다"*).

*"이 턴에 뭔가 성공했다"* 와 *"이 턴에 차단됐던 그 파일이 여전히 안 바뀌었다"* 는 배타적이지
않은데 옛 판정식은 배타적인 것처럼 다뤘습니다. 조용한 턴 분기는 **라운드 31 문구 그대로**
두고, 움직인 턴에서는 **차단된 호출 하나하나에 같은 질문**을 던집니다 — 경로별 git 질의 2개
(그 구간 커밋이 이 경로를 건드렸나 / 지금 dirty 인가). porcelain 파싱도 경로 분류도 없습니다.

**적용범위가 안전 논증 전부입니다**(39/46/48과 같은 계열): 정확한 `filePath` 인자를 가진
`write`/`edit`만 대상 — **bash 는 명령을 파싱해야 하므로 구조적으로 제외**(라운드 45가 정밀도
6%로 기각한 자유텍스트 판정 축). git 이 못 답하거나(null) 경로가 없거나 실제로 바뀌었으면
전부 침묵이라, **이 분기의 최악은 아무 말도 안 하는 것**입니다. 셸 메타문자가 있는 경로는
이스케이프하지 않고 "확인 불가"로 버립니다(테스트 작성 중 `$(touch pwned)` 가 실제로 실행되는
것을 확인하고 넣은 가드입니다).

`detail` 은 60자 표시용 절단이라 검사에 못 씁니다 — 라이브의 그 경로가 정확히 70자라
basename 이 잘려 있었습니다. 같은 throw 지점에서 **잘리지 않은 저장소 상대경로**를 같이
기록합니다(파생값, 새 진실 원천 아님).

### ② `[gate-ok]` 가 라운드 39의 오탐 수정을 재오염시켰습니다 (고침)

라운드 47의 리터럴은 **라틴 글자 6개**입니다. 짧은 한국어 지시에 붙이면
`anchorHeuristicApplies()` 의 라틴≤비라틴 판정이 뒤집혀 **라운드 39가 한국어에서 빼놓은 넛지가
다시 켜집니다.** DB 에 그 턴의 양쪽이 1밀리초 차이로 남아 있습니다 — 22:24:44
`"네, 진행하세요. [gate-ok]"` 와 바로 뒤 `"STOP before doing anything else..."`.

토큰은 **게이트에 보내는 제어 신호이지 사용자가 묘사한 일감이 아닙니다.** 그래서 내용 판정기는
전부 토큰을 뺀 텍스트를 읽습니다. `GATE_OVERRIDE_TOKEN` 에서 단일 출처로 파생(대소문자 무시,
waiver 와 같은 규칙)했고, 휴리스틱 자체는 1비트도 안 바뀝니다 — 토큰을 단 영어 애매 문장은
여전히 넛지됩니다(T27b).

### ③ 대형 `write` 크래시 — **짓지 않았습니다. 구조 실측이 근거입니다**

3회 재현(`kilo.db` 전수: write+edit **608건 중 3건**, 0.49%). 전부 기존 마크다운 문서의 통째
재작성이고 에러는 `Invalid input for tool write: JSON parsing failed` 입니다.

**결정적 사실 둘 — 이 층에서는 도달 불가능합니다.**

1. 3건 모두 파트의 `state.input` 키가 **`["value"]`**(원문 문자열 그대로)입니다. `filePath` 도
   `content` 도 파싱된 적이 없으니, `tool.execute.before` 가 읽을 크기도 경로도 **존재하지
   않습니다.**
2. 그리고 그 훅은 **애초에 돌지 않았습니다.** 22:30:34 시점엔 primer 경계가 이미 서 있었고
   (`2394aa1`, 22:30:12), 훅이 돌았다면 22:41:51에 같은 파일에 실제로 났던
   `BLOCK_MESSAGE_COMMIT` 이 나왔어야 합니다. 대신 SDK 파싱 에러가 났습니다.

즉 크기 게이트는 **크래시하지 않은 write 에만** 발화합니다 — 고치려는 집합의 여집합입니다.
재시도 감지도 같은 이유로 불가능합니다(1회차 실패를 기록할 수 없음). 게다가 성공한 write
216건 중 상당수가 **추적 중인 파일의 정당한 통째 재작성**이고(`SESSION_PRIMER.md` 6건 포함 —
build.md 3단계가 시키는 바로 그 동작), 막으면 그게 전부 오탐입니다. 라운드 48 문제 B와 같은
결론: **값 못 버는 검사는 안 만듭니다.** 정직한 자리는 하네스가 아니라 프롬프트/사람입니다.

### ④ 2층 버전 환각 (고침 — 프롬프트가 아니라 도구로)

라이브 정밀도 **0/14**, 그중 **5건이 한 종류**입니다 — `typescript ^7.0.2`, `vite ^8.2.2`,
`@types/node ^26.4.1`, `Phaser 4` 가 *"존재하지 않는다"*. `node_modules` 에 넷 다 **정확히 그
버전으로** 설치돼 있습니다. 모델 학습 시점 이후 버전일 뿐입니다.

문구 재작성이 아닙니다(0/2, 그리고 학습 컷오프는 지시로 안 움직입니다). **판정 가능한 것은
도구에 맡긴다** — 세 조건이 전부 참일 때만 강등합니다: (a) 비존재 주장 문구, (b)
`package.json` 이 **선언**하고 `node_modules` 에 **설치**된 패키지 이름을 언급, (c) 버전 토큰이
있음. 방향이 중요합니다 — 프롬프트에서 이름을 뽑는 게 아니라 **선언된 이름들을 텍스트에 정확
부분일치**로 시험합니다(`isMutating()` 계열). 강등된 건 **여전히 인쇄**되고 설치 버전을 같이
적으며 개수도 밝힙니다 — 이 프로젝트 규칙대로 침묵이 "검사했고 깨끗함"으로 읽히면 안 됩니다.

**계측**: 라이브 5건 전부 강등 · 설치 안 된 진짜 엉터리 버전(`lodash ^99.0.0`)은 **그대로
신고** · 버전 없는 범주 주장은 **그대로 신고** · off-by-one 같은 일반 결함 무영향 ·
`package.json`/`node_modules` 가 없는 저장소(라운드 37의 n=16 파이썬 픽스처 전부)는 **무영향**.

### ⑤ carryover 중복 제거 (고침)

idle nudge 는 라운드 27부터 시그니처 dedup 이 있었는데 쌍둥이인 미커밋 안내엔 없었습니다.
`kilo.db` 전수: 안내 **93건 중 distinct 54건**, 최악 **한 세션에서 12번 바이트 동일 반복**(커밋할
생각 없던 스크린샷 1개). idle nudge 와 같은 규칙 — **세션 단위가 아니라 dirty 시그니처 단위**라
다른 파일이 새로 더러워지면 다시 말합니다. 상태 필드 추가 0, git 호출 추가 0(직전 턴 시그니처가
이미 이 훅에 `priorDirty` 로 있습니다).

### 계측 (전부 실측)

| 항목 | 결과 |
|---|---|
| 신규 테스트 | 게이트 T27a~d·T28a~f·T29a~b (**66 → 78 단언**) · 2층 T12a~i (**26 → 35 단언**) |
| 수정 전 FAIL 확인 | T27a·T27c / T28a / T12a·T12b·T12c·T12g / T29a 가 **수정 전 코드에서 먼저 FAIL**. 나머지는 수정 전에도 통과(= 진짜 음성) |
| 결함 심기 (하나씩) | 토큰 strip→identity **T27a+T27c** / 정규식 `i` 제거 **T27c** / bash 를 경로로 파싱 **T28d** / 경로 미기록 **T28a** / 커밋 절반 제거 **T21(기존 회귀망)+T28c** / 작업트리 절반 제거 **T28b** / 두 분기 비배타 **T20(기존)+T28e** / 셸 문자 가드 제거 **T28f**(주입 명령이 실제로 실행됨) / 비존재문구 요건 제거 **T12h** / 미선언 패키지 포함 **T12i** / 설치검사 제거 **T12d** / 버전토큰 요건 제거 **T12e** / 전부 복원 → **0 FAIL** |
| 오탐 실측 (①) | `kilo.db` 전수 — 차단된 write/edit 를 가진 턴 **69개** 중 새 분기 발화 **17개**, 그중 실프로젝트 **5개**. 발화 문장은 전부 git 파생 사실이라 거짓 진술 부류가 **구조적으로 없음**. 5건 중 **2건이 실제 허위 완료보고 위에 앉습니다** — 라이브 그 건과 `sm4-r28-verify` 의 *"word-count.sh 생성 완료"*(이번에 새로 발견). 나머지 3건은 모델이 이미 정확히 보고한 차단을 다시 말합니다 |
| 오탐 실측 (④) | 라이브 finding 14건 전수 + 음성 대조 4종 |
| 라이브 오프라인 재현 | `kilo serve` 무개입, 플러그인 직접 구동, warms-mobile 클론을 실제 SHA(`7431967`)로 리셋 후 22:24:44→23:33:21 재생. **before: 넛지 발동 + 팩트체크 0건 / after: 넛지 없음 + 팩트체크 1건**(`write(wiki/PROJECT_BACKGROUND.md)` 를 이름으로 지목) |
| 템플릿 | **7스위트 전부 ALL PASS** · `check-caps.sh` **EXIT=0** · required-read 26,501/27,800(불변) · rule-archive 312/450 |
| fresh `bootstrap.sh` 전파 | 새 프로젝트 파일이 템플릿과 **바이트 동일**, 거기서 게이트 스위트 ALL PASS + 2층 필터가 심은 환각을 **실제로 강등** |
| 드리프트 | 라운드 49 직전 `warms-mobile`↔템플릿 공유 11파일 중 **라운드 49분 3개만** 차이(나머지 0), 백포트 후 **0** |

### ⚠️ Jay가 알아야 할 부작용 하나

`warms-mobile` 백포트 3커밋(`0757f7c` `fd6aead` `fe9f81c`)이 HEAD 를 옮겨, primer 마지막 터치
(`755a195`) 이후 **4커밋**이 됐습니다. elective 임계가 4라 **다음 라이브 턴은 elective
체크포인트가 선 채로 시작합니다.** 라운드 47의 `[gate-ok]` 한 개로 그 자리에서 넘길 수
있고, 이제 그 토큰이 애매함 넛지를 부르지 않습니다(②).

### 남는 것 — Jay가 라이브에서 볼 것

Cursor를 완전히 껐다 켠 뒤(플러그인은 `kilo serve` 기동 시 1회 로드, 3-3절):

1. **①이 이 라운드의 유일한 미검증 핵심입니다.** 한 턴 안에서 뭔가는 커밋되고 뭔가는
   차단된 다음 턴에, 사용자 메시지 위에 `fact check: other work did land last turn, but ...`
   이 붙는지. 안 붙으면 데몬이 구버전입니다. **그리고 모델이 그걸 받고 실제로 정정 보고를
   하는지** — 붙는데도 계속 "완료"라고 하면 이 수정은 실패한 것이고 그때는 그렇게 적을 것.
2. 짧은 한국어 지시에 `[gate-ok]` 을 붙였을 때 STOP 넛지가 **더는 안 붙는지**(②).
3. 미커밋 파일이 남아 있을 때 안내가 **매 메시지가 아니라 한 번만** 뜨는지, 그리고 **새 파일이
   더러워지면 다시 뜨는지**(⑤).

**push** — 템플릿 **push 완료**. `origin/master` `ec1038f`(308) → **`d52cd7f`(316커밋)**,
독립 fresh clone 으로 7스위트 ALL PASS·check-caps EXIT=0 재검증했습니다. `warms-mobile`
3커밋은 **로컬에만** — 계측용 스크래치라 push 여부가 무의미하다는 기존 지시 그대로이고,
**하네스 파일 3개만** 건드렸습니다(`SESSION_PRIMER.md`·`PROJECT_BACKGROUND.md`·`src/` 무변경,
`git diff --name-only 252cd99..HEAD` 로 확인).

---

## 7-10. 라운드 50 — 아무것도 막히지 않았는데 아무것도 저장되지 않았습니다 (2026-09-05)

발단은 라이브입니다(`warms-mobile`, 세션 `ses_f92a556e4ffepjAjQkfai9H6fN`, 09-05 01:57:33~02:02:08).
Jay가 *"SESSION_PRIMER 읽고, FEEDBACK_PENDING 이슈 재확인, 그다음 권장순위대로 이어서 진행하세요"*
라고 지시했고, 모델은 protocol-read 게이트에 1회 걸린 뒤 문서를 읽고 통과한 다음 **`write`/`edit`
14회를 전부 성공**시켰습니다(신규 `ResultScene.ts` · `GameScene.ts` · `FEEDBACK_PENDING.md` ·
`SESSION_PRIMER.md`). 그리고 *"모든 작업 완료"* 로 턴을 끝냈습니다 — **커밋 0회.**

**이건 게이트가 막은 사건이 아닙니다.** 전부 성공했고 훅도 정상이었습니다. `AGENTS.md`/`build.md`의
"commit per file, always"는 **프로즈로만** 있었고, *"커밋 없이 몇 개가 쌓였는가"* 를 세는 기제가
없었습니다.

### 먼저 잰 것 — 기존 기제 둘은 실제로 발화했습니다. 늦었을 뿐입니다

DB에 그대로 남아 있습니다.

| 시각 | 사건 |
|---|---|
| 02:02:08 | 모델의 *"모든 작업 완료"* 요약 |
| 02:02:12 | **idle nudge 발화** — 4개 경로를 정확히 이름으로 나열. 그런데 라운드 27이 `noReply: true`로 붙이므로 **모델이 답할 턴이 없습니다** |
| 02:50:01 | **carryover 고지 발화** — 역시 정확. 다만 그 자리에 이미 Jay가 손으로 찾아낸 지시가 함께 도착했습니다 |

즉 **하네스가 침묵한 게 아니라, 말할 수 있는 가장 빠른 순간이 턴이 끝난 뒤였습니다.** 그리고
그 48분 사이에 세션이 죽었으면 4개 파일이 전부 working tree에만 남았습니다.

### 구조 실측 — 라운드 44가 적어둔 "이 층의 레버는 던지는 메시지뿐"은 절반만 맞았습니다

그 문장은 `tool.execute.before`에 대해선 참이고, **나머지 절반은 한 번도 확인된 적이 없습니다.**
이 프로젝트의 L01 규율대로 **실제 돌고 있는 바이너리**(kilo 7.5.9, `ps`로 확인한 바로 그 프로세스)를
읽었습니다. `@kilocode/plugin`의 타입에 `"tool.execute.after"?: (input, output: { title, output,
metadata })` 가 있고, 내장 도구 경로의 호출부가 이렇습니다:

```
if (yield* _.trigger("tool.execute.after", {tool:O.id, ...}, r), c.abortSignal?.aborted) ...
return r
```

**훅에 건네지는 객체 `r`이 훅이 끝난 뒤 그대로 모델에게 반환됩니다.** 그래서 이 층에는
**비차단 채널이 있습니다** — `output.output`에 한 줄 덧붙이면 아무것도 거부하지 않고 턴 안에서
모델에게 닿습니다. 라운드 49 ③(대형 write 크래시)이 *"이 층에서는 도달 불가능"*이라 접었던 것과
**반대 결론이고, 이유도 반대**입니다: 그건 훅이 아예 안 돌던 경우였고 이건 훅이 정상으로 도는 경우입니다.

### 지은 것 (보고 계열, 강제는 1비트도 안 바뀜)

`write`/`edit`가 성공할 때마다, **이 턴이 시작할 때 더럽지 않았는데 지금 더러운 경로**를 세고
3개가 되면 결과 뒤에 한 줄을 붙입니다. 거부는 없습니다 — 이미 성공한 호출의 결과에 얹는 것이라
**거부할 대상 자체가 없습니다.**

**임계 3은 튜닝값이 아니라 `build.md`에서 유도한 값입니다.** step 2가 커밋 단위를 파일 하나로
못박고, step 3이 허용하는 예외는 **정확히 한 쌍**(서브태스크 마지막 파일 + `SESSION_PRIMER.md`)
뿐입니다. 그러니 2개는 프로토콜이 제대로 도는 모습일 수 있고 **3개는 그 쌍일 수 없습니다.**
(step 5의 자체 체크포인트 조건이 *"2+ files dirty and no commit"* 이라, 이건 프로토콜 자신의
선보다 한 칸 보수적입니다.)

**적용범위가 안전 논증 전부입니다**(39/46/48/49와 같은 계열):
- `write`/`edit`만. **bash는 구조적으로 제외** — 커밋을 하는 도구라 그 결과에 주석을 달면 문제가
  아니라 해법에 대고 말하는 셈이고, args가 경로가 아니라 명령입니다(라운드 45가 기각한 자유텍스트 축).
- **이 턴이 시작한 working tree와만 대조**합니다. 전체 트리를 세면 스크래치 파일 하나를 늘 더럽게
  두는 저장소가 **모든 턴의 모든 write마다** 영구히 발화합니다 — 이게 유일한 실질 오탐 계열이고
  `turnStartDirtySignature`(chat.message가 이미 매 메시지 기록 중)와 diff해서 닫았습니다.
- **한 번의 누적당 한 번만**. 이건 이 플러그인에서 가장 시끄러울 수 있는 표면(모델이 읽는 모든
  도구 결과에 얹힘)이라 가장 작은 용량을 씁니다.
- **카운터는 git입니다.** 커밋하면 그 경로들이 status에서 빠지므로 수치가 저절로 내려갑니다 —
  리셋 로직 없음. 상태는 "이번 턴에 이미 말했는가" 한 필드뿐이고 턴 경계에서 비웁니다.
- 전체가 try/catch 안이고 유일한 변경이 텍스트 덧붙이기라, **이 훅의 최악은 아무 말도 안 하는 것**입니다.

### 계측 (전부 실측)

| 항목 | 결과 |
|---|---|
| 오탐 실측 | `kilo.db` **실프로젝트 턴 236개 전수**(그중 성공 write가 있는 턴 85개) → **발화 턴 9개(3.8%), 고지 총 10건**. 10건 전부 *"서로 다른 3개 파일이 바뀌었고 하나도 커밋 안 됨"* 이라는 **참인 진술**(문장이 git 파생이라 거짓 진술 부류가 구조적으로 없음). 9개 중 **3개는 턴이 끝날 때까지 정말 커밋이 0**이었고, 나머지 6개는 그 턴 안에서 결국 커밋했습니다 — 즉 *"프로토콜 위반을 맞게 지목했는가"* 는 10/10, *"이 턴이 끝내 아무것도 저장 못 할 것인가"* 는 3/9입니다. **후자를 미리 아는 신호는 없습니다**(라운드 45가 정밀도 6%로 기각한 그 축) |
| 임계 스윕 | 2 → 23턴 발화(step 3의 정당한 쌍을 신고) · **3 → 9턴/10건** · 4 → 7턴/7건 |
| 라이브 턴 오프라인 재현 | `kilo serve` 무개입, 플러그인 직접 구동, `warms-mobile` 클론을 그 턴의 실제 시작 SHA(`752f6d4`)로 리셋 후 14회 write/edit 재생. **before: 그 훅이 아예 없음 → 0건** / **after: 02:00:43에 정확히 1건**(3개 경로를 이름으로) — Jay가 손으로 찾은 02:50보다 **81분 앞**입니다 |
| 신규 테스트 | T30a~i, **78 → 87 단언** |
| 결함 심기 (하나씩) | 전체트리 카운트→**T30a/b/c/d/e/h/i** / dedup 제거→**T30b** / 임계 2→**T30a/b/c/e/i**(정당한 쌍을 신고) / 턴경계 리셋 제거→**T30e** / bash 포함→**T30f** / 커밋 후 재무장 제거→**T30c** / 턴시작 스냅샷 가드 제거→**T30g** / 전부 복원 → **0 FAIL** |
| 수정 전 확인 | 라운드 49 플러그인에는 `tool.execute.after` 훅이 **없어서** T30 블록이 실행 자체가 안 됩니다(`hooks.tool.execute.after is not a function`) |
| 템플릿 | **7스위트 전부 ALL PASS** · `check-caps.sh` **EXIT=0** · required-read 26,501/27,800(불변) · rule-archive 312/450 |
| fresh `bootstrap.sh` 전파 | 새 프로젝트 플러그인이 템플릿과 **바이트 동일**, 거기서 3개 누적을 만드니 **실제로 발화** |
| 드리프트 | 라운드 50 직전 `warms-mobile`↔템플릿 공유파일 **0**, 백포트 후도 **0** |

### 정직하게 남는 한계

- **6/9는 그 턴 안에서 결국 커밋한 턴입니다.** 고지 내용은 그때도 참이지만("지금 3개가 미커밋"),
  모델이 어차피 하려던 일을 말한 셈일 수 있습니다. 이걸 미리 가르는 신호는 없습니다.
- **모델이 이 줄을 읽고 실제로 멈춰서 커밋하는지는 미검증**입니다. 오프라인 재현이 증명한 건
  "그 순간에 그 문장이 붙는다"까지입니다. 다만 같은 계열의 선례는 2/2입니다 — 라운드 44의
  `[repeat]` 접미사와 라운드 45의 고지 둘 다 라이브에서 작동이 확인됐습니다(각각 7-5절·7-6절).
  이건 *"같은 주장을 더 세게 다시 쓰기"*(역대 0/2)가 아니라 **없던 순간에 없던 사실을 주는 것**이라
  그쪽 계열입니다.
- **`noReply: true`인 idle nudge는 그대로 뒀습니다.** `false`로 바꾸면 모델이 실제로 응답해서
  커밋할 가능성이 있지만, 그건 라이브 데몬 없이는 검증이 불가능하고(데몬 kill 금지) 무엇보다
  **부르지 않은 턴을 하네스가 시작시키는 것**이라 성격이 다릅니다. 필요하면 다음 후보입니다.

### ⚠️ Jay가 알아야 할 부작용 하나

`warms-mobile` 백포트 2커밋이 HEAD를 `6dea13f`에서 옮겼습니다. `6dea13f`는 **primer 커밋이라
경계였고**, 이제 primer 이후 2커밋(임계 4)이라 **그 체크포인트는 사라졌습니다** — 다음 라이브
턴은 체크포인트 없이 시작합니다.

### 남는 것 — Jay가 라이브에서 볼 것

Cursor를 완전히 껐다 켠 뒤(플러그인은 `kilo serve` 기동 시 1회 로드, 3-3절):

1. 한 턴에서 서로 다른 파일 3개를 커밋 없이 고치게 하면, **3번째 `write`/`edit` 결과 끝에**
   `[subtask-gate] 3 files have now been changed this turn with nothing committed ...`이 붙는지.
   안 붙으면 데몬이 구버전입니다.
2. 그리고 **모델이 그걸 받고 실제로 멈춰서 커밋하는지.** 붙는데도 계속 쓰기만 하면 이 수정은
   실패한 것이고, 그때는 되돌리고 그렇게 적을 것(0/2가 0/3이 됩니다).
3. 4번째·5번째 파일에서 **다시 안 뜨는지**(누적당 한 번), 그리고 커밋한 뒤 새로 3개가 쌓이면
   **다시 뜨는지**.

**push** — 템플릿 **push 완료**. `origin/master` `d52cd7f`(316) → **`c15b42c`(318커밋)**,
독립 fresh clone으로 7스위트 ALL PASS·check-caps EXIT=0 재검증했습니다. `warms-mobile`
2커밋(`7cd5d8a` + 테스트)은 **로컬에만** — 계측용 스크래치라 push 여부가 무의미하다는 기존 지시
그대로이고, **하네스 파일 2개만** 건드렸습니다(`git diff --name-only 6dea13f..HEAD`로 확인).

---

## 8-1. 오늘 사이클 마무리 — 라운드 44~50을 여기서 닫습니다 (2026-09-05)

> 이 절은 라운드 절이 아닙니다. **하루 동안 이어진 이 테스트 사이클 전체의 결론**이고,
> 다음 세션이 "또 라운드를 열어야 하나"를 판단할 때 읽을 자리입니다.

### 무엇을 확인했는가

라운드 39~43이 *"입력 언어·실행 환경·배포 전파"* 라는, 37라운드가 한 번도 안 흔든 경계를 닫았고,
**44~50은 전부 같은 한 가지 질문의 변주**였습니다 — *"게이트가 옳게 판정했는데도 사용자의 턴이
왜 낭비되는가."* 일곱 라운드가 찾은 답을 한 줄로 줄이면 이렇습니다:

> **판정 논리는 처음부터 거의 다 맞았습니다. 부족했던 건 전부 "그 사실을 언제, 누구에게, 어떤
> 문장으로 말하는가"였습니다.**

실제로 이 사이클에서 **차단 범위는 단 한 번도 넓어지지 않았습니다.** 넓힌 건 라운드 47의
`[gate-ok]` 하나뿐이고 그건 오히려 **푸는** 쪽입니다. 나머지 여섯은 전부 보고 계열입니다.

| 라운드 | 증상 | 실제 결함 | 계열 |
|---|---|---|---|
| 44 | 같은 턴 재시도 6회 폭주 | 차단 메시지가 *"실행되지 않았다"*를 안 말함 | 보고 |
| 45 | 구체적 지시가 왜 안 먹히나 | 이미 계산해둔 경계를 버리고 있었음 | 보고 |
| 46 | "풀고 진행"인데 또 막힘 | 2시간 전 커밋 메시지가 거짓이었음(primer 미포함) | 보고 |
| 47 | 실패 아웃풋 말고 요청에 반응하게 | 왕복 한 번이 상시 비용 → 리터럴 토큰 | 해제 |
| 48 | 번호는 맞는 척, 파일은 딴 것 | `[sub-task N]`의 N이 어떤 문서에서도 안 옴 | 보고 |
| 49 | 섞인 턴 팩트체크 침묵 · 2층 버전환각 | 판정식이 "턴 전체가 조용할 때"만 참 · 학습컷오프 | 보고 |
| 50 | 다 성공했는데 하나도 저장 안 됨 | 미커밋 누적을 세는 기제가 아예 없었음 | 보고 |

**그리고 이 사이클이 실제로 확인한 것 셋** — 전부 실측이고 추측이 아닙니다:
- **라이브에서 작동이 확인된 수정**: 라운드 44의 `[repeat]`(46에서 확인), 라운드 45의 고지
  (47에서 확인), 라운드 47의 `[gate-ok]`(7-8절에서 확인). 즉 **"없던 순간에 없던 사실을 주는"
  수정은 3/3**이고, 이건 *"같은 주장을 더 세게 다시 쓰기"*(역대 0/2)와 다른 계열임이 실측으로 갈렸습니다.
- **안 지은 것도 셋**: 라운드 45(자유텍스트 구체성 판정, 정밀도 6.0~7.7%), 라운드 48 문제 B
  (report-only 층에서 구조적으로 도달 불가), 라운드 49 ③(훅이 아예 안 도는 크래시). **"값 못
  버는 검사는 안 만든다"가 세 번 실제로 발동했습니다.**
- **라운드 44의 "이 층의 레버는 하나뿐"이 틀렸음**을 라운드 50이 바이너리 판독으로 뒤집었습니다.
  이 사이클의 자체 교정 사례입니다 — **문서가 적어둔 구조적 불가능도 의심하고 실제로 확인할 것.**

### 무엇이 남았는가 — 세 갈래로 갈라서

**(a) 로컬 모델의 한계로 받아들이고 넘어갈 것.** 더 파도 하네스로는 못 고칩니다.
- **자기서술 자체는 못 막습니다.** FEEDBACK #6은 원래 그렇게 접수됐고, 라운드 31/49가 한 건
  *"거짓말을 막는 것"*이 아니라 *"git 사실로 다음 턴에 반박하는 것"*입니다. 라운드 50도 같습니다.
- **"이 턴이 끝내 커밋을 안 할 것인가"는 미리 알 수 없습니다.** 라운드 50이 3/9로 실측했고,
  이걸 가르려면 텍스트 판정으로 돌아가야 하는데 그 축은 6.0~7.7%로 이미 기각됐습니다.
- **번호·순서를 통째로 맞바꾼 계획 드리프트**(7-7절 마지막 한계) — 계획 변경 승인의 문제이지
  기계 판정의 문제가 아닙니다.
- **SDK 파싱 단계에서 죽는 대형 write**(7-9절 ③) — 훅보다 먼저 실패하므로 이 층 밖입니다.

**(b) 실사용에서 또 나오면 그때 열 후보.** 지금 열지 마세요 — 근거가 아직 1건씩뿐입니다.
- **idle nudge를 `noReply: false`로.** 라운드 50이 검토했지만 라이브 검증이 불가능해 안 했습니다.
  "턴이 끝났는데 미커밋"이 **반복해서** 실제 손실로 이어지면 그때가 근거입니다.
- **`design.md` 계획표에 경로가 없는 줄**(7-7절) — 라운드 48 검사가 Skipped로 넘깁니다. 계획표
  형식을 더 조이는 건 실제로 걸릴 때 하면 됩니다.
- **아직 안 흔든 축**(7-2절 재채점의 잔여): 동시 세션 · 비UTF8 · 비리눅스 경로 · 초대형 저장소.
- **2층 68.75%의 미탐 3종**(4절 B) — 여전히 "문구 재작성" 계열이라 before/after n=16 재측정
  없이는 손대지 말 것.

**(c) 다음 라이브 턴에서 그냥 지켜볼 것 — 새 코드 없이.**
- 라운드 48의 번호-계획 대조 검사: **아직 한 번도 실전 발동한 적 없음.** `6dea13f`가 primer를
  건드렸으니 **다음 primer 커밋이 첫 기회**입니다.
- 라운드 49 ①(섞인 턴 팩트체크)과 라운드 50(턴중 고지): **둘 다 라이브 미검증.**

### 그래서 지금 일반 사용에 들어가도 되는가 — 예

**들어가도 됩니다.** 근거는 이렇습니다.

1. **이번 사이클의 결함은 전부 "말하는 방식"이었고, 판정 논리에서는 한 건도 안 나왔습니다.**
   라운드 44는 게이트가 *"문서화된 계약대로 정확히 동작했다"*로 끝났고, 45·46은 아예 게이트를
   안 고쳤고, 48·50은 게이트를 1비트도 안 건드렸습니다. **되돌릴 수 없는 것에 거는 두 강제
   (시크릿 차단·sub-task 경계)는 이 사이클 내내 논쟁 대상이 아니었습니다.**
2. **일곱 라운드에서 차단 범위가 순증한 적이 0번입니다.** 늘어난 건 전부 보고이고, 유일한 범위
   변경(라운드 47)은 사용자가 명시적으로 푸는 쪽입니다. 강제를 최소화하라는 Jay의 원칙이
   실제로 지켜졌는지의 답이 이겁니다.
3. **오탐 실측이 매 라운드 전수로 붙어 있습니다** — 46(345커밋), 48(345커밋), 49(kilo.db 전수),
   50(실프로젝트 236턴 전수). 오탐률이 추정이 아니라 세어본 값입니다.
4. **7스위트 ALL PASS·check-caps EXIT=0이 매 라운드 fresh clone으로 재현됩니다.** 이번에도
   push 후 독립 클론에서 다시 확인했습니다.

**다만 "완벽"은 여전히 도달 불가능한 목표입니다**(7절의 원래 판단 그대로). 오늘 나온 결함 7건도
전부 초록 테스트 상태에서 나왔고, 앞으로 쓰면 또 나옵니다. 그건 이 도구가 나빠서가 아닙니다.

### 다음 세션에게 — 하지 말 것

**라운드를 먼저 열지 마세요.** 라운드 17~26이 10라운드를 태운 게 정확히 *"쓰지도 않으면서 계속
다듬은 것"*이고, 오늘 44~50이 값을 번 이유는 **전부 Jay의 실제 트랜스크립트 한 건에서 출발했기
때문**입니다. 발단이 `kilo.db`의 실제 세션이 아니면, 그건 열 이유가 아닙니다.

그리고 **라이브 미검증 3건**(라운드 49 ①, 라운드 50, 라운드 48 번호검사)이 쌓여 있습니다.
다음 라운드보다 이 셋이 실제로 뜨는지 확인하는 게 먼저입니다 — 안 뜨면 되돌릴 것이 있다는 뜻이고,
그게 새 기능보다 값이 큽니다.
