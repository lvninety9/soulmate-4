# Feedback archive — every resolved row moved out of FEEDBACK_PENDING.md's Completed history

> Not auto-loaded, same role as `wiki/rule-archive-archive.md`/`wiki/handoffs/SESSION_MASTER-archive.md`
> — read on explicit request only. Rows #1-#18 (rounds 1-8) moved here in the PRUNE pass between
> rounds 27 and 28 after `FEEDBACK_PENDING.md` hit its 40/40 hard cap. Rows #19-#38 (rounds 9-27)
> moved here in round 28's S5 (external review, FEEDBACK_PENDING row #39): local-Qwen `/tokenize`
> measured 8,545 of 15,128 required-read tokens (56%) as this exact table, pure resolved narrative
> nobody needs to re-read every session — see AGENTS.md's Rule Zero grep-don't-read-whole
> discipline, which this file's own past size defeated in practice regardless of what the rule
> said. Every row below is either (a) fully re-documented with more detail in
> `wiki/rule-archive-archive.md` (rounds 1-4, L01-L09) or `wiki/rule-archive.md` (rounds 5-27, the
> "Round N" sections), or (b) a one-time architectural/process fix whose lasting effect is now just
> how the repo is built — visible in `AGENTS.md`'s current structure/Fixed Rules directly, not
> something a cold agent needs the historical justification for. No content deleted, only
> relocated — full detail for anything summarized here still exists at the cross-reference named
> in each row.

| # | Feedback / issue | How it was handled | Session resolved |
|---|---|---|---|
| 1 | `scripts/bootstrap.sh` had never been run end-to-end; Round 1 blind validation found it fails on a completely fresh checkout (AGENTS.md.template 1 line over its own cap after comment-stripping), leaks its `/tmp` scratch clone on that failure, and never copies `templates/harness-integration-test.md`/`cold-read-test-prompt.md` into the target | Trimmed the template under cap, decoupled the README one-liner's cleanup from bootstrap.sh's exit code, made bootstrap.sh copy the two verification templates and strip comments from all 4 templates consistently; re-ran the real one-liner end-to-end and confirmed `--bootstrap-check` passes. Full detail: `wiki/rule-archive-archive.md` L06 (state-persistence fix, same round) | round 1 |
| 7 | Gate's trigger was 100% elective (L07) — round 2 reproduced a full silent multi-sub-task chain with the gate never arming | `tool.execute.after` now checks real committed files via `git diff-tree`; N non-primer commits in a row arms anyway; verified via unit test + real 2-process Kilo run. Full detail: `wiki/rule-archive-archive.md` L07 | round 2 |
| 8 | Fresh bootstrap's AGENTS.md sat at 58/60 before any customization, 59/60 after the README's own instructed step (add a File Map row) — one line from a hard fail | Raised cap 60→65 (content already near-minimal); re-verified via a real bootstrap + realistic row add, now 59/65 with real headroom left | round 2 |
| 5 | `COMMITS_WITHOUT_PRIMER_THRESHOLD=4` untested against real multi-file work | Round 3: confirmed no false positive on 3 legitimate commits, correct primer-vs-elective distinction; separately found+fixed the detection itself (regex→real git HEAD diff, L08). Full detail: `wiki/rule-archive-archive.md` L07-L08 area | round 3 |
| — | AGENTS.md's Learned/Fixed Rules split into PROJECT_BACKGROUND.md re-created soulmate-3's own L02 risk (rule referenced but not stated in the auto-loaded file); check-caps.sh's Learned/Fixed checks were also silently still targeting AGENTS.md the whole time | Verified against the real original soulmate repo (not assumed): merged Rules back into AGENTS.md, cap 65→85 matching the original exactly; re-bootstrapped and caught+fixed one more off-by-one before push. This is why Rules live in `AGENTS.md` today — see that file directly, no further historical justification needed | session 4 (architecture realignment) |
| 9 | `wiki/protocols/refactor.md` had never been exercised by any blind validation round | Round 4: 3 independent blind trials (abstract framing, literal "refactor" word, cross-process) — self-serve never fired in any of them, backup-first/small-units/verify-loop all failed as a consequence (L09). Full detail: `wiki/rule-archive-archive.md` L09 | round 4 |
| 10 | Self-serve never fired for refactor.md (L09) — needed a mechanical fix, unit-tested and live re-verified | `subtask-gate.ts`: block a session's first mutating call unless a protocols/*.md doc was read this session (one-shot, same trade-off as the other 2 checks). 6/6 unit tests pass; live re-run of round 4 trial 1's exact prompt: blocked once, model then read refactor.md, made a backup branch, stated the rollback command, ran real pytest, committed per file — no regression. Full detail: `wiki/rule-archive-archive.md` L09 | round 4 |
| 11 | `refactor.md`/`build.md` never named a canonical verification command — round 4 trial 3 found the model trust a bare `python3 file.py` (no `__main__` block, silently ran 0 tests, exit 0) as "passed" | Both protocol docs now name the real invocation explicitly (`pytest <file>`/`npm test`/`npx tsc --noEmit`) and call out "0 exit + empty output is a red flag, not a pass" — this is now `AGENTS.md`'s own Fixed Rule ("Never claim a sub-task 'done' without running its build/typecheck command"), no further historical detail needed | session 5 |
| 14 | Objective blind audit (independent fresh agent, calibrated against the original `soulmate`'s own real rubric): soulmate-4 scored turnkey 74/100, structural 73/100 vs the original's 87/98.75 | Fixed all concrete findings same session: `AGENTS.md`/template zero-headroom bug (real repro: literal README step 1 broke the commit hook), `main`→`master` 404 in bootstrap.sh's own header, README file-tree staleness — all re-verified fixed (L09/L10). Full detail: `wiki/rule-archive.md` "Round 5" section | session 5 |
| 15 | Audit's #1-priority + a live-reproduced gap: L09's gate was a true one-shot for the whole session (any *different* mutation after the first block sailed through unchecked forever), and no mechanism at all caught a completed mutation that was simply never committed | `subtask-gate.ts` hardened: L09 now re-checks on *every* mutating call, not just the first, until real compliance. Added `chat.message` hook injecting a mechanical warning naming uncommitted leftovers on the next message. Found+fixed a real crash along the way (L10). 10/10 unit tests + 3 live re-verifications. Full detail: `wiki/rule-archive.md` "Round 5" section | session 5 |
| 3 | `subtask-gate.ts`'s one-shot disarm meant an immediate mutating call right after a block — verbatim retry or not — slipped through unconditionally, because `armed[sessionID]` was deleted the instant the first block fired | The clear moved out of `tool.execute.before` entirely, into `chat.message`, gated on a genuinely new user message arriving (L11). 4 new unit tests, live-verified via `.subtask-gate-state.json` inspection. Full detail: `wiki/rule-archive.md` "Round 8" section (Fix 1) | round 8 |
| 16 | Round 6 objective re-audit (independent fresh agent, re-cloned the original, re-verified all 5 round-5 fixes live): all 5 confirmed holding — turnkey 74→81 (+7), structural 73→77 (+4). Found `templates/AGENTS.md.template` had drifted *again*, same session, same bug class. Also: "10/10 unit tests" existed only as prose, no committed test file | Synced template fully; added `check_template_drift()` to check-caps.sh — diffs the Learned Rule ID set, `OVER CAP`s on mismatch. Committed `tests/subtask-gate.test.mjs` for real, wired into `bootstrap.sh`. Full detail: `wiki/rule-archive.md` "Round 6" section | round 6 |
| 17 | Round 7 objective re-audit: turnkey held at 81, structural **dropped** 77→69. Two causes: `check_template_drift()` was ID-token-only (content-blind); FEEDBACK #4 converted from "untested" to a confirmed live failure | Drift check rewritten to diff full content from `## Language` onward. `chat.message` ambiguity nudge added for #4 — found+fixed a real bug: `kilo run "<msg>"`'s literal quote-wrapping defeated the anchor check on every CLI message. Full detail: `wiki/rule-archive.md` "Round 7" section | round 7 |
| 18 | Round 8 objective re-audit (scored round 7's 3 fixes) — both axes dropped (turnkey 81→78, structural 77→74); FEEDBACK #4/#12's nudge confirmed firing but not changing behavior, plus a newly-found CLI-invisibility bug | Fixed #3 (see row above). #4/#12: reworded the nudge to remove its self-granted "ignore if..." escape hatch — 2/2 live passes with the new wording, up from 0/3 with the old. CLI-invisibility researched and confirmed structurally unfixable with the current plugin API. Full detail: `wiki/rule-archive.md` "Round 8" section | round 8 |
| 19 | Round 9 objective re-audit (scored round 8's fixes) — turnkey 78→~70, structural 74→~68 (different grader's own estimate, not a strict continuation of this repo's internal formula, so noise is expected); independently re-confirmed FEEDBACK #3 and #4/#12's round-8 fixes both hold under fresh adversarial live trials; found 3 new issues: (a) `SESSION_PRIMER.md`/`session-log.md`/`FEEDBACK_PENDING.md` mutually inconsistent — title said round 8 not started when it was done, session-log had no round-8 entry, this table's own row #18 mislabeled round 8's numbers as "round 7" (now fixed above); (b) `AGENTS.md` zero-headroom cap regression, 3rd recurrence; (c) L01 cited the wrong npm package path (`@opencode-ai/plugin` vs the actually-shipped `@kilocode/plugin`) | (a) doc self-consistency fixed this round — see this table's own edit + `SESSION_PRIMER.md` round table rows 8-9 + `session-log.md` entries 11-12. (b) `AGENTS.md` compressed 85→75 lines (Learned Rules L01-L03/L05-L11 merged, all IDs preserved), live-verified fresh bootstrap + fill project name + add File Map row now succeeds at 76/85 (was failing at 86/85). (c) L01 citation corrected to `@kilocode/plugin` at its real shipped path, substantive claim re-confirmed still correct | round 9 |
| 20 | Round 10 objective re-audit (scored round 9's 3 fixes) — turnkey ~70→76, structural ~68→77 (all held: fresh bootstrap+File-Map-row now lands at 76/85 with margin, retry-bypass still blocked live); found the *same* stale-narrative bug class recurring in 2 more places round 9's fix never checked: `README.md` (2 places — described the retry-bypass fix as unpatched, cited a non-existent AGENTS.md rule; described Rules as living in PROJECT_BACKGROUND.md, stale since session 4) and `.kilo/plugins/subtask-gate.ts:324` (same wrong `@opencode-ai/plugin` citation round 9 only fixed in the doc copy, not the source comment) | Fixed both `README.md` spots + the plugin source citation, then ran a repo-wide `grep -rn "@opencode-ai/plugin"` sweep (6 total hits — 2 now-fixed live-artifact hits, 4 correctly-historical mentions left as-is) plus a broader stale-language sweep across README.md/AGENTS.md/wiki/protocols/*.md — nothing else found stale at the time | round 10 |
| 21 | Round 11 objective re-audit (scored round 10's fixes) — turnkey 76→80, structural 77→78 (all held, incl. independently verifying `@kilocode/plugin` against the real installed binary); found a **4th instance of the same recurring bug**, this time in `scripts/check-caps.sh:409-411` itself (claimed the gate-vs-fresh-bootstrap combo "hasn't yet been independently verified" when it had been, twice — rounds 9 and 10) — diagnosed the actual root cause as *process*, not content: every sweep so far manually hand-picked which files to check, so each round's fix only closed the file class the previous audit happened to flag | Fixed the concrete `check-caps.sh:409-411` claim. Then built the architectural fix instead of another point-patch (mirrors the original soulmate's own round-11 lesson, L04: replace N hand-rolled scans with one mechanism that structurally can't miss a class): `check_stale_language()` in `check-caps.sh` — one mechanical `git grep` sweep over every tracked file (short pattern list + short historical-file exemption list), wired into the normal check-caps.sh run (and therefore the pre-commit hook) so no future round has to remember to re-grep by hand. Live-verified: fires on injected staleness in a live file, stays quiet on the identical text in an exempted historical file, and a real false positive (this file's own past-tense description of the patterns) was found and fixed by rewording, not by expanding the exemption list | round 11 (audit) → round 12 (fix) |
| 22 | Round 12 objective re-audit (scored round 11's fix) — turnkey 80 (held), structural 78→74 (**regressed**); the retry-bypass gate and unit tests (17/17) held under a 5th round of adversarial live testing, but round 12 found `check_stale_language()` — the round-11 mechanism built specifically to close the recurring "narrated as more capable than it is" bug — **had exactly that bug itself**: it printed WARN but never set `status=1`, so a real pre-commit attempt with a staged stale-claim injection landed anyway. Round 12 verified this by actually installing the hook and committing, not just reading the code. Also confirmed `FEEDBACK_PENDING.md` was exempt whole-file, not just its Completed-history section | `check_stale_language()` now sets `status=1` on a match (same convention as this script's other hard-FAIL checks), and `FEEDBACK_PENDING.md`'s exemption narrowed from whole-file to just its "## Completed history" section (clean heading boundary already used elsewhere) — its open table is swept like any other live-state file now. Live-verified before committing: baseline clean (0 exit); injected staleness into a never-swept file → blocks; injected into FEEDBACK_PENDING's open table (before the heading) → blocks; same text after the heading → stays clean; installed the real pre-commit hook and attempted a real commit with a stale injection staged → genuinely rejected this time (exit 1) | round 12 (audit) → round 13 (fix) |
| 23 | Round 13 re-audit independently reconfirmed round 12's exact gap now closed (turnkey 80, structural 74→80). Round 14 tried genuinely new adversarial angles instead of repeating prior tests — fresh-bootstrap survival passed, but found 2 real new bugs in `check_stale_language()`: (1) a stale phrase split across this repo's own hard-wrap house style evades the per-line grep (false negative, live-reproduced); (2) `session-log.md` was missing the `-archive` exemption sibling its 2 neighbors already had, so this repo's own routine archiving convention for it would wrongly self-block (false positive, live-reproduced). Structural 80→76 | Joined each run of non-blank lines into one string before matching (blank lines still end a paragraph, so genuinely separate claims can't falsely bridge) — closes the wrap-evasion gap without a markdown parser. Added `(-archive)?` to the `session-log` exemption, matching the existing pattern exactly. Live-verified: both round-14 repro cases now behave correctly, plus all of round 13's own regression checks (unswept-file block, FEEDBACK_PENDING open-table block/Completed-history pass) re-run clean | round 13 (audit) → round 14 (audit) → round 15 (fix) |
| 24 | Jay directly intervened after rounds 11-15's pattern (one new `check_stale_language()` edge case found per round — enforcement missing, then line-wrap evasion, then exemption gap): diagnosed the input space itself as unbounded and asked for a structural fix, not another point-patch, "다른 좋은 레퍼런스를 참고해도 좋다" | Two changes: (1) enumerated per-file exemption list replaced with a naming-pattern rule (3 base files by exact name + any `-archive.md` suffix) — kills the "forgot to add a sibling" bug class structurally, any future archive file needs no code change; (2) `tests/stale-language.fuzz.test.mjs` — 19 generated cases across wrap-position/casing/whitespace/punctuation/paragraph-boundary/exemption-pattern dimensions, front-loading edge-case discovery instead of waiting for one per audit round. Found 2 real bugs on first run: internal whitespace (multi-space/tabs) wasn't normalized before matching — fixed in the same commit; the fuzz test file itself tripped its own check on commit (quotes the phrases as literal fixtures) — fixed via self-exemption, same as `check-caps.sh` already had. Live-verified: all of rounds 13/14/15's regression cases still pass post-redesign | round 16 (fix) |
| 25 | Round 16 objective re-audit (scored round 16's fuzz-based redesign) — turnkey 80 (held), structural 76→83 (real gain, fuzz suite's 19 dimensions genuinely hardened). But a fresh adversarial pass tried different angles and found 2 new gaps: (1) `check_stale_language()`'s awk pass had no fenced-code-block/inline-code awareness, flagging example/quoted text as live claims; (2) the `*-archive.md` wildcard exemption silently swallowed `wiki/deploy-archive.md` (present-tense, non-historical content) purely by filename coincidence — the dangerous, silent-evasion direction. Also confirmed the fuzz suite itself isn't wired into `bootstrap.sh` (fresh projects don't inherit it) or the pre-commit hook — the same "narrated as more capable than it is" bug class, one level up, in the tool built to fix it | Fenced-code/inline-code content now skipped before matching. Exemption wildcard narrowed back to the 3 known PRUNE stems with `(-archive)?` each (not an unbounded suffix) — closes the collision, loses zero real coverage. `bootstrap.sh` now copies the fuzz suite into fresh projects; pre-commit hook deliberately left NOT invoking either `.mjs` suite, matching this repo's own pre-existing precedent for `subtask-gate.test.mjs` (manual run-before-trusting-a-fix, not commit-gated). Also fixed `FILE_MAP_ROW_CAP` 12→10 (unreconciled soulmate-3 leftover, real original value confirmed via fresh clone). 23/23 fuzz (4 new cases), 17/17 subtask-gate, full regression battery + 3 fresh manual live-commit tests all pass | round 16 (audit) → round 17 (fix) |
| 26 | Round 17 objective re-audit (scored round 17's 4 fixes) — turnkey 80→82, structural 83→86 (both up; dogfooded the repo's own real `rule-archive-archive.md` double-suffix file as a live test, held). But a targeted retest of the exact mechanism this round touched found 2 more valid-markdown forms slipping through within minutes: an inline `` `code span` `` split across this repo's own hard-wrap style, and 4-space indented code blocks (never recognized as code at all). Auditor's diagnosis: "a hand-rolled, non-CommonMark markdown recognizer keeps yielding one more slipped-through form each time someone looks from a new angle" — both false positives (over-flagging), not silent-evasion | Traced both to one root cause (processing order), not two special cases: exclude fence+indented-code lines first (never enter the paragraph buffer) → join what's left into paragraphs as before → THEN strip inline `` `...` `` spans from the joined text, so a wrap-split span is contiguous again and one strip catches it. Live-verified with the real pre-commit hook: both round-17 cases now pass; full regression battery (rounds 13-17) holds; 2 new fuzz cases added, suite now 25/25; `subtask-gate.test.mjs` 17/17 unaffected | round 17 (audit) → round 18 (fix) |
| 27 | Round 18 objective re-audit (scored round 18's reorder fix) — turnkey 82 (held), structural 86→83; the reorder genuinely generalized (auditor's own mixed wrap+dual-inline-code stress case held), but 2 more non-prose constructs neither round 16 nor 17 tried came back positive within minutes: HTML comments (`<!-- ... -->`) and YAML frontmatter (`---`-delimited), both real live-reproduced false positives via the real pre-commit hook. Auditor's warning: "unless the next fix generalizes the concept... expect round 19 to find another instance" (tables, `<details>` blocks, link-ref definitions named as likely next hits) — Jay decided against chasing every remaining CommonMark construct indefinitely and asked for one more structural generalization, then an honest stop | `check_stale_language()`'s awk pass redesigned around a small (open-marker, close-marker) table for *symmetric* delimited regions — fence and frontmatter now share one generic loop instead of separate bespoke code; a future symmetric construct is one table row (verified: a hypothetical 4th entry is a 2-line diff). HTML comments are asymmetric (open≠close, usually self-contained on one line with real prose alongside) so they get their own small same-line-strip-else-block-skip handling rather than being forced into the table — a genuine shape difference, same reasoning as why indented code stayed its own per-line check. Frontmatter is line-1-anchored (a bare `---` later in a file is a horizontal rule, not frontmatter). Live-verified via the real pre-commit hook: all 3 concrete gaps closed, full regression battery (rounds 13-18) holds, `subtask-gate.test.mjs` 17/17, fuzz suite 25→31/31 (6 new cases, incl. a same-line-prose-after-comment control and a footnote-should-still-flag control). **Explicit decision, not oversight**: tables, `<details>` blocks, link-reference definitions, and any other CommonMark construct not yet hit remain out of scope — any of those slipping through is a false positive (blocks a legitimate commit until reworded), the safe direction, never a silent miss; the fix if one is ever hit is reword the line or add one table row, not another audit-and-patch round. This is stated in `check_stale_language()`'s own code comment and here, not overstated as "fully solved" anywhere | round 18 (audit) → round 19 (fix) |
| 28 | Round 19 objective re-audit (scored round 19's generic table + HTML-comment/frontmatter fix) — turnkey 82 (held), structural 83→84. All 3 claimed-closed gaps genuinely closed (own test cases, not the fix's own fuzz fixtures: frontmatter, fence, bare-`---`-not-frontmatter all held). But found a real **silent miss** — the first false-negative (dangerous direction) found across 20 rounds on this mechanism, every prior finding was a false positive (safe direction): a multi-line HTML comment's closing line unconditionally discarded the whole line, silently dropping real prose trailing the `-->` on that same line (e.g. `still commented --> This feature is still unpatched.`) — directly contradicted the round-19 code comment's own "never a silent miss" claim. Auditor separately confirmed the honest-disclosure part is accurate: table cells and `<details>` blocks (named out-of-scope) do produce false positives exactly as documented, not a surprise | Fixed by mirroring the already-correct same-line-open handling: on the closing line of a multi-line comment, strip up through `-->` and fall through to keep scanning the remainder instead of unconditionally skipping the whole line via `next`. Softened the code comment's "never a silent miss" claim to an actively-defended design goal, not a formal guarantee, since the prior absolute wording was directly falsified by this bug. Live-verified with the real pre-commit hook: round 19's exact repro now blocks; a control case (comment content genuinely still inside the comment) stays clean; full regression battery (rounds 13-19, 17 live commit tests) all hold. 2 new fuzz cases, suite 31→33/33; `subtask-gate.test.mjs` 17/17 unaffected | round 19 (audit) → round 20 (fix) |
| 29 | Round 20 objective re-audit (scored round 20's closing-line fix) — turnkey 82 (held), structural 84→82. Round 20's exact reported case held, but a fresh adversarial composition (a closing line carrying a SECOND, trailing same-line comment after the true close) reproduced a **2nd genuine silent miss on the same mechanism**: `sub(/^.*-->/, "", $0)` is GREEDY (POSIX ERE `.*` matches maximally, awk has no non-greedy quantifier), so it consumed through the LAST `-->` on the line instead of the first, swallowing real prose in between. Auditor's diagnosis: the ceiling on this hand-rolled regex approach may be "low-to-mid 80s" without a structural fix, not another special case. Jay reviewed and gave two explicit instructions: (1) don't chase this with more regex patches — switch the whole HTML-comment-stripping mechanism from regex pattern-matching to `index()`-based direct position search (finds the FIRST occurrence by definition, making the greedy/non-greedy ambiguity class structurally impossible, not just less likely); (2) reaffirmed code minimalism as a standing project value, not optional polish | Replaced both the same-line-open and closing-line regex branches with one `strip_comments()` procedure driven entirely by `index()`/`substr()` — no `sub()`/`gsub()` for the actual position-finding. Live-verified with the real pre-commit hook: round 20's exact bug case now blocks; the full 22-case HTML-comment + rounds-13-19 regression battery holds; 3 extra adversarial compositions tried beyond the brief (3 comments on one line with prose in both gaps, an unterminated comment spanning to EOF, an unterminated comment closed 5 lines later with 2 more comments interleaved) all correct. Code comment rewritten to explain *why* index()-based pairing is structurally sounder (not just restate the same "not a formal guarantee" disclaimer). 2 new fuzz cases (33→35/35), `subtask-gate.test.mjs` 17/17 unaffected. One self-inflicted `git reset --hard` wiped the uncommitted fix mid-verification (same mistake rounds 13/16/18/20 each independently caught once) — caught immediately via re-inspection, reapplied, committed before any further destructive git ops this time | round 20 (audit) → round 21 (fix) |
| 30 | Round 21 objective re-audit (stress-tested round 21's `index()`-based rewrite specifically) — turnkey 82 (held), structural 82→80. The wrong-occurrence-matching bug class is genuinely closed: pushed to 60 comments on one line, hand-verified position math, several adversarial compositions — all correct, could not reproduce round 20's bug shape under heavy stress well beyond the repo's own 35-case suite. But found a **different, pre-existing (since round 19) dangerous-class silent miss**: an HTML comment opener that never finds its closer anywhere in the file leaves `incmt` stuck at 1 for the rest of the scan — every real claim after the mistake goes unswept, unbounded, unwarned, directly contradicting the mechanism's own documented "false-positive-only" design goal. Notably this exact case had been exercised in round 21's own self-testing ("unterminated-to-EOF... all correct") but not recognized as dangerous, since that test checked for crashes/mismatches, not whether downstream real content silently goes unswept | Added a parity check mirroring `check_fence_parity`'s odd-fence-count hard-FAIL. A naive raw opener/closer occurrence count was tried first and immediately produced real false positives on this repo's own handoff docs (both legitimately quote a standalone closer token inside backtick spans documenting this exact bug's regex, with zero real openers anywhere in either file) — switched to reusing `strip_comments()`'s own `incmt` state instead (only starts counting after it actually finds a real opener, the same contextual pairing the normal match path already uses correctly), checked at `END` and reported as a distinct `OVER CAP` message. Live-verified with the real pre-commit hook: a genuinely unclosed comment with real stale claims after it is now blocked (previously silently passed); the backtick-quoted-closer false-positive case and all properly-closed comments stay clean; full rounds-13-21 regression battery holds. 4 new fuzz cases (35→39/39), `subtask-gate.test.mjs` 17/17 unaffected. Code comment's safety-language updated to state the unclosed-to-EOF gap is specifically closed, while keeping the broader "not a formal guarantee" humility for any other undiscovered edge. One self-inflicted `git stash` (inside a test helper, not a deliberate op) stashed away the uncommitted fix mid-verification — the same mistake every round since 13 has independently hit once — caught via `git stash list` + diffing the stash content against the working tree, popped back cleanly, re-verified from a clean state before committing | round 21 (audit) → round 22 (fix) |
| 31 | Round 22 objective re-audit (independently reproduced round 22's parity fix — held) — turnkey 82 (held), structural 80→79. Following the fix's own suggested angle, tried plausible contributor prose using bare `<!--`/`-->` tokens with and without backtick-quoting: a **new false positive specific to round 22's own delta** — `strip_comments()` runs on raw per-line text, backtick-span stripping only happens later on the joined paragraph buffer, so a backtick-quoted example token like `` `<!--` `` (this repo's own established "quote it in backticks to be safe" convention) is misread as a real unclosed opener by the new parity hard-FAIL, wrongly blocking a legitimate commit. Also confirmed (code-read + hands-on) a separate, pre-existing gap: bare (non-backtick) tokens spanning a real paragraph break still silently pair, exempting real content in between — same shape as round 20's finding, just via a different path (blank-line handling, not greedy regex) | Reordered the pipeline: on a fresh (non-comment-continuation) line, fence/frontmatter region-open is checked on the RAW line first (gsub-ing backticks first can mangle a literal ` ``` ` fence delimiter — 2 of its 3 backticks match `` `[^`]*` ``), THEN same-line backtick spans are stripped, THEN comment detection runs — so a backtick-quoted token never reaches `strip_comments()` as raw text. Live-verified: backtick-quoted example token no longer blocks; round 17/18's wrap-split-code-span handling unregressed (verified via the exact existing fuzz case, not just re-read); full rounds-13-22 regression battery (14 live commit cases) holds. **Deliberately did NOT fix the bare cross-paragraph gap** — the obvious fix (force-close any suspected-unclosed comment at the next blank line) was implemented, tested, and reverted after it immediately broke real content: `templates/FEEDBACK_PENDING.md.template`/`SUBSYSTEM-learnings.md.template` both have a genuine multi-paragraph instructional HTML comment with a blank line inside it, and there is no reliable syntactic signal distinguishing that from an accidental bare-token pairing. Documented explicitly as an accepted, known gap in the code comment instead of silently leaving it or overclaiming a fix — backtick-quoting (now verified to work) is the real mitigation available to an author. 2 new fuzz cases (39→41/41), `subtask-gate.test.mjs` 17/17 unaffected. One self-inflicted `git reset --hard` wiped the uncommitted fix mid-task (the same mistake every round since 13 has independently hit once) — caught immediately, reapplied, re-verified from a clean state before committing | round 22 (audit) → round 23 (fix) |
| 32 | Round 23 objective re-audit (independently reproduced round 23's pipeline reorder — held) — turnkey 82 (held), structural 79→81. Round 23's backtick-before-comment reorder held under a fresh construction, and round 18's original wrap-split-inline-code case stayed unregressed. Found one more: a backtick span itself wrap-split across a line break, containing a comment-like token on its unclosed portion, silently swallows real content between it and a later unrelated closer — confirmed pre-existing on the prior script too (not a round-23 regression), just a previously-untested variant of the already-disclosed bare-cross-paragraph-token gap. The one actionable note: the shipped mitigation wording ("quote it in backticks to be safe") doesn't say the guarantee is same-line-only, so an author following the literal advice could still hit the gap if their editor hard-wraps the span | Wording-only fix, no logic change (deliberately scoped this way — not another architecture round): reworded the code comment, `FEEDBACK_PENDING.md`, and `SESSION_PRIMER.md` to state precisely that backtick-quoting protects same-line spans only, and that a wrap-split backtick span falls under the same accepted bare-cross-paragraph-token limitation. Added 1 fuzz case named `KNOWN GAP (accepted, not a bug to fix)` asserting the current, accepted (not-caught) behavior for this composition, so any future behavior change either direction is a deliberate, visible diff rather than an accidental regression or an unnoticed fix. Live-verified 4 spot-check cases through the real pre-commit hook (backtick-quoted same-line token still doesn't false-positive; never-swept file still blocks; round 22's unclosed-comment-parity case still blocks; round 18's wrap-split inline-code case with no comment token still doesn't block). Fuzz suite 41→42/42, `subtask-gate.test.mjs` 17/17 unaffected. Round 24's own re-audit independently re-verified the wording is accurate and reproduced the disclosed gap exactly as documented, tried 2 more compositions and found nothing new, and judged this specific sub-mechanism (comment/backtick handling) done for practical purposes — while flagging that turnkey (82, static since ~round 17-18) and the *other* check-caps.sh mechanisms (`check_fence_parity`, `check_template_drift`, bootstrap-placeholder/primer-handoff checks) haven't been targeted by a fresh adversarial pass in many rounds, unlike the original repo's own final convergence rounds which widened scope rather than narrowing it | round 23 (audit) → round 24 (fix) |
| 33 | Round 24 objective re-audit — turnkey 82 (held), structural stayed 81 (no logic touched round 24, wording-only). Independently reproduced the round-23→24 wording fix (backtick-quoting is same-line-only, wrap-split not protected) via own fresh construction, confirmed accurate. Tried 2 more genuinely new compositions on `check_stale_language()` specifically (unclosed-with-real-claims-then-fence-then-close, unclosed-with-no-real-closer-anywhere) — **found nothing new**, both behaved correctly (fails safe on the second case). Explicitly judged this sub-mechanism done for practical purposes and recommended widening scope: turnkey untouched since ~round 17-18, sibling mechanisms (`check_fence_parity`, `check_template_drift`, bootstrap-placeholder checks) untested in many rounds — mirrors the original repo's own final convergence rounds, which widened scope rather than narrowing it | No fix needed this round — round 24's audit was the clean, "nothing new" round that ends the `check_stale_language()`-focused stretch (rounds 11-24, 14 rounds). Directive for round 25 set: widen scope per the audit's own recommendation | round 24 (audit, clean) |
| 34 | Round 25 objective widened-scope audit (first dedicated turnkey pass since ~round 17-18, per row #33's directive) — turnkey 82→78 (real findings, not noise), structural held ~81 (light-touch spot-check only, as instructed). Found 4 concrete turnkey gaps via a literal fresh-adopter walkthrough + real `kilo run` against a genuinely fresh bootstrap (not the seed's own copy): (1) `templates/SUBSYSTEM-learnings.md.template` points at an "A-D self-diagnosis pattern"/bookstore-pricing README section that only exists in the *original* repo — a dead cross-reference left over from the port, the exact L03 failure mode; (2) **root-caused the AGENTS.md-headroom bug that had recurred 3+ times across this whole thread**: `templates/AGENTS.md.template` ships pre-populated with soulmate-4's own harness-development history (e.g. the exact Kilo Part-ID prefix requirement) instead of blank like the original's template — a fresh bootstrap + one File Map row already lands at 78/85 before an adopter writes anything of their own; (3) README's file-tree diagram omits `tests/` even though every bootstrap copies it; (4) `check_stale_language()`/the 42-case fuzz suite (the single most heavily-engineered mechanism in the repo) has zero onboarding-visible documentation. Also spot-checked 4 other `check-caps.sh` mechanisms live (fence parity, template drift, bootstrap-placeholder checks, primer-handoff reminder) — 3 clean, but found `check_fence_parity()`'s odd/even-count blind spot (two independently-unclosed fences go undetected) is real; a controlled comparison against a fresh clone of the *original* repo confirmed this is **inherited**, not a soulmate-4 regression | Fixed 3 of 4 findings same session (round 26): (1) ported a real "A-D self-diagnosis pattern" section into soulmate-4's own README.md, closing the dead pointer; (2) compressed `AGENTS.md`'s Learned Rules (dropped implementation trivia, kept `rule-archive.md` pointers for evidence), re-synced the template — live-verified fresh-bootstrap-+-one-row headroom 78/85→73/85, a **partial** fix since a full blank-template rewrite would break `check_template_drift()`'s current byte-identical-content design (a real, disclosed tradeoff, not an oversight); (3) added `tests/` + a stale-sweep explanation to README's file tree. `check_fence_parity()`'s shared blind spot and `bootstrap.sh`'s missing README-creation logged, not fixed (low urgency/severity, out of this round's scope) | round 25 (audit) → round 26 (fix) |
| 35 | Round 26 objective re-audit (independently reconfirmed round 26's 3 fixes hands-on) — turnkey 78→82, structural 80→81. All 3 verified real: A-D cross-reference resolves to a genuine README section; 73/85 headroom number reproduced exactly via the literal recipe; `tests/` + fuzz-suite mention confirmed present post-bootstrap. Did one honest adversarial spot-check on `pre-commit-check-caps` (staged a real 93-line AGENTS.md past the 85 hard cap, real commit through the installed hook) — **genuinely rejected, found nothing new**, reported as a clean round rather than forcing a finding. Noted `kilo` CLI is not installed on this machine as of this round — degraded the round's own gate-behavior check to unit-tests-only (T9b), a real environment gap for future rounds to fix first. Overall 26-round assessment: turnkey has plateaued at 82 since ~round 17 minus this round's dip-and-recovery; structural has oscillated without ever reaching the original's "zero new findings" convergence bar. Highest-leverage next step: `check_fence_parity()`/`check_template_drift()`/bootstrap-placeholder checks, untested in many rounds | Jay called an explicit consolidation checkpoint at this score (turnkey 82/structural 81) — this round's result is the final state recorded in this handoff. Not a claim of convergence; SESSION_PRIMER.md's "Recommended next step" carries this forward for whichever session resumes the loop | round 26 (audit, checkpoint) |
| 36 | Jay shared an external reference (`https://github.com/multica-ai/andrej-karpathy-skills`) asking whether it had ideas applicable to raising soulmate-4's scores further | Reviewed in full (README, CLAUDE.md, SKILL.md, EXAMPLES.md, Cursor rule) — it republishes the same 4 Karpathy principles (Think First / Simplicity First / Surgical Changes / Goal-Driven Execution) four different ways, verbatim identical in substance to what soulmate-4 already implements via Hermes's own CLAUDE.md and has been actively applying for all 26 rounds of this thread. No session-handoff, memory, wiki, or audit-loop methodology content anywhere in it. **Concrete finding: nothing to adapt** — closed, not an open question | round 26 (reference check) |
| 37 | Round 27: a round-8-style audit accidentally run against the frozen round-7 clone (not the real round 8-26 history) surfaced a candidate finding that directly contradicted an existing ceiling claim — `chat.message`'s own code comment (round 5, re-cited but never re-verified against the real package after round 9 fixed its wrong-name citation) stated opencode's plugin API "has no end-of-turn/end-of-session hook at all." Re-checked against the actually-installed `@kilocode/plugin` v7.4.20 (not assumed) — **false**: `Hooks.event` exposes a real hook whose `Event` union includes `session.idle` (fires exactly once per completed turn, confirmed via a raw SSE capture against a real `kilo serve`), and `KiloClient.session.prompt()`'s body genuinely accepts `noReply: boolean` (confirmed in the installed SDK's own `SessionPromptData` type) | Added the `event` hook: on `session.idle` with a dirty working tree, appends a synthetic nudge via `client.session.prompt({noReply:true})` naming the real uncommitted files, deduped per session on the exact dirty-file-set signature (re-fires if the dirty set changes, not on a repeat idle for the same unresolved state). Closes a real gap `chat.message`'s own carryover check can't: a turn that ends with uncommitted work and *no next message in the session at all* — L11/row #15's honest "can't catch a session abandoned outright" limitation, now partially closed. Live-verified end-to-end via `kilo serve` + raw HTTP (a session that edited-and-stopped without committing got exactly one idle-nudge naming the real dirty files; a follow-up real message in the same session correctly quoted those file paths back with no fabrication; a repeat `session.idle` for the same still-dirty state produced zero duplicate nudges). 24/24 unit tests (7 new — one, T11b, initially gave a false pass from an unrelated gate masking the real assertion, caught and fixed while verifying, same "the fix needs its own bugs checked" discipline as every prior round). Corrected the now-false code comment. Committed through the real installed pre-commit hook | round 27 |
| 38 | Same round-27 audit pass's second candidate: wrapping `BLOCK_MESSAGE_UNCOMMITTED_CARRYOVER`'s text in `<system-reminder>` tags, claimed by the stale-clone audit to fix unreliable model attention to the carryover warning (reported 2/2 fabrication/denial → 0/2 after the wrap) | Live-reproduced the underlying failure mode against **current** round-26 code first (real, not stale-clone-specific: confirmed via `kilo export` that the real warning genuinely lands in context, yet the model still denied any warning was delivered, or fabricated an entirely fictional unrelated one, in 2/2 trials with one prompt phrasing). Applied the identical tag-wrap fix and re-tested rigorously — 7 live trials total across 3 phrasings, pre- and post-fix. **Directly contradicts the stale-clone's claim**: post-fix trials still failed 4/5 (2 "quick check"-style, 2 neutral-phrasing, 1 partial success using the one phrasing that had also succeeded pre-fix) — model self-report reliability tracked prompt phrasing far more than injected-text delivery format in this data. Extends row #6's existing "inherent LLM self-report unreliability, no plugin-level fix possible" ceiling to this specific mitigation hypothesis explicitly (tried, not just untested) — reverted the tag-wrap code change (no demonstrated benefit, unjustified added complexity per this repo's own minimalism standard) | round 27 |
| 39 | External review (not a fresh audit) of round 28's handoff-rigor primer, before its own planned fresh audit ran — found 7 concrete, already-broken mechanisms via adversarial code reading rather than the score loop: (S1) `SESSION_PRIMER.md`'s "Current sub-task" block is referenced/required by 5 files (`subtask-gate.ts:167`, `design.md:25`, `build.md:34`, `check-caps.sh:321`, its own template) but doesn't exist in the real file; (S2) `check-caps.sh:238`'s bootstrap canary string was deleted from wiki content in `dba3a3e`, so `--bootstrap-check` has silently always passed for 23 rounds; (S3) PRUNE's exempt-file regex (`check-caps.sh:486`) omits `wiki/feedback-archive.md` (round 27's own archive), so the next PRUNE would self-block; (S4) all 9 doc caps are line-based with zero char-based backstop — `FEEDBACK_PENDING.md` is 48 lines/37,747 chars (one row 2,669 chars) and reads as "ok"; (S5) hot/cold split — 8,545 of 15,128 required-read tokens (56%, local-Qwen `/tokenize` measured) are this file's "Completed history", pure narrative, not actionable state; (S6) `check_template_drift()` forces the template byte-identical to `AGENTS.md` past `## Language`, so S5's cleanup would break it unless redesigned first; (S7) `templates/harness-integration-test.md` is already a 9-step machine-checkable benchmark never once run in 28 rounds | All 7 landed, one commit (or small cluster) each against a fresh clone with the real pre-commit hook: **S1** restored the "Current sub-task" block + a hard-FAIL check-caps.sh guard (`b3e2ed2`). **S2** fixed the dead canary — a fresh-bootstrap-with-unadapted-wiki reproduction now correctly FAILs (`9b5fb13`). **S3** renamed the archive to match the PRUNE convention (`${file%.md}-archive.md`) + added it as a 4th enumerated exempt stem, deliberately not re-widening to the wildcard round 16-17 had already closed (`d9acc26`/`7e458ee`/`1aca7c1`). **S4** added `check_chars()` as a companion to every line cap + extended `check_watch_size()` to `.kilo/plugins/subtask-gate.ts`/`scripts/check-caps.sh`; live-caught FEEDBACK_PENDING.md's real 38,931-char bloat before S5 fixed it (part of `ebbac3a`). **S5** physically split FEEDBACK_PENDING.md's Completed history into `wiki/FEEDBACK_PENDING-archive.md` (`c78ff32`), then stripped AGENTS.md/its template of soulmate-4's own audit-loop history and now-dangling rule-ID citations (`ef507b2`) — measured required-read tokens 15,128→6,978 via the real local llama.cpp `/tokenize` endpoint, under row #39's own 8,000 target. **S6** narrowed `check_template_drift()` to exclude only "## Learned Rules" (project-specific by definition), keeping "## Fixed Rules" and every mechanism section in the diff — unblocked S5's template cleanup without losing real drift coverage (part of `ebbac3a`). **S7** scripted `templates/harness-integration-test.md`'s Steps 1-6 into `scripts/harness-integration-test.sh`, k/N scored (`2996ea6`) — took 3 real bugs to get the *script* right (no per-call timeout; Step 2's grading assumed grep-not-whole-read is always correct when a fresh bootstrap's FEEDBACK_PENDING.md is genuinely under Rule Zero's own threshold; Steps 3→4 were missing the human's scripted answer to discuss's questions), each caught via live smoke-testing against real kilo + local Qwen, none of them harness bugs. That same smoke test surfaced one real new harness gap while dogfooding S7 — logged separately as row #40, not fixed here (out of round 28's stated scope) | round 28 |
| 41 | Live 5-trial run of S7's benchmark found `chat.message`'s clear fired on ANY next message even when no block had ever actually surfaced -- a primer commit landed, but design.md's own "commit-then-stop" meant no mutating call (and thus no block) happened before "continue" disarmed the gate for free -- 0/5 | Redesigned: the boundary is now derived fresh from git on every mutating call (`computeBoundary()`), no persisted `armed` flag to go stale; it only clears via a genuine block-then-new-message, or a session-start pre-approval of whatever boundary already existed (protects build.md's fresh-session workflow). 26/26 unit (2 new: one reproduces this exact bypass, one checks the fresh-session exemption doesn't overcorrect), 42/42 fuzz. **Live re-verified with the harness forcing the premise** (a real primer-touching commit made directly by the test script, not left to chance -- the original 5-trial run only got a genuine primer commit in 1/5 tries): 5/5 fresh `kilo run` + local Qwen trials correctly blocked, including surviving repeated retries in the same turn (write->bash->bash all blocked) exactly matching the new unit tests' coverage. `.kilo/plugins/subtask-gate.ts` `ab64ecf`, tests+primer `007ab5d`, live re-run script kept at `/home/jay/sm4-tap-capture/item3-live-n5.sh` (not committed) | round 28 |
| 40 | S7 smoke test found `build.md` step 3's primer-touch rule could be satisfied in words (`git add <file> wiki/handoffs/SESSION_PRIMER.md`) without a real diff -- the gate never armed at that boundary, so "continue" sailed through unstopped | Round 28 item 5: measured directly (no live model needed) that the existing elective-arm mechanism already structurally can't be fooled by this -- it reads real git history (`git log -- SESSION_PRIMER.md`), not a commit's own claimed `git add` arguments -- and arms correctly after exactly `COMMITS_WITHOUT_PRIMER_THRESHOLD=4` non-touching commits. Decided NOT to lower the threshold: it would trip on entirely legitimate in-progress multi-file sub-tasks (build.md's own per-file-commit norm) more often than this narrow "fake claim inside a 1-2-commit sub-task" pattern occurs. Closed as a documented, accepted, reversible gap -- no code change. Full measurement + reasoning: `wiki/rule-archive.md` "Round 28" | round 28 |
| 44 | Q3 vs Q4 quantization decision for the local model backend -- which one to run in production | Extended beyond the plan's VRAM-only gate at Jay's request: 6 prompts (4 short, 2 long-generation -- a full Tic-Tac-Toe game and a portfolio HTML page, both live-executed/validated, not eyeballed) run against both quantizations. Q4 VRAM came in LOWER than Q3 (3,416 vs ~3,604-3,718 MiB, contradicting the plan's own pre-swap estimate), RAM +3.3GB (already acceptable per the daily restart), generation speed identical, prompt processing ~20% slower. Zero correctness gap across all 6 tasks; Q4 showed a mild, consistent polish edge (3/6 tasks) but never a substantive quality difference. Weighted decision (quality 40/VRAM 25/speed 20/RAM 15): Q3 ~7.7, Q4 ~8.0. **Q4_K_M adopted** -- `llama.env` LLAMA_MODEL_PATH switched, `systemctl restart llama` applied and verified, only that one line touched. Full data: `wiki/rule-archive.md` "Round 28" | round 28 |
| 45 | Round 29 work order item 1: subtask-gate's three git helpers (currentHead/lastPrimerTouchSha/commitCountSince) each independently caught a git failure into null/0, indistinguishable from "no boundary" -- a corrupt repo, mid-rebase, or permissions error silently disarmed the gate with no log | All three now route through one gitExec() that throws GitCommandError on real failure; a new isInsideWorkTree() is the only place allowed to say "not a repo" (pass silently). computeBoundary() returns a third outcome (GitFailure) distinct from null; both call sites (tool.execute.before's block, chat.message's pre-approval bookkeeping) fail CLOSED on it with the specific failing command named, instead of treating it as "no boundary." 3 new tests against REAL git failures, not mocks: not-a-repo (pass), unborn-HEAD repo (rev-parse HEAD fails -> blocked), invalid rev-list range via a new __internal test export (commitCountSince isolated -- object corruption always breaks lastPrimerTouchSha's `git log` first since rev-list's object needs are a strict subset of log's, confirmed live, so a bogus-but-well-formed SHA was needed instead). 29/29 unit, 42/42 fuzz, live-verified via a real bootstrapped `kilo run` multi-file task under the real Bun runtime (3 correct per-file commits + fresh-session pre-approval observed, not just Node's unit tests) | round 29 |
| 46 | Round 29 axis-C (complexity-ladder) live trial: once the elective gate armed mid-turn, the model read subtask-gate.ts's own source via the (correctly unblocked) `read` tool, noticed `background_process` was missing from MUTATING_TOOLS, and used it to write files + `git commit`, bypassing the gate completely -- discovered in the same round item 1 (#45) hardened this exact gate against a different fail-open vector | `background_process` added to MUTATING_TOOLS, treated as fully mutating regardless of the specific command run, same conservative treatment as `bash`. New unit test (T17) asserts it's now blocked by an armed gate with the exact block message. 30/30 unit, 42/42 fuzz. Not separately live-re-verified with a fresh model trial reproducing the exact discovery -- the fix is a one-line addition to an already-proven-working Set (the same trial's log shows write/bash correctly blocked under the real runtime), flagged honestly rather than over-claimed | round 29 |
| 47 | Round 29 axis-C found: once the elective gate blocked a mid-turn mutation, the model retried 4-20 tool variants instead of asking or backing off -- no escape hatch, since an armed gate blocks `bash` too (even read-only diagnostic commands) | Root-caused as a symptom of the elective arm judging mid-turn instead of at turn boundaries (round 30 item 3) -- fixed there: `electiveBoundaryAtTurnStart` snapshot, `tool.execute.before` only blocks a boundary matching that snapshot, a freshly-crossed one defers to the next `chat.message`. T19a/T19b unit-simulate the exact mid-turn-crossing pattern (real git commits, real hook calls) and confirm it now defers correctly; primer-boundary blocking unchanged (regression-safe). Live reproduction of an actual multi-retry storm not attempted in round 30 or this closing pass (kilo-run reliability blocked round 30; judged out of scope for a closing pass, not a new audit round) -- flagged honestly, same standard as row #46 | rounds 29,30 (closed: round 30 closing pass) |
| 48 | Round 29/30: axis B's Step 4 (design writes+commits the SESSION_PRIMER.md sub-task block) scored 0/5 in BOTH harness ON and OFF modes -- meant the bench never once exercised the primer gate's own arming trigger, so Step 5's numbers had nothing real to measure either. Root cause undetermined through round 30 (blocked by kilo-run's live-trial reliability that round) | Solved statically, no live run needed: mined `~/.local/share/kilo/kilo.db` for 4 real historical axis-B trial transcripts (both harness modes, 2 scenarios) instead of waiting on a fresh live run. Every trial shows the same sequence -- Step 3's `build_scope` answer is specified fully enough (exact files/language/responsibilities) that the model correctly treats it as "clearly-scoped" per AGENTS.md's own routing rule and finishes the entire sub-task via build before Step 4's "design" message is ever sent; the model then correctly declines to force a retroactive design pass on already-finished work. Root cause = (c) the bench's own Step 3 premise pre-empts Step 4's trigger, NOT (a) the model failing to self-serve design.md (it reads design.md every trial) NOR (b) design.md's instructions being unclear (steps 4-5 are unambiguous). No code fix to design.md/build.md (nothing wrong in either); documented as a comment at harness-integration-test.sh's Step 4 block + full evidence in `wiki/rule-archive.md` "Round 30 closing pass". Fixing the bench itself (under-specifying build_scope so design must decide the split) is a real bench redesign, deliberately not attempted (out of scope for a closing pass) | rounds 29,30 (closed: round 30 closing pass) |
