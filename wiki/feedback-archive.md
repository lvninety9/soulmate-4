# Feedback archive — rows moved out of FEEDBACK_PENDING.md's Completed history

> Not auto-loaded, same role as `wiki/rule-archive-archive.md`/`wiki/handoffs/SESSION_MASTER-archive.md`
> — read on explicit request only. Moved here (PRUNE pass between rounds 27 and 28, not itself a
> numbered audit/fix round — Jay-requested prune of
> `FEEDBACK_PENDING.md`'s Completed-history section after it hit its 40/40 hard cap) because every
> row below is either (a) fully re-documented with more detail in `wiki/rule-archive-archive.md`
> (rounds 1-4, L01-L09) or `wiki/rule-archive.md` (rounds 5-8, the "Round N" sections), or (b) a
> one-time architectural/process fix whose lasting effect is now just how the repo is built —
> visible in `AGENTS.md`'s current structure/Fixed Rules directly, not something a cold agent needs
> the historical justification for. No content deleted, only relocated — full detail for anything
> summarized here still exists at the cross-reference named in each row.

| # | Feedback / issue | How it was handled | Session resolved |
|---|---|---|---|
| 1 | `scripts/bootstrap.sh` had never been run end-to-end; Round 1 blind validation found it fails on a completely fresh checkout (AGENTS.md.template 1 line over its own cap after comment-stripping), leaks its `/tmp` scratch clone on that failure, and never copies `templates/harness-integration-test.md`/`cold-read-test-prompt.md` into the target | Trimmed the template under cap, decoupled the README one-liner's cleanup from bootstrap.sh's exit code, made bootstrap.sh copy the two verification templates and strip comments from all 4 templates consistently; re-ran the real one-liner end-to-end and confirmed `--bootstrap-check` passes. Full detail: `wiki/rule-archive-archive.md` L06 (state-persistence fix, same round) | round 1 |
| 7 | Gate's trigger was 100% elective (L07) — round 2 reproduced a full silent multi-sub-task chain with the gate never arming | `tool.execute.after` now checks real committed files via `git diff-tree`; N non-primer commits in a row arms anyway; verified via unit test + real 2-process Kilo run. Full detail: `wiki/rule-archive-archive.md` L07 | round 2 |
| 8 | Fresh bootstrap's AGENTS.md sat at 58/60 before any customization, 59/60 after the README's own instructed step (add a File Map row) — one line from a hard fail | Raised cap 60→65 (content already near-minimal); re-verified via a real bootstrap + realistic row add, now 59/65 with real headroom left | round 2 |
| 5 | `COMMITS_WITHOUT_PRIMER_THRESHOLD=4` untested against real multi-file work | Round 3: confirmed no false positive on 3 legitimate commits, correct primer-vs-elective distinction; separately found+fixed the detection itself (regex→real git HEAD diff, L08). Full detail: `wiki/rule-archive-archive.md` L07-L08 area | round 3 |
| — | AGENTS.md's Learned/Fixed Rules split into PROJECT_BACKGROUND.md re-created soulmate-3's own L02 risk (rule referenced but not stated in the auto-loaded file); check-caps.sh's Learned/Fixed checks were also silently still targeting AGENTS.md the whole time | Verified against the real original soulmate repo (not assumed): merged Rules back into AGENTS.md, cap 65→85 matching the original exactly; re-bootstrapped and caught+fixed one more off-by-one before push. This is why Rules live in `AGENTS.md` today — see that file directly, no further historical justification needed | session 4 (architecture realignment) |
| 9 | `wiki/protocols/refactor.md` had never been exercised by any blind validation round | Round 4: 3 independent blind trials (abstract framing, literal "refactor" word, cross-process) — self-serve never fired in any of them, backup-first/small-units/verify-loop all failed as a consequence (L09). Full detail: `wiki/rule-archive-archive.md` L09 | round 4 |
| 10 | Self-serve never fired for refactor.md (L09) — needed a mechanical fix, unit-tested and live re-verified | `subtask-gate.ts`: block a session's first mutating call unless a protocols/*.md doc was read this session (one-shot, same trade-off as the other 2 checks). 6/6 unit tests pass; live re-run of round 4 trial 1's exact prompt: blocked once, model then read refactor.md, made a backup branch, stated the rollback command, ran real pytest, committed per file — no regression. Full detail: `wiki/rule-archive-archive.md` L09 | round 4 |
| 11 | `refactor.md`/`build.md` never named a canonical verification command — round 4 trial 3 found the model trust a bare `python3 file.py` (no `__main__` block, silently ran 0 tests, exit 0) as "passed" | Both protocol docs now name the real invocation explicitly (`pytest <file>`/`npm test`/`npx tsc --noEmit`) and call out "0 exit + empty output is a red flag, not a pass" — this is now `AGENTS.md`'s own Fixed Rule ("Never claim a sub-task 'done' without running its build/typecheck command"), no further historical detail needed | session 5 |
| 14 | Objective blind audit (independent fresh agent, calibrated against the original `soulmate`'s own real rubric): soulmate-4 scored turnkey 74/100, structural 73/100 vs the original's 87/98.75 | Fixed all concrete findings same session: `AGENTS.md`/template zero-headroom bug (real repro: literal README step 1 broke the commit hook), `main`→`master` 404 in bootstrap.sh's own header, README file-tree staleness — all re-verified fixed (L09/L10). Full detail: `wiki/rule-archive.md` "Round 5" section | session 5 |
| 15 | Audit's #1-priority + a live-reproduced gap: L09's gate was a true one-shot for the whole session (any *different* mutation after the first block sailed through unchecked forever), and no mechanism at all caught a completed mutation that was simply never committed | `subtask-gate.ts` hardened: L09 now re-checks on *every* mutating call, not just the first, until real compliance. Added `chat.message` hook injecting a mechanical warning naming uncommitted leftovers on the next message. Found+fixed a real crash along the way (L10). 10/10 unit tests + 3 live re-verifications. Full detail: `wiki/rule-archive.md` "Round 5" section | session 5 |
| 3 | `subtask-gate.ts`'s one-shot disarm meant an immediate mutating call right after a block — verbatim retry or not — slipped through unconditionally, because `armed[sessionID]` was deleted the instant the first block fired | The clear moved out of `tool.execute.before` entirely, into `chat.message`, gated on a genuinely new user message arriving (L11). 4 new unit tests, live-verified via `.subtask-gate-state.json` inspection. Full detail: `wiki/rule-archive.md` "Round 8" section (Fix 1) | round 8 |
| 16 | Round 6 objective re-audit (independent fresh agent, re-cloned the original, re-verified all 5 round-5 fixes live): all 5 confirmed holding — turnkey 74→81 (+7), structural 73→77 (+4). Found `templates/AGENTS.md.template` had drifted *again*, same session, same bug class. Also: "10/10 unit tests" existed only as prose, no committed test file | Synced template fully; added `check_template_drift()` to check-caps.sh — diffs the Learned Rule ID set, `OVER CAP`s on mismatch. Committed `tests/subtask-gate.test.mjs` for real, wired into `bootstrap.sh`. Full detail: `wiki/rule-archive.md` "Round 6" section | round 6 |
| 17 | Round 7 objective re-audit: turnkey held at 81, structural **dropped** 77→69. Two causes: `check_template_drift()` was ID-token-only (content-blind); FEEDBACK #4 converted from "untested" to a confirmed live failure | Drift check rewritten to diff full content from `## Language` onward. `chat.message` ambiguity nudge added for #4 — found+fixed a real bug: `kilo run "<msg>"`'s literal quote-wrapping defeated the anchor check on every CLI message. Full detail: `wiki/rule-archive.md` "Round 7" section | round 7 |
| 18 | Round 8 objective re-audit (scored round 7's 3 fixes) — both axes dropped (turnkey 81→78, structural 77→74); FEEDBACK #4/#12's nudge confirmed firing but not changing behavior, plus a newly-found CLI-invisibility bug | Fixed #3 (see row above). #4/#12: reworded the nudge to remove its self-granted "ignore if..." escape hatch — 2/2 live passes with the new wording, up from 0/3 with the old. CLI-invisibility researched and confirmed structurally unfixable with the current plugin API. Full detail: `wiki/rule-archive.md` "Round 8" section | round 8 |
