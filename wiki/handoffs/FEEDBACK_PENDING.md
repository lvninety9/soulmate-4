# Feedback / Issue Tracking

Valid `status` values: `open` (not started) | `in-progress` | `blocked` (waiting on something
external — note what) | `done`.

Valid `priority` values: `p0` (blocking/urgent) | `p1` (normal) | `p2` (someday) — lets a
20+-row table stay triageable at a glance instead of every row reading as equally urgent.

| # | Feedback / issue | Priority | Status | How it's handled | Session logged |
|---|---|---|---|---|---|
| 2 | Custom project slash commands (`.kilo/commands/*.md`) don't work in Kilo's CLI as of v7.4.20 — may change in a future Kilo release, which would make `wiki/protocols/*.md`'s self-serve framing obsolete in a good way | p2 | open | Documented in AGENTS.md "Known gap" and L02; watch Kilo's changelog periodically | seed |
| 4 | "discuss" self-serve failure — now 3/3 confirmed live (round 7 + round 8's own re-repro before fixing): an ambiguous ask went straight to build/refactor-shaped action with zero clarifying questions, once even editing `subtask-gate.ts` itself off a self-generated interpretation | p1 | open (stronger partial mitigation, round 8) | Round 8: nudge reworded from a soft suggestion with an explicit "ignore if..." opt-out to a direct imperative with none — 2/2 fresh live trials passed after the change (model asked clarifying questions instead of acting), up from 0/3 with the old wording. Still a nudge, not a block (discuss.md produces zero tool calls) and separately confirmed unfixable-with-current-API: `chat.message`'s only output is the *incoming user message's own parts* (`@kilocode/plugin` type defs, corrected round 9 — was miscited as `@opencode-ai/plugin`), never rendered in `kilo run`'s normal terminal output — invisible to a human watching live, only visible via `kilo export`. This CLI-invisibility half stays open regardless of wording, independently reconfirmed round 9 | round 2 → confirmed round 7 → 3rd repro + stronger fix round 8 → reconfirmed round 9 |
| 6 | After a real gate block, the model's self-report was observed fabricating a "done, file written" claim — round 2, round 5's own regression test, and again round 8 (claimed "write succeeded on the first attempt" when the tool trace showed only one failed attempt, no success) | p1 | open | Inherent LLM self-report unreliability, same shape as soulmate-3's own L06 — no plugin-level fix possible; always verify actual file/git state (or, as of round 8, the persisted `.subtask-gate-state.json` directly), never the model's summary, after any gate block | round 2, reconfirmed round 8 |
| 12 | `discuss.md` has no mechanical backstop — the one protocol step producing zero tool calls, `subtask-gate.ts` structurally can't hook into it the way L09 does for mutations | p2 | open (see #4 — same fix, same remaining ceiling) | `chat.message` can inject text but can't block or force Q&A, and (round 8 finding) isn't even visibly rendered to a human watching `kilo run` — confirmed against the real shipped plugin API type defs, not assumed; this is the actual ceiling of what's possible without opencode adding a real pre-response/notify hook | session 5, mitigated round 7, re-confirmed + strengthened round 8 |
---

## Completed history

<!-- rows move here the moment they're done -->

| # | Feedback / issue | How it was handled | Session resolved |
|---|---|---|---|
| 1 | `scripts/bootstrap.sh` had never been run end-to-end; Round 1 blind validation found it fails on a completely fresh checkout (AGENTS.md.template 1 line over its own cap after comment-stripping), leaks its `/tmp` scratch clone on that failure, and never copies `templates/harness-integration-test.md`/`cold-read-test-prompt.md` into the target | Trimmed the template under cap, decoupled the README one-liner's cleanup from bootstrap.sh's exit code, made bootstrap.sh copy the two verification templates and strip comments from all 4 templates consistently; re-ran the real one-liner end-to-end and confirmed `--bootstrap-check` passes | round 1 |
| 7 | Gate's trigger was 100% elective (L07) — round 2 reproduced a full silent multi-sub-task chain with the gate never arming | `tool.execute.after` now checks real committed files via `git diff-tree`; N non-primer commits in a row arms anyway; verified via unit test + real 2-process Kilo run | round 2 |
| 8 | Fresh bootstrap's AGENTS.md sat at 58/60 before any customization, 59/60 after the README's own instructed step (add a File Map row) — one line from a hard fail | Raised cap 60→65 (content already near-minimal); re-verified via a real bootstrap + realistic row add, now 59/65 with real headroom left | round 2 |
| 5 | `COMMITS_WITHOUT_PRIMER_THRESHOLD=4` untested against real multi-file work | Round 3: confirmed no false positive on 3 legitimate commits, correct primer-vs-elective distinction; separately found+fixed the detection itself (regex→real git HEAD diff, L08) | round 3 |
| — | AGENTS.md's Learned/Fixed Rules split into PROJECT_BACKGROUND.md re-created soulmate-3's own L02 risk (rule referenced but not stated in the auto-loaded file); check-caps.sh's Learned/Fixed checks were also silently still targeting AGENTS.md the whole time | Verified against the real original soulmate repo (not assumed): merged Rules back into AGENTS.md, cap 65→85 matching the original exactly; re-bootstrapped and caught+fixed one more off-by-one before push | session 4 (architecture realignment) |
| 9 | `wiki/protocols/refactor.md` had never been exercised by any blind validation round | Round 4: 3 independent blind trials (abstract framing, literal "refactor" word, cross-process) — self-serve never fired in any of them, backup-first/small-units/verify-loop all failed as a consequence (L09) | round 4 |
| 10 | Self-serve never fired for refactor.md (L09) — needed a mechanical fix, unit-tested and live re-verified | `subtask-gate.ts`: block a session's first mutating call unless a protocols/*.md doc was read this session (one-shot, same trade-off as the other 2 checks). 6/6 unit tests pass; live re-run of round 4 trial 1's exact prompt: blocked once, model then read refactor.md, made a backup branch, stated the rollback command, ran real pytest, committed per file — no regression (independently re-verified) | round 4 |
| 11 | `refactor.md`/`build.md` never named a canonical verification command — round 4 trial 3 found the model trust a bare `python3 file.py` (no `__main__` block, silently ran 0 tests, exit 0) as "passed" | Both protocol docs now name the real invocation explicitly (`pytest <file>`/`npm test`/`npx tsc --noEmit`) and call out "0 exit + empty output is a red flag, not a pass" | session 5 |
| 14 | Objective blind audit (independent fresh agent, calibrated against the original `soulmate`'s own real rubric): soulmate-4 scored turnkey 74/100, structural 73/100 vs the original's 87/98.75 | Fixed all concrete findings same session: `AGENTS.md`/template zero-headroom bug (real repro: literal README step 1 broke the commit hook), `main`→`master` 404 in bootstrap.sh's own header, README file-tree staleness — all re-verified fixed (L09/L10, rule-archive.md Round 5) | session 5 |
| 15 | Audit's #1-priority + a live-reproduced gap: L09's gate was a true one-shot for the whole session (any *different* mutation after the first block sailed through unchecked forever), and no mechanism at all caught a completed mutation that was simply never committed | `subtask-gate.ts` hardened: L09 now re-checks on *every* mutating call, not just the first, until real compliance. Added `chat.message` hook (opencode has no true end-of-turn hook — confirmed from its own type defs) injecting a mechanical warning naming uncommitted leftovers on the next message; honest limitation stated (can't catch a session abandoned outright, only "next message in this session"). Found+fixed a real crash along the way (L10: opencode rejects non-`prt`-prefixed synthetic Part IDs). 10/10 unit tests + 3 separate live re-verifications (blocked-then-blocked-again, chat.message injection visible in `kilo export`, no false-positive on a clean tree) | session 5 |
| 17 | Round 7 objective re-audit: turnkey held at 81, structural **dropped** 77→69 — first regression in the loop. Two causes: `check_template_drift()` was ID-token-only (content-blind, live-verified: swapped a whole rule's body text, check passed silently); FEEDBACK #4 converted from "untested" to a confirmed live failure | Drift check rewritten to diff full content from `## Language` onward (byte-for-byte, not just IDs) — verified by swapping rule text again, now correctly fails. `chat.message` ambiguity nudge added for #4 (see above) — found+fixed a real bug in the same pass: `kilo run "<msg>"` wraps the text in a literal quote pair that matched the "quoted string" anchor on every single CLI message, so the nudge could never fire in normal usage; live-verified via debug log, fixed, re-verified live it now fires correctly | round 7 |
| 16 | Round 6 objective re-audit (independent fresh agent, re-cloned the original, re-verified all 5 round-5 fixes live): all 5 confirmed holding — turnkey 74→81 (+7), structural 73→77 (+4). Found `templates/AGENTS.md.template` had drifted *again*, same session, same bug class (L06-L08 merge landed but L09/L10 never synced) — check-caps.sh's line/row counts can't catch content drift, only length. Also: "10/10 unit tests" existed only as prose in rule-archive.md, no committed test file | Synced template fully (verified: `diff` of both files' Learned Rules/Sub-task-gate sections is now empty except the expected title placeholder); added `check_template_drift()` to check-caps.sh — diffs the Learned Rule ID set between `AGENTS.md`/template, `OVER CAP` on mismatch (tested: deliberately desynced, confirmed it fires, then confirmed it clears). Committed `tests/subtask-gate.test.mjs` for real (was scratch-only before), wired into `bootstrap.sh` so new projects inherit it too. Fixed stale FEEDBACK #4 text (said "first-mutation gate", now accurate). Re-verified full headroom chain after all this: fresh bootstrap + literal README step → 84/85 (was flickering back to 85/85 mid-fix as rows got added — tightened L04's wording in both files to restore margin) | round 6 |
| 3 | `subtask-gate.ts`'s one-shot disarm meant an immediate mutating call right after a block — verbatim retry or not — slipped through unconditionally, because `armed[sessionID]` was deleted the instant the first block fired | The clear moved out of `tool.execute.before` entirely, into `chat.message`, gated on a genuinely new user message arriving (the only mechanical proxy for "user was asked and responded", matching the block message's own text). L11. 4 new unit tests (T9a-d, 17/17 total). Live-verified against the real model+Kilo: real block fired with the exact error text, then `.kilo/plugins/.subtask-gate-state.json` inspected directly afterward — still shows `armed:"primer"` for that session, proof the pre-fix code's immediate deletion is gone | round 8 |
| 18 | Round 8 objective re-audit (scored round 7's 3 fixes) — both axes dropped (turnkey 81→78, structural 77→74); FEEDBACK #4/#12's nudge confirmed firing but not changing behavior (2nd live failure), plus a newly-found CLI-invisibility bug (`chat.message`'s injected warnings never appear in `kilo run`'s normal terminal output) | Fixed #3 (see row above). #4/#12: reworded the nudge to remove its self-granted "ignore if..." escape hatch, live-reproduced the old wording's 3rd failure first, then got 2/2 live passes with the new wording — real, measured improvement, reported honestly as partial (not solved: CLI-invisibility researched against the actual `@kilocode/plugin` type defs and confirmed there's no hook that renders to the terminal outside a thrown tool error, so that half stays open with no code fix possible) | round 8 |
| 19 | Round 9 objective re-audit (scored round 8's fixes) — turnkey 78→~70, structural 74→~68 (different grader's own estimate, not a strict continuation of this repo's internal formula, so noise is expected); independently re-confirmed FEEDBACK #3 and #4/#12's round-8 fixes both hold under fresh adversarial live trials; found 3 new issues: (a) `SESSION_PRIMER.md`/`session-log.md`/`FEEDBACK_PENDING.md` mutually inconsistent — title said round 8 not started when it was done, session-log had no round-8 entry, this table's own row #18 mislabeled round 8's numbers as "round 7" (now fixed above); (b) `AGENTS.md` zero-headroom cap regression, 3rd recurrence; (c) L01 cited the wrong npm package path (`@opencode-ai/plugin` vs the actually-shipped `@kilocode/plugin`) | (a) doc self-consistency fixed this round — see this table's own edit + `SESSION_PRIMER.md` round table rows 8-9 + `session-log.md` entries 11-12. (b) `AGENTS.md` compressed 85→75 lines (Learned Rules L01-L03/L05-L11 merged, all IDs preserved), live-verified fresh bootstrap + fill project name + add File Map row now succeeds at 76/85 (was failing at 86/85). (c) L01 citation corrected to `@kilocode/plugin` at its real shipped path, substantive claim re-confirmed still correct | round 9 |
| 20 | Round 10 objective re-audit (scored round 9's 3 fixes) — turnkey ~70→76, structural ~68→77 (all held: fresh bootstrap+File-Map-row now lands at 76/85 with margin, retry-bypass still blocked live); found the *same* stale-narrative bug class recurring in 2 more places round 9's fix never checked: `README.md` (2 places — described the retry-bypass fix as unpatched, cited a non-existent AGENTS.md rule; described Rules as living in PROJECT_BACKGROUND.md, stale since session 4) and `.kilo/plugins/subtask-gate.ts:324` (same wrong `@opencode-ai/plugin` citation round 9 only fixed in the doc copy, not the source comment) | Fixed both `README.md` spots + the plugin source citation, then ran a repo-wide `grep -rn "@opencode-ai/plugin"` sweep (6 total hits — 2 now-fixed live-artifact hits, 4 correctly-historical mentions left as-is) plus a broader stale-language sweep across README.md/AGENTS.md/wiki/protocols/*.md — nothing else found stale at the time | round 10 |
| 21 | Round 11 objective re-audit (scored round 10's fixes) — turnkey 76→80, structural 77→78 (all held, incl. independently verifying `@kilocode/plugin` against the real installed binary); found a **4th instance of the same recurring bug**, this time in `scripts/check-caps.sh:409-411` itself (claimed the gate-vs-fresh-bootstrap combo "hasn't yet been independently verified" when it had been, twice — rounds 9 and 10) — diagnosed the actual root cause as *process*, not content: every sweep so far manually hand-picked which files to check, so each round's fix only closed the file class the previous audit happened to flag | Fixed the concrete `check-caps.sh:409-411` claim. Then built the architectural fix instead of another point-patch (mirrors the original soulmate's own round-11 lesson, L04: replace N hand-rolled scans with one mechanism that structurally can't miss a class): `check_stale_language()` in `check-caps.sh` — one mechanical `git grep` sweep over every tracked file (short pattern list + short historical-file exemption list), wired into the normal check-caps.sh run (and therefore the pre-commit hook) so no future round has to remember to re-grep by hand. Live-verified: fires on injected staleness in a live file, stays quiet on the identical text in an exempted historical file, and a real false positive (this file's own past-tense description of the patterns) was found and fixed by rewording, not by expanding the exemption list | round 11 (audit) → round 12 (fix) |
| 22 | Round 12 objective re-audit (scored round 11's fix) — turnkey 80 (held), structural 78→74 (**regressed**); the retry-bypass gate and unit tests (17/17) held under a 5th round of adversarial live testing, but round 12 found `check_stale_language()` — the round-11 mechanism built specifically to close the recurring "narrated as more capable than it is" bug — **had exactly that bug itself**: it printed WARN but never set `status=1`, so a real pre-commit attempt with a staged stale-claim injection landed anyway. Round 12 verified this by actually installing the hook and committing, not just reading the code. Also confirmed `FEEDBACK_PENDING.md` was exempt whole-file, not just its Completed-history section | `check_stale_language()` now sets `status=1` on a match (same convention as this script's other hard-FAIL checks), and `FEEDBACK_PENDING.md`'s exemption narrowed from whole-file to just its "## Completed history" section (clean heading boundary already used elsewhere) — its open table is swept like any other live-state file now. Live-verified before committing: baseline clean (0 exit); injected staleness into a never-swept file → blocks; injected into FEEDBACK_PENDING's open table (before the heading) → blocks; same text after the heading → stays clean; installed the real pre-commit hook and attempted a real commit with a stale injection staged → genuinely rejected this time (exit 1) | round 12 (audit) → round 13 (fix) |
