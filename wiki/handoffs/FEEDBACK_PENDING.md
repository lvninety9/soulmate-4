# Feedback / Issue Tracking

Valid `status` values: `open` (not started) | `in-progress` | `blocked` (waiting on something
external — note what) | `done`.

Valid `priority` values: `p0` (blocking/urgent) | `p1` (normal) | `p2` (someday) — lets a
20+-row table stay triageable at a glance instead of every row reading as equally urgent.

| # | Feedback / issue | Priority | Status | How it's handled | Session logged |
|---|---|---|---|---|---|
| 2 | Custom project slash commands (`.kilo/commands/*.md`) don't work in Kilo's CLI as of v7.4.20 — may change in a future Kilo release, which would make `wiki/protocols/*.md`'s self-serve framing obsolete in a good way | p2 | open | Documented in AGENTS.md "Known gap" and L02; watch Kilo's changelog periodically | seed |
| 4/12 | "discuss" self-serve failure (3/3 confirmed live rounds 7-8) — an ambiguous ask went straight to build/refactor-shaped action with zero clarifying questions, once even editing `subtask-gate.ts` itself off a self-generated interpretation. Merged with #12: same underlying gap seen from the structural side — `discuss.md` is the one protocol step producing zero tool calls, so `subtask-gate.ts` can't hook into it the way L09 does for mutations | p1 | open (partial mitigation, permanent structural ceiling for the rest) | Round 8: nudge reworded from a soft suggestion with an "ignore if..." opt-out to a direct imperative with none — 2/2 fresh live trials passed (model asked clarifying questions), up from 0/3 with the old wording. Still just a nudge, not a block, and confirmed unfixable with the current plugin API: `chat.message`'s only output is the *incoming user message's own parts* (`@kilocode/plugin` type defs), never rendered in `kilo run`'s normal terminal output — invisible to a human watching live, only visible via `kilo export`. No `toast`/`notify` hook exists anywhere in the shipped `Hooks` interface. This ceiling is unchanged as of round 27 (`SESSION_PRIMER.md`'s "Known open issues") | round 2 → confirmed round 7 → stronger fix + CLI-invisibility confirmed round 8 → reconfirmed round 9 |
| 6 | After a real gate block, the model's self-report was observed fabricating a "done, file written" claim — round 2, round 5's own regression test, round 8, and round 27 (tried tag-wrapping the warning text as a mitigation — did not help, see completed row #38) | p1 | open, permanent ceiling | Inherent LLM self-report unreliability, same shape as soulmate-3's own L06 — no plugin-level fix possible; always verify actual file/git state (or the persisted `.subtask-gate-state.json` directly), never the model's summary, after any gate block. Round 27 additionally tried and rejected a `<system-reminder>`-tag mitigation (7 live trials, no demonstrated benefit) — extends the ceiling, doesn't close it | round 2, reconfirmed round 8, extended round 27 |
| 40 | S7's smoke test (round 28, live via kilo + local Qwen) found a real gate-bypass gap while dogfooding the new benchmark script: build.md step 3's "stage SESSION_PRIMER.md's Current sub-task update with the sub-task's last file" rule can be satisfied in words (`git add <file> wiki/handoffs/SESSION_PRIMER.md`) without being satisfied in substance — the model ran that exact `git add` twice in one trial but never called Edit/Write on SESSION_PRIMER.md first, so the file had no real diff; `git show --stat` on both resulting commits confirms neither actually touched it. `subtask-gate.ts`'s own commit detection (`git diff-tree`, not string-matching the bash command) correctly judged "not touched" both times — the plugin isn't lying, the model's own claimed action was. Net effect: the gate never armed at either sub-task boundary, so the next "continue" sailed through with zero block — this project's core safety property (mechanically forced stop between sub-tasks) silently didn't fire | p1 | open | Not investigated further this round — row #39's brief was S1-S7 only, no new fixes beyond that scope. Reproduction: `scripts/harness-integration-test.sh 1 <workdir>`, then `git show --stat` on the trial's 2nd sub-task commit, compare against the model's own stated `git add` command in `<workdir>/trial-1.log` | round 28 |
| 41 | Jay's real 5-trial live run of S7's benchmark (round 28, real kilo + local Qwen3.6-35B, not this repo's own smoke test): Step 5 (gate blocks live) **0/5** — worse than row #40's gap, a distinct root cause, live-diagnosed by reading `subtask-gate.ts` + `.subtask-gate-state.json` directly (not guessed): trial-5 is the one trial where `design` genuinely ran and genuinely committed a real SESSION_PRIMER.md edit (`git show --stat` confirms real diff, unlike row #40) — `armed[sessionID]="primer"` really got set in `tool.execute.after`. But the model, correctly following `design.md`'s own instruction ("commit, then stop — don't start executing"), made no further tool call that turn and just replied with text. `chat.message`'s clearing logic — "a new message starting is the only available proxy for the user having seen the block message" (its own code comment) — deletes `armed[sessionID]` on ANY next message, with no check that a block ever actually fired. So the very next message ("continue") silently disarms the gate before its first tool call, and that call sails through unblocked. **The more faithfully the model follows design.md's "stop and wait" instruction, the more reliably the gate gets bypassed** — confirmed via the state file (`armed: {}` post-trial) plus the source read, not inference from the transcript alone. Steps 4/6's own low scores (1/5 each) are a separate, non-bug effect worth distinguishing (Opus's own H1 standard): 4/5 trials had the model correctly apply design.md's own "skip design for a small, clearly-scoped task" rule and go straight to a real, multi-commit build — `git log` on those trials shows normal per-file commits, just never through this script's `design` step, so the model wasn't misbehaving there | p1 | open | Not investigated further this round (same S1-S7-only brief as row #40) — likely fix direction for whoever picks this up: `chat.message`'s clear should only fire if a block message was actually delivered this session (e.g. track a `wasBlocked[sessionID]` flag set only inside the `throw new Error(BLOCK_MESSAGE_*)` paths, cleared alongside `armed`), not on architected state clearing itself. Full transcripts + `.subtask-gate-state.json` snapshots kept at `/tmp/sm4-hit-full/trial-{1..5}` on the machine this was run on (not committed, may not survive a reboot) | round 28 |
| 42 | Jay's live 5-trial discuss test (round 28, via Cursor's Kilo Code **plugin**, not the CLI rows #40/41 used) directly contradicts row #4/12's round-8 "2/2 fresh live trials passed" conclusion. `kilo export` on all 5 fresh sessions confirms the exact same nudge from row #4/12 (round 8's direct-imperative rewording, unchanged since) landed correctly in every session's first message: `"[subtask-gate] STOP before doing anything else... ask the user 1-2 clarifying questions... Do not assume an interpretation and proceed"`. Despite that, the model ignored it in **5/5** — every first assistant response acknowledged the ask was ambiguous, then self-decided scope and moved toward building without ever asking a real question (verified: zero `?` in any of the 5 `kilo export` transcripts' first substantive response). Round 8's "fixed" conclusion was drawn from n=2 — too small a sample (the external review's own H2 standard, row #39). Notably the exact inverse of row #39's S7 CLI benchmark, which scored this identical scenario 5/5 PASS via `kilo run` in the same repo state — a real behavioral difference between the CLI and the Cursor plugin invoking the same model/plugin/prompt text, cause not yet identified (agent-preset default, prompt-delivery mechanism, and sampling params are all still-open candidates) | p1 | open | Not investigated further this round (S1-S7-only brief). Updates row #4/12's now-stale "2/2 passed" claim without editing that row's own text (round 28 preserves this file's append-only-within-a-round discipline) — whoever revisits row #4/12 should treat it as still-open with a stronger evidence base (n=5 fail via plugin, not n=3 pass via presumably-CLI) and check the CLI/plugin behavioral gap as a possible root cause before trying another nudge-wording change. `kilo export <sessionID>` (or `kilo session list`) is a reliable way to pull exact transcripts for re-grading — found live this round, not previously documented anywhere in this repo | round 28 |
---

## Completed history

<!-- rows move to wiki/FEEDBACK_PENDING-archive.md the moment they're done — this section stays
     a pointer only, never a table, so this file (auto-loaded per wiki/protocols/discuss.md's
     Rule Zero grep target) can't silently regrow into pure narrative again (round 28, S5). -->

All resolved rows (#1-#38, rounds 1-27) are in `wiki/FEEDBACK_PENDING-archive.md`, not auto-loaded.
Nothing deleted, only relocated.
