# Feedback / Issue Tracking

Valid `status` values: `open` (not started) | `in-progress` | `blocked` (waiting on something
external — note what) | `done`.

Valid `priority` values: `p0` (blocking/urgent) | `p1` (normal) | `p2` (someday) — lets a
20+-row table stay triageable at a glance instead of every row reading as equally urgent.

| # | Feedback / issue | Priority | Status | How it's handled | Session logged |
|---|---|---|---|---|---|
| 2 | Custom project slash commands (`.kilo/commands/*.md`) don't work in Kilo's CLI as of v7.4.20 — may change in a future Kilo release, which would make `wiki/protocols/*.md`'s self-serve framing obsolete in a good way | p2 | open | Documented in AGENTS.md "Known gap" and L02; watch Kilo's changelog periodically | seed |
| 3 | `subtask-gate.ts`'s one-shot disarm means an immediate verbatim retry of a blocked call *would* slip through — round 2 confirmed this is real, not hypothetical (retried a blocked write in a new process, succeeded unconditionally), exactly as documented | p1 | open | Matches the documented "Known gap" precisely; AGENTS.md's "no verbatim 3rd retry" rule is the only thing discouraging this, no mechanical layer exists | round 2 |
| 4 | "discuss" self-serve failure, now confirmed live (round 7): an ambiguous ask ("this feels slow, can you help?") went straight to refactor.md and committed with zero clarifying questions — L09's gate guarantees *some* protocol doc read, never specifically discuss.md | p1 | open (partial mitigation shipped) | Round 7: `chat.message` heuristic nudge added (no backtick/file-ext/quoted-string anchor + >15 chars → inject a discuss.md suggestion). Nudge only, not a block (discuss.md produces zero tool calls, no hook can force it) — N=1 live trial so far, needs 2 more per this repo's own 3-trial confidence bar | round 2 → confirmed round 7, partial fix round 7 |
| 6 | After a real gate block, the model's self-report was observed fabricating a "done, file written" claim once (round 2) and once more in this session's own regression test — the tool result showed `status: "error"` both times, contradicting the model's own summary | p1 | open | Inherent LLM self-report unreliability, same shape as soulmate-3's own L06 — no plugin-level fix possible; always verify actual file/git state, never the model's summary, after any gate block | round 2 |
| 12 | `discuss.md` has no mechanical backstop — the one protocol step producing zero tool calls, `subtask-gate.ts` structurally can't hook into it the way L09 does for mutations | p2 | open (heuristic nudge shipped round 7, see #4) | `chat.message` can inject text but can't block or force Q&A — this is the ceiling of what's possible without a real pre-response hook opencode doesn't have | session 5, mitigated round 7 |
---

## Completed history

<!-- rows move here the moment they're done -->

| # | Feedback / issue | How it was handled | Session resolved |
|---|---|---|---|
| 1 | `scripts/bootstrap.sh` had never been run end-to-end; Round 1 blind validation found it fails on a completely fresh checkout (AGENTS.md.template 1 line over its own cap after comment-stripping), leaks its `/tmp` scratch clone on that failure, and never copies `templates/harness-integration-test.md`/`cold-read-test-prompt.md` into the target | Trimmed the template under cap, decoupled the README one-liner's cleanup from bootstrap.sh's exit code, made bootstrap.sh copy the two verification templates and strip comments from all 4 templates consistently; re-ran the real one-liner end-to-end and confirmed `--bootstrap-check` passes | round 1 |
| 7 | Gate's trigger was 100% elective (L07) — round 2 reproduced a full silent multi-sub-task chain with the gate never arming | `tool.execute.after` now checks real committed files via `git diff-tree`; N non-primer commits in a row arms anyway; verified via unit test + real 2-process Kilo run | round 2 |
| 8 | Fresh bootstrap's AGENTS.md sat at 58/60 before any customization, 59/60 after the README's own instructed step (add a File Map row) — one line from a hard fail | Raised cap 60→65 (content already near-minimal); re-verified via a real bootstrap + realistic row add, now 59/65 with real headroom left | round 2 |
| 5 | `COMMITS_WITHOUT_PRIMER_THRESHOLD=4` untested against real multi-file work | Round 3: confirmed no false positive on 3 legitimate commits, correct primer-vs-elective distinction; separately found+fixed the detection itself (regex→real git HEAD diff, L08) | round 3 |
| — | AGENTS.md's Learned/Fixed Rules split into PROJECT_BACKGROUND.md re-created soulmate-3's own L02 risk (rule referenced but not stated in the auto-loaded file); check-caps.sh's Learned/Fixed checks were also silently still targeting AGENTS.md the whole time | Verified against the real original soulmate repo (not assumed): merged Rules back into AGENTS.md, cap 65→85 matching the original exactly; re-bootstrapped and caught+fixed one more off-by-one before push | session 4 (architecture realignment) |
| 9 | `wiki/protocols/refactor.md` had never been exercised by any blind validation round | Round 4: 3 independent blind trials (abstract framing, literal "refactor" word, cross-process) — self-serve never fired in any of them, backup-first/small-units/verify-loop all failed as a consequence (L09) | round 4 |
| 10 | Self-serve never fired for refactor.md (L09) — needed a mechanical fix, unit-tested and live re-verified | `subtask-gate.ts`: block a session's first mutating call unless a protocols/*.md doc was read this session (one-shot, same trade-off as the other 2 checks). 6/6 unit tests pass; live re-run of round 4 trial 1's exact prompt: blocked once, model then read refactor.md, made a backup branch, stated the rollback command, ran real pytest, committed per file — no regression (independently re-verified) | round 4 |
| 11 | `refactor.md`/`build.md` never named a canonical verification command — round 4 trial 3 found the model trust a bare `python3 file.py` (no `__main__` block, silently ran 0 tests, exit 0) as "passed" | Both protocol docs now name the real invocation explicitly (`pytest <file>`/`npm test`/`npx tsc --noEmit`) and call out "0 exit + empty output is a red flag, not a pass" | session 5 |
| 14 | Objective blind audit (independent fresh agent, calibrated against the original `soulmate`'s own real rubric): soulmate-4 scored turnkey 74/100, structural 73/100 vs the original's 87/98.75 | Fixed all concrete findings same session: `AGENTS.md`/template zero-headroom bug (real repro: literal README step 1 broke the commit hook), `main`→`master` 404 in bootstrap.sh's own header, README file-tree staleness — all re-verified fixed (L09/L10, rule-archive.md Round 5) | session 5 |
| 15 | Audit's #1-priority + a live-reproduced gap: L09's gate was a true one-shot for the whole session (any *different* mutation after the first block sailed through unchecked forever), and no mechanism at all caught a completed mutation that was simply never committed | `subtask-gate.ts` hardened: L09 now re-checks on *every* mutating call, not just the first, until real compliance. Added `chat.message` hook (opencode has no true end-of-turn hook — confirmed from its own type defs) injecting a mechanical warning naming uncommitted leftovers on the next message; honest limitation stated (can't catch a session abandoned outright, only "next message in this session"). Found+fixed a real crash along the way (L10: opencode rejects non-`prt`-prefixed synthetic Part IDs). 10/10 unit tests + 3 separate live re-verifications (blocked-then-blocked-again, chat.message injection visible in `kilo export`, no false-positive on a clean tree) | session 5 |
| 17 | Round 7 objective re-audit: turnkey held at 81, structural **dropped** 77→69 — first regression in the loop. Two causes: `check_template_drift()` was ID-token-only (content-blind, live-verified: swapped a whole rule's body text, check passed silently); FEEDBACK #4 converted from "untested" to a confirmed live failure | Drift check rewritten to diff full content from `## Language` onward (byte-for-byte, not just IDs) — verified by swapping rule text again, now correctly fails. `chat.message` ambiguity nudge added for #4 (see above) — found+fixed a real bug in the same pass: `kilo run "<msg>"` wraps the text in a literal quote pair that matched the "quoted string" anchor on every single CLI message, so the nudge could never fire in normal usage; live-verified via debug log, fixed, re-verified live it now fires correctly | round 7 |
| 16 | Round 6 objective re-audit (independent fresh agent, re-cloned the original, re-verified all 5 round-5 fixes live): all 5 confirmed holding — turnkey 74→81 (+7), structural 73→77 (+4). Found `templates/AGENTS.md.template` had drifted *again*, same session, same bug class (L06-L08 merge landed but L09/L10 never synced) — check-caps.sh's line/row counts can't catch content drift, only length. Also: "10/10 unit tests" existed only as prose in rule-archive.md, no committed test file | Synced template fully (verified: `diff` of both files' Learned Rules/Sub-task-gate sections is now empty except the expected title placeholder); added `check_template_drift()` to check-caps.sh — diffs the Learned Rule ID set between `AGENTS.md`/template, `OVER CAP` on mismatch (tested: deliberately desynced, confirmed it fires, then confirmed it clears). Committed `tests/subtask-gate.test.mjs` for real (was scratch-only before), wired into `bootstrap.sh` so new projects inherit it too. Fixed stale FEEDBACK #4 text (said "first-mutation gate", now accurate). Re-verified full headroom chain after all this: fresh bootstrap + literal README step → 84/85 (was flickering back to 85/85 mid-fix as rows got added — tightened L04's wording in both files to restore margin) | round 6 |
